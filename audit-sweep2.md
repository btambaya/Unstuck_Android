## HIGH (50)

### [Task CRUD + detail (create/e] Scheduling a task ALWAYS bumps moveCount — even first-time scheduling from the create sheet
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:154
- Web only increments moveCount when an EXISTING block is moved. The create path (task-create-modal.tsx:254-282) calls upsertBlock and NEVER touches moveCount; ScheduleTaskModal.persistOrMove (schedule-task-modal.tsx:56-79) bumps only when it finds an existing block whose date/time actually changed (line 65). Android AppViewModel.scheduleTask unconditionally ends with `write?.upsertTask(bumpMoveCount(task, isoNow()))` (AppViewModel.kt:174) — there is no first-time/reschedule distinction. NewTaskSheet submit calls vm.scheduleTask(t, ...) for every brand-new scheduled task (NewTaskSheet.kt:193), so every Android-created scheduled task is born with moveCount=1. DayGrid drag-to-schedule (DayGrid.kt:108) and the detail-pane Schedule button (TaskDetailSheet.kt:112) also route through scheduleTask, so the very first placement also bumps. isSlipping (VisibleTasks.kt:38, SLIP_MOVE_THRESHOLD=3) and analytics read moveCount, so Android tasks slip toward Backlog faster than web tasks for an identical history.
- fix: Do not bump moveCount for first-time placement. Mirror web persistOrMove: if the task already has a task-shaped cal_block, update that block in place (block.copy(date,startTime)) and bump moveCount only when date/time changed; otherwise create a new block and do NOT bump. Add a dedicated rescheduleTask() (or a bump flag) and have NewTaskSheet/DayGrid first-create call the non-bumping path.

### [Task CRUD + detail (create/e] Detail-pane 'Schedule' creates a duplicate block every tap and re-materialises recurrence
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:110
- Web ScheduleTaskModal.persistOrMove finds an existing block (blocks.find(b => b.taskId === task.id), schedule-task-modal.tsx:62) and reschedules it in place to prevent the 'click Schedule N times -> N blocks' bug. Android TaskDetailSheet.kt:110-113 calls vm.scheduleTask which, for a non-recurring task, always does upsertCalBlock(CalBlock(id = newUuid(), ...)) (AppViewModel.kt:169) — a fresh row each time. The 'scheduled' state label is set but the button is not disabled, so re-taps produce overlapping blocks. For a RECURRING task it is worse: scheduleTask runs materializeOccurrences and upserts a whole new horizon of brand-new-uuid blocks (AppViewModel.kt:163-167) with no dedup against existing blocks, duplicating every future occurrence. The codebase already has regenerateForTask (Recurrence.kt:60) which diffs against existing blocks and is used by setRecurrence — scheduleTask does not use it.
- fix: Route detail-pane Schedule through a reschedule-in-place path: if a task-shaped cal_block exists, upsert that same block id with the new date/startTime; for recurrence use regenerateForTask (already ported) to diff instead of materializeOccurrences which always inserts.

### [Tags (vocabulary, filter, pi] Tag filter (?tag=NAME) is completely unwired in the Android UI
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TasksScreen.kt:72,123-131; app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:70,106
- Verified. Core supports it: VisibleTasks.kt:50-57 declares `activeTag: String? = null` and :98-99 AND-combines a case-insensitive tag filter onto every view including Today, and matchesTag exists at :30-33. But the UI never feeds it: TasksScreen.kt:72 calls `visibleTasks(view, tasks, blocks, vm.nowMs(), activeArea = if (view==TODAY) null else activeArea, slipMode = false)` with NO activeTag argument; MainScaffold.kt:70 only declares `var activeArea` (no activeTag), and the TasksScreen call at MainScaffold.kt:106 passes no tag. The row tag chips at TasksScreen.kt:123-131 are plain non-clickable Box (no .clickable{}), and there is no 'Filtering by tag' banner / clear affordance. Web: list-row.tsx:117 and dashboard/task-row.tsx:340 both `router.push('/tasks?tag=${encodeURIComponent(name)}')`, and task-list-pane.tsx:156-189 renders the 'Filtering by tag #NAME · clear' banner. So a user can create/apply tags but can never filter by them.
- fix: Add `var activeTag by remember { mutableStateOf<String?>(null) }` in MainScaffold, pass it to TasksScreen and forward to visibleTasks(... activeTag = activeTag). Make the TasksScreen.kt row tag chips clickable to set activeTag (mirror list-row onClickTag), render a 'Filtering by tag #NAME · clear' banner like task-list-pane.tsx:156-189, and wire dashboard/today task-row chips to switch to the tasks tab with the tag preset.

### [Tags (vocabulary, filter, pi] Rename cascade does not dedupe — produces duplicate tag entries on a task
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:318
- Verified. AppViewModel.kt:318 remaps with `tags = t.tags?.map { if (it == tag.name) nm else it }` and never dedupes, so a task tagged ['B','A'] renamed A→B becomes ['B','B']. Web cascadeRenameTaskTag dedupes after remap (use-tags.ts:50-51 `Array.from(new Set(remapped))`). That duplicate then renders twice and breaks chip/filter uniqueness.
- fix: Dedupe after remap: `tags = t.tags?.map { if (it == tag.name) nm else it }?.distinct()`.

### [Tags (vocabulary, filter, pi] Rename cascade is case-sensitive — diverges from web's case-insensitive matching
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:317-318
- Verified. AppViewModel.kt:317 filters tasks with `it.tags?.contains(tag.name) == true` and :318 remaps with strict `if (it == tag.name)`. Web's tag matching is case-insensitive throughout (visible-tasks.ts:127, tag-picker.tsx:69/77-83 lowerValue Set). If a task stored the tag with different casing than the vocab entry, Android's rename silently skips that task, orphaning the old-name string; web would still remap it.
- fix: Match case-insensitively: filter `it.tags?.any { n -> n.equals(tag.name, ignoreCase = true) } == true` and remap `if (it.equals(tag.name, ignoreCase = true)) nm else it`, then `.distinct()`.

### [Tags (vocabulary, filter, pi] Delete-tag cascade is absent — task.tags keep the dead tag name forever
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:302; app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:357
- Verified. AppViewModel.kt:302 `fun deleteTag(id: String) = launchWrite { write?.deleteTag(id) }` only deletes the TagRow vocabulary entry; it never scrubs tasks. SettingsScreen.kt:357 passes only `tag.id`. Web delete cascades: use-tags.ts:116-124 remove() calls cascadeRenameTaskTag(removed.name, undefined, uid) which removes the name from every task's tags array and re-upserts. Result on Android: deleted tags stay stuck on every task (still render as chips, still match a would-be tag filter), permanently diverging from web.
- fix: Make deleteTag take the TagRow (or look it up), and after write?.deleteTag(id) iterate tasks.value; for any task whose tags contains the name (case-insensitive) upsert it with the name removed: `t.copy(tags = t.tags?.filterNot { it.equals(name, true) }?.ifEmpty { null }, updatedAt = isoNow())`. Update SettingsScreen.kt:357 to pass the tag.

### [Tags (vocabulary, filter, pi] No delete confirmation for tags (web requires a two-step confirm)
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:357
- Verified. The Tags menu deletes immediately on a single tap: SettingsScreen.kt:357 `DropdownMenuItem(... onClick = { menu = false; vm.deleteTag(tag.id) })`. The Areas panel in the same file correctly uses a confirm AlertDialog (SettingsScreen.kt:302-309). Web TagPanel requires a two-step confirm: first click flips the menu label to 'Delete forever?' and only the second deletes (tag-panel.tsx:294-304 confirmDelete). One stray tap permanently deletes a tag (and, once the cascade is added, scrubs it from all tasks) with no undo.
- fix: Add a `confirm` flag + AlertDialog like AreasContent (SettingsScreen.kt:302-309): the Delete menu item sets confirm=true; dialog 'Delete "#name"? Tasks lose this label.' with Delete/Cancel.

### [Today / Start-Next / Up-Next] Live-session card on Today never ticks — timer + ring are frozen
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:84
- CONFIRMED. TodayScreen reads `val now = vm.nowMs()` once per recomposition (TodayScreen.kt:84). `AppViewModel.nowMs()` is a plain `fun nowMs(): Long = System.currentTimeMillis()` (AppViewModel.kt:85) — there is no `now` StateFlow / ticker flow in the VM (grep found none). There is NO `LaunchedEffect { while(true){ now=…; delay(1000) } }` in TodayScreen, unlike FocusScreen.kt:66 (`LaunchedEffect(Unit) { while (true) { nowMs = System.currentTimeMillis(); delay(1000) } }`). LiveSessionCard derives `elapsed = FocusTimer.elapsedSec(live, now)` and `progress` from that frozen `now` (TodayScreen.kt:208-210), and renders `formatMMSS(elapsed)` at lines 225/229 and the progress arc at 223. Recomposition only fires when a collected flow emits (pause/resume mutates liveSession), so between user actions the on-Today timer is dead. Web TodayList re-ticks `now` every 60s (today-list.tsx:96-101) AND the LiveTaskRow re-derives from `useFocusTimer` which ticks every second, so web stays live.
- fix: Drive `now` from a ticker in TodayScreen: `var now by remember { mutableLongStateOf(vm.nowMs()) }; LaunchedEffect(live != null) { while (true) { now = vm.nowMs(); delay(if (live != null) 1000 else 60_000) } }`, and use it for the LiveSessionCard plus the completed-today / backlog-age derivations (which currently also use the frozen `now`).

### [Today / Start-Next / Up-Next] No PausedTaskRow — tasks with partial progress get no Resume affordance on Today
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:241
- CONFIRMED. Web TaskRow renders a third variant PausedTaskRow for any non-live task where `0 < totalFocused < estimateMin*60` (task-row.tsx:40-44 isPausedTask, 68-69, 113-149): a rich amber-ring card showing accumulated time with a single Resume button. Android's Today TaskRow (TodayScreen.kt:241-270) only renders the plain compact row — it never reads `totalFocused` (which DOES exist on the model, Models.kt:82) and has no Resume button. `TaskItem.totalFocused` is available, so the data is there; the affordance simply isn't surfaced. CORRECTION to the proposed fix: web's PausedTaskRow Resume calls `timer.start(task.id, estimate, { priorAccumulatedSec: task.totalFocused })`, but Android's `vm.startFocus(task)` calls `FocusTimer.start(cur, task.id, estimateMin = task.estimateMin, now = …)` WITHOUT passing `priorAccumulatedSec` (AppViewModel.kt:202-205). So a Resume that just calls `startFocus(task)` would NOT continue from the accumulated total — it would restart the displayed timer at the prior progress only via the resume branch, and for a non-live paused-elsewhere task it starts fresh at 0. A new resume entry point that threads `priorAccumulatedSec = task.totalFocused` into the start is required, not a bare `startFocus`.
- fix: Add an `isPausedTask` check (`!done && totalFocused in 1 until estimateMin*60`) and render an amber-ring rich row with elapsed = task.totalFocused. The Resume action must seed prior progress — add an overload `vm.startFocus(task, priorAccumulatedSec = task.totalFocused)` (or a `vm.resumeTask(task)`) that passes `priorAccumulatedSec` into `FocusTimer.start`, then call `onStartFocus(task)`. A bare `vm.startFocus(task)` is NOT sufficient (it drops priorAccumulatedSec).

### [Focus: treatments, pause-rea] Overrun extend flow is entirely absent (Add 10 min / I'm in the zone / Stop here)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:140-148
- Web controls.tsx state==='overrun' (lines 131-150) renders a soft check-in ('You've gone past your estimate... still going well?') plus three buttons: 'Add 10 min · keep going' (onExtend(10)), 'I'm in the zone' (onExtend(15)), 'Stop here · I'm losing thread' (onEndSession). Cockpit additionally shows a flame banner (cockpit.tsx:230-251). On Android FocusScreen.kt the only overrun behavior is the big timer turning coral (line 131, `if (state == FocusState.OVERRUN) c.coral`); the action Row at lines 140-148 is identical in every state (Capture / Pause / Done) — there is no check-in copy and NO extend buttons. vm.extendFocus(minutes) exists (AppViewModel.kt:215) but is called by nothing, so a user can never extend an estimate.
- fix: When state==FocusState.OVERRUN, render overrun-specific buttons: 'Add 10 min' → vm.extendFocus(10), 'I'm in the zone' → vm.extendFocus(15), 'Stop here' → finishFocus(task). Add the soft check-in text. Keep Pause/Capture available too as cockpit does.

### [Focus: treatments, pause-rea] Soft-overrun setting (focusOverrunMin) is ignored — overrun always fires at +1s
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:84
- Web reads PREF_FOCUS_OVERRUN ('Never'|'5 min'|'10 min') via overrunGraceSeconds() and passes the seconds (Infinity=never) to deriveState (use-focus-timer.ts:54-78,164). FocusTimer.kt ports overrunGraceSeconds() and a grace-aware deriveState correctly (lines 41-60), but FocusScreen.kt:84 hardcodes `FocusTimer.deriveState(l, nowMs, 1.0)` — it never reads settings.focusOverrunMin. SettingsStore defaults focusOverrunMin=5 (line 25, 0=Never) and SettingsScreen exposes 'Soft overrun: Off/5/10' (lines 122-123), yet the timer flips to OVERRUN one second past estimate regardless. A user who set 'Off' (Never) still gets coral + (once the extend flow exists) the overrun prompt at +1s.
- fix: Compute grace from settings: `val grace = if (settings.focusOverrunMin == 0) Double.POSITIVE_INFINITY else settings.focusOverrunMin * 60.0` and pass it: `FocusTimer.deriveState(l, nowMs, grace)`. Use the same grace wherever overrun is derived.

### [Focus: treatments, pause-rea] No focus sounds — start chime, overrun bell, completion sound never play
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:64-84
- Web use-focus-timer.ts plays playChime() on a fresh start (line 223), playBell() on RUNNING→OVERRUN transition once per session via lastStateRef (lines 169-175), and playCompletion() in done() (line 259); use-sound.ts gates each on its pref. Android has SettingsStore flags soundStartChime/soundOverrunBell/soundCompletion (lines 29-31) and SettingsScreen toggles (lines 130-132), but a grep of app/src/main + core/src/main finds ZERO ToneGenerator/SoundPool playback and no playChime/playBell/playCompletion analog — the only audio code is AmbientAudio (MediaPlayer) which is unrelated to these three events. The three toggles are dead.
- fix: Add a small sound helper (SoundPool or short raw assets / ToneGenerator). Play start chime when startFocus creates a FRESH session (not a resume), gated on settings.soundStartChime. Track previous derived state in FocusScreen and on RUNNING→OVERRUN play bell once (settings.soundOverrunBell). Play completion in finishFocus (settings.soundCompletion).

### [Focus: treatments, pause-rea] Ambient sound is implemented but never wired; only one track so 'pink' is meaningless
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/surface/AmbientAudio.kt:11
- Web AmbientPlayer mounts once and watches PREF_SOUND_AMBIENT ('off'|'brown'|'pink'), calling setAmbient() which synthesizes distinct brown vs pink noise buffers (use-sound.ts:106-161), persisting across state transitions. Android has surface/AmbientAudio.kt (looping MediaPlayer on res/raw/ambient_focus.wav) and a Settings 'Ambient: off/brown/pink' SegRow that persists settings.ambient (SettingsScreen.kt:133, SettingsStore.kt:32), but a grep confirms AmbientAudio.start()/stop() is referenced by NOTHING outside its own definition. So selecting brown/pink does nothing, and there is a single track so brown and pink would be identical even once wired.
- fix: In FocusScreen add a DisposableEffect/LaunchedEffect keyed on settings.ambient: AmbientAudio.start(context) when != 'off', stop on 'off' and on dispose (mirror AmbientPlayer's mount-once + cleanup). Provide distinct brown vs pink tracks (or synthesize noise) so the choice is meaningful, or collapse the Settings option to on/off until pink exists.

### [Focus: treatments, pause-rea] Returning to a PAUSED session auto-resumes it instead of keeping it paused
- regression | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:64
- FocusScreen.kt:64 runs `LaunchedEffect(task.id) { vm.startFocus(task) }` on every mount. startFocus (AppViewModel.kt:202-207) calls FocusTimer.start, which for a same-task PAUSED session returns resume() (FocusTimer.kt:76 → silently unpauses by shifting sessionStart). Today's LiveSessionCard onReturn = onStartFocus(liveTask) (TodayScreen.kt:158) → MainScaffold sets focusTask (line 100) → FocusScreen mounts → the paused session is resumed just by opening it; the user lands on a running timer they never chose to restart. Web FocusMode never auto-starts on mount (focus-mode.tsx onStart is user-triggered only); opening /focus keeps the paused state and requires an explicit Resume tap.
- fix: Only auto-start when there is no live session for this task. Guard: if store live session exists for task.id (running or paused), do NOT call startFocus — render the existing state and let Pause/Resume drive transitions. Reserve startFocus for live==null or a different task.

### [Focus: treatments, pause-rea] firstPhysicalAction cue is never shown in Focus
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:127-130
- All three web treatments surface task.firstPhysicalAction while focusing: ambient renders 'First physical action · …' (ambient.tsx:124-136), cockpit a boxed FIRST PHYSICAL ACTION card (cockpit.tsx:142-166), monk uses it as the body line (monk.tsx:43-48). Android Models.kt has the firstPhysicalAction field (line 91) but FocusScreen.kt only renders task.name (line 128) and '${task.estimateMin}m estimate' (line 129) — it never references firstPhysicalAction. The key re-entry/anti-stuck cue is missing.
- fix: Render task.firstPhysicalAction (when non-null and state != done) below the task name as a labeled card/line, matching the treatment (monk as the body, cockpit as a boxed FIRST PHYSICAL ACTION).

### [Calendar: day grid (24h/now/] Move-count tracking is inverted vs web: first-time tray-schedule bumps move_count, but editing/rescheduling a block does NOT
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:174,177,180-182
- Verified. Web only bumps moveCount when an EXISTING block is dragged to a new slot (today-timeline.tsx:167-170; week-full.tsx:252-255; month-full.tsx:169-175) or when a block is deleted (use-tasks.ts:266 `if (removed?.taskId) void bumpMoveCount(...)`). Creating a fresh block from the tray drop does NOT bump it (today-timeline.tsx:179-186; week-full.tsx:264-271; month-full.tsx:183-190 all omit it). Android is reversed: vm.scheduleTask() ALWAYS calls write?.upsertTask(bumpMoveCount(task, isoNow())) (AppViewModel.kt:174) on every tray drop including the first schedule, and for a recurring task it bumps once even though it creates N occurrence blocks. Meanwhile vm.moveBlock() (AppViewModel.kt:180-182), the actual reschedule path used by the start-time chips, never bumps. vm.unschedule() (AppViewModel.kt:177) also never bumps, unlike web's remove(). The slip detector (Analytics.kt:156-168, threshold moveCount>=3) therefore gets inverted input: new schedules inflate slips, real reschedules/deletes count for nothing.
- fix: Remove the unconditional bumpMoveCount from scheduleTask. In moveBlock(), after upserting, bump the owning task's moveCount guarded by `block.taskId != null && (existing.startTime != startTime || existing.date != date)`. In unschedule(), bump the linked task's moveCount before deleting the block, mirroring web remove() (use-tasks.ts:259-269).

### [Calendar: day grid (24h/now/] Unscheduled-tray filter is scoped to the current day only — tasks scheduled on other days reappear as unscheduled and can be double-booked
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:96-98
- Verified. Web treats a task as scheduled if ANY cal_block references it across all dates: unscheduled-tray.tsx:31-33 `tasks.filter(t => !t.done && !blocks.some(b => b.taskId === t.id))`, and week-full.tsx:354-356 uses `!allBlocks.some(b => b.taskId === t.id)`. Android's DayGrid computes scheduledIds from ONLY the viewed day: dayBlocks = blocks.filter { it.date == date } (DayGrid.kt:96), scheduledIds = dayBlocks.filter { isTaskBlock(it) }.mapNotNull { it.taskId } (DayGrid.kt:97), then unscheduled = tasks.filter { ... it.id !in scheduledIds } (DayGrid.kt:98). A task scheduled tomorrow still appears in today's tray and can be dragged on again, creating a second block — exactly what web prevents.
- fix: Compute scheduledIds against ALL blocks, not just the day's: `val scheduledIds = blocks.filter { isTaskBlock(it) }.mapNotNull { it.taskId }.toSet()`. Keep dayBlocks only for grid rendering.

### [Calendar: day grid (24h/now/] Block-edit sheet is missing most of the web CalBlockEditModal's fields and actions
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:235-263
- Verified. Web's CalBlockEditModal (cal-block-edit-modal.tsx) edits name (211-229), first physical action (233-251), estimate as a number input kept in sync with block duration (262-273, save() at 124-132 sets durationMinutes=estimate), life area picker (276-338), tags via TagPicker (343), date+time inputs (349-360), shows a live findConflicts warning banner (92-95,369-391), and offers Start now (402), Mark complete (405), Open in tasks (408), Delete block (412), plus Cancel/Save (417-424) with a draft model. Android's CalBlockEditSheet (DayGrid.kt:235-263) only offers start-time chips (current time + up to 5 free-slot suggestions, DayGrid.kt:241-254), fixed duration chips 15/25/45/60/90 (DayGrid.kt:258), and Unschedule (DayGrid.kt:261). It cannot edit name, first action, area, tags, an arbitrary estimate/time/date; it has no conflict warning, no Start now, no Mark complete, no Open in tasks; every chip tap commits immediately (no Save/Cancel draft). The :core findConflicts (FreeSlots.kt:123) is available but unused here.
- fix: Expand the sheet to mirror cal-block-edit-modal: editable name/firstAction/estimate/area/tags, a date picker + free-form time picker, a findConflicts(date,startTime,duration,blocks,block.id) warning banner, and Start now / Mark complete / Open in tasks / Delete actions. On save, persist duration==estimate like web save() (cal-block-edit-modal.tsx:124-132).

### [Calendar: day grid (24h/now/] Block-time feature (create a busy block + backing task) has no entry point and the VM helper is semantically wrong
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:194-198
- Verified. Web's BlockTimeModal (block-time-modal.tsx) creates a real Task (submit() at 73-86) PLUS a task-shaped CalBlock (kind:'task', taskId=task.id, 87-95) from a label + date + FROM/TO time, shows live duration (194-198), warns via findConflicts (58-61,206-228), and toasts success (97). On Android vm.blockTime() exists (AppViewModel.kt:194-198) but (a) has ZERO callers — grep across app/src/main returns only the definition; and (b) is wrong: it creates CalBlock(taskId="placeholder", kind=PLACEHOLDER) with NO backing Task. blockKind() returns PLACEHOLDER for taskId=="placeholder" (CalBlockKind.kt:12), so isTaskBlock is false (CalBlockKind.kt:17), the block renders as a neutral c.bg2 swatch with no life-area color (DayGrid.kt:152-156), and it never appears in /tasks — unlike web where the block opens its backing task.
- fix: Add a Block-time entry point (FAB/menu action on CalendarScreen.kt). Rewrite blockTime() to upsertTask(real TaskItem with estimateMin=duration) + upsertCalBlock(kind=TASK, taskId=task.id, taskName=label). Build a sheet with label/date/from/to, live duration, findConflicts warning, and a success message, mirroring block-time-modal.tsx.

### [Google Calendar connect/pull] CalendarClient.listConnections() decodes snake_case Edge-Function JSON into camelCase core model -> MissingFieldException, silently kills every pull
- bug | sync/src/main/kotlin/tech/csalliance/unstuck/sync/CalendarClient.kt:22,42-43 (decode into camelCase model); SyncCoordinator.kt:93 (gate)
- VERIFIED. Edge Function GET /calendar-sync/connections returns rows straight from Postgres with credentials stripped: snake_case keys account_email, display_name, selected_calendar_ids, color_slot, last_sync_cursor, connected_at (index.ts:62-72 ConnectionRow; :247-251 handleListConnections does conns.map(({credentials,...rest})=>rest) -> jsonResponse({connections: safe}) — keys stay snake_case). CalendarClient.listConnections() does .body<ConnectionsResponse>() where ConnectionsResponse = List<core.model.CalendarConnection> (CalendarClient.kt:22,42-43). core.model.CalendarConnection (Models.kt:144-154) has REQUIRED non-null camelCase fields accountEmail/displayName/selectedCalendarIds/colorSlot/connectedAt and NO @SerialName. No custom serializer is installed on the Supabase client (SupabaseClientProvider.kt:24-35 just install(Functions)), so supabase-kt's default KotlinXSerializer is used — its Json has ignoreUnknownKeys=true but NO SnakeCase naming strategy and does not synthesize absent required fields, so the camelCase required fields are missing -> MissingFieldException. pullCalendar gates on `runCatching { calendar.listConnections() }.getOrNull() ?: return` (SyncCoordinator.kt:93) BEFORE pulling events, so Google events are never ingested whenever the user actually has a connection. Note: connect path (/connect -> ConnectResponse with accountEmail/colorSlot, index.ts:218-223) and events path (/events -> ExternalEvent calendarId/connectionId) decode fine; only the listConnections gate is broken, and it fronts every pull (sign-in :133, connect :86, manual :191, worker :66). The connected-state of the bar is fed by PostgREST via Hydrator (Hydrator.kt:34 decodes with DbRowCodec.CalendarConnectionRow's @SerialName), NOT by listConnections — so the bar can show connected while every pull dies.
- fix: Add a private @Serializable response row to CalendarClient that maps the snake_case shape with @SerialName(account_email/display_name/selected_calendar_ids[=emptyList]/color_slot/last_sync_cursor/connected_at), then .toModel() to CalendarConnection — exactly like DbRowCodec.CalendarConnectionRow (DbRowCodec.kt:183-198) already does for the PostgREST path; reuse that row type. Do NOT decode the function response directly into the camelCase core model.

### [Google Calendar connect/pull] No push (Unstuck -> Google) at all: local task blocks are never written back to Google
- missing | sync/src/main/kotlin/tech/csalliance/unstuck/sync/WriteThrough.kt:25-29 (upsertCalBlock) and :63 (deleteCalBlock); CalendarClient.kt:52-68 (unused insert/patch/delete); core GoogleSyncMapping.kt:64 blockToIsoRange (only test caller)
- VERIFIED. Web pushes every locally-owned block on upsert and delete: use-tasks.ts:244-256 calls pushBlockUpsert(item) for item.kind !== 'external' && !== 'placeholder' and restamps externalEventId from the returned event id; use-tasks.ts:268 calls pushBlockDelete(removed) when a previously-pushed (removed.externalEventId) block is removed. google-sync.ts implements getPushContext() (:213), pushBlockUpsert (:250) and pushBlockDelete (:297). On Android, WriteThrough.upsertCalBlock (WriteThrough.kt:25-29) and deleteCalBlock (delegates to deleteLocalAndEnqueue) ONLY write Room + enqueue a Supabase outbox op — they never call CalendarClient.insert/patch/deleteEvent. grep across the whole repo (excluding /build and tests) shows CalendarClient.insertEvent/patchEvent/deleteEvent and core.logic.blockToIsoRange have ZERO production callers (only their own defs + GoogleSyncMappingTest.kt). So scheduling/moving/deleting a focus block on Android never reflects on the user's Google calendar and externalEventId is never minted/persisted. Whole capability absent.
- fix: Port google-sync.ts push: add getPushContext() (cache primary google connection + selectedCalendarIds[0] || 'primary') and pushBlockUpsert/pushBlockDelete to :sync, invoked from AppViewModel after scheduleTask/moveBlock/resizeBlock/unschedule (or inside WriteThrough.upsertCalBlock/deleteCalBlock) for blocks where kind != EXTERNAL && != PLACEHOLDER. On insert, restamp externalEventId via write.upsertCalBlock(block.copy(externalEventId=newId)) like use-tasks.ts:248-256. Skip placeholder/external; best-effort/silent on failure, matching web.

### [Google Calendar connect/pull] Connections store never refreshed after connect/disconnect — sync bar shows stale state
- bug | sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:81-88 (completeGoogleConnect) and :109-114 (disconnectCalendar)
- VERIFIED. CalendarSyncBar reads vm.connections (CalendarScreen.kt:87) = store.connections() = Room CALENDAR_CONNECTIONS table (AppViewModel.kt:67; LocalStore.kt:52). That table is populated ONLY by Hydrator.hydrate() (Hydrator.kt:34, runs on sign-in / WorkManager) — grep confirms CALENDAR_CONNECTIONS is referenced only in Hydrator.kt and DbRowCodec; SyncCoordinator never writes it. Web, after connect, calls refreshConnections() + startAutoSync() (sync-flow.tsx:138-139) so the accounts list updates immediately. Android completeGoogleConnect (SyncCoordinator.kt:81-88) only calls pullCalendar() and never refreshes connections, so the Room table stays empty and the bar keeps showing '＋ Connect Google Calendar' until the next full hydrate. Symmetrically disconnectCalendar (SyncCoordinator.kt:109-114) deletes local EXTERNAL blocks but never removes the CALENDAR_CONNECTIONS row, and that table is intentionally NOT realtime-subscribed (RealtimeMirror.kt:29), so the bar keeps showing 'Synced · <email>' + 'Disconnect' after disconnecting until the next hydrate. (Web onDisconnect updates the list immediately.)
- fix: After completeGoogleConnect succeeds, refresh the local connections table — call hydrator.hydrate() (which already decodes the PostgREST snake_case shape correctly) or, once finding #1 is fixed, calendar.listConnections() then store.replace(Tables.CALENDAR_CONNECTIONS, ...). In disconnectCalendar, after calendar.disconnect succeeds, also store.delete(Tables.CALENDAR_CONNECTIONS, connectionId) so the bar updates immediately (mirrors sync-flow.tsx onDisconnect).

### [Collections CRUD (new/rename] New collection sortOrder uses collections.size, can collide after a delete
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/NewCollectionSheet.kt:67
- Web computes the new sort order as max(sortOrder)+1 with an explicit comment that this 'survives deletes without colliding on sort_order' (use-collections.ts add, lines 155-165: `const maxSort = prev.reduce((m, c) => Math.max(m, c.sortOrder), -1)` then `sortOrder: maxSort + 1`). Android instead uses `sortOrder = collections.size` (NewCollectionSheet.kt line 67). After deleting any collection, collections.size no longer equals max(sortOrder)+1, so a new collection gets a sortOrder that duplicates an existing row. The overview grid is sorted purely by sortOrder (CollectionsScreen.kt line 50: `collections.sortedBy { it.sortOrder }`, mirroring collections-overview.tsx line 26), so two collections share an order key and the grid order becomes non-deterministic. Verified against both files.
- fix: Mirror web: `val maxSort = collections.maxOfOrNull { it.sortOrder } ?: -1` then `sortOrder = maxSort + 1`. Do not use collections.size.

### [Collections CRUD (new/rename] Inline item edit and title rename only commit on the ✓ tap — tapping away silently discards the edit
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionDetailScreen.kt:92
- Web commits an item-body edit on blur (collection-item-row.tsx onBlur={commit}, line 100) AND on Enter (line 102), and commits a collection rename on blur (collection-detail.tsx onBlur={commitRename}, line 143) AND on Enter (line 145). On Android the title-rename BasicTextField (CollectionDetailScreen.kt line 92) and the item-edit BasicTextField (line 172) are plain fields with NO keyboardOptions/keyboardActions and NO onFocusChanged — the only commit path is tapping the small green '✓' (lines 93 and 173). Note: the ADD-item field at line 116 DOES correctly have ImeAction.Done + onDone, so this gap is specific to the edit/rename fields. If the user finishes typing and taps elsewhere, the typed text is lost. Verified by reading both fields and confirming the add field's contrasting correct handling.
- fix: Add `keyboardOptions = KeyboardOptions(imeAction = ImeAction.Done)` + `keyboardActions = KeyboardActions(onDone = { commit })` to both the title field (line 92) and the item-edit field (line 172), and commit on focus loss via `Modifier.onFocusChanged { if (!it.isFocused && editing) commit() }`, matching the add field at line 116. Also restore the original value on cancel as web does (Escape).

### [Insights / Analytics (report] Stacked-bars segments and legend dots all render gray (areaColor gets a NAME, not a token)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:205
- weekdayAreaHours returns columns keyed by area NAMES from DEFAULT_AREAS = ["Work","Personal","Home","Health","Volunteering"] (Analytics.kt:30,32-49). InsightsScreen.kt:104 passes DEFAULT_AREAS into StackedBars, which calls c.areaColor(areas.getOrElse(i){""}) for the bar segments (line 205) and c.areaColor(a) for the legend swatches (line 211). But Theme.areaColor() (Theme.kt:38-47) only matches color TOKENS (indigo/primary/coral/rethink/test/violet/blue/green/'new feature'/bug/amber/red) — it has NO case for Work/Personal/Home/Health/Volunteering, so every one falls through to else -> ink4 (gray). Result: the entire 'When focus happens' chart is identical gray segments with a gray legend, where web colors each area distinctly via AREA_COLOR / NAME_TO_OKLCH (report.tsx:97 uses AREA_COLOR.Work/Personal/Home/Volunteering; area-dot.tsx:18-24 maps Work=indigo, Personal=coral, Volunteering=green, Home=amber, Health=blue).
- fix: Map the canonical area name to its color before calling areaColor. Either add a Work/Personal/Home/Health/Volunteering name->token branch in Theme.areaColor() mirroring NAME_TO_OKLCH (Work=indigo, Personal=coral, Volunteering=green, Home=amber, Health=blue), or resolve each area name through vm.lifeAreas to its token in InsightsScreen and pass the token. Apply to both the segment fill (line 205) and the legend swatch (line 211).

### [Insights / Analytics (report] Estimate calibration scatter chart (H2) is entirely absent from the Android Report
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:78
- The web Report renders an EstimateCalibration scatter panel (report.tsx:150-226 / line 414): an estimate-vs-actual dot plot with a dashed diagonal y=x reference line, green dots for hits (|actual-estimate|<=5) and amber for misses, axis labels 'actual' / 'estimate →', and a narrative line ('{hit}% of recent sessions landed within 5 min'). Android computes dots = calibrationDots(sessions, tasks) (InsightsScreen.kt:78) but only ever uses the hit-rate number in a StatCard (line 98). The scatter visualization — a core Report chart — is never drawn anywhere in InsightsScreen.kt.
- fix: Add a Compose calibration scatter composable for the Report branch: a Canvas/Box plotting each CalibrationDot at x=e/max, y=a/max (max = max(70, max(e,a))), a dashed diagonal reference, color green when abs(a-e)<=5 else amber, plus 'actual'/'estimate →' labels and the '{hit}% landed within 5 min of estimate' subtitle. Render it in the Report section alongside StackedBars and the interruption histogram.

### [Command palette + quick capt] Life areas are not searchable / no area-filter jump from the palette
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:50-61
- Web builds candidates from tasks, captures, lifeAreas AND routes. Each life area becomes a 'Filter tasks by area' item that on select navigates to /tasks?area=<name> (command-palette.tsx:85-93, onSelect: router.push(`/tasks?area=${encodeURIComponent(a.name)}`)). Android's palette assembles only taskResults + noteResults + actions (CommandPalette.kt:61) and never includes life areas, even though vm.lifeAreas exists (AppViewModel.kt:66) and Tasks already supports area filtering (MainScaffold.kt:106 TasksScreen onAreaPick={ activeArea = it }, and AreasMenu onPick={ area -> activeArea = area; tab = "tasks"; stack.clear() } at MainScaffold.kt:149). The whole 'area' item category and the jump-to-area-filter capability is absent.
- fix: Collect `val areas by vm.lifeAreas.collectAsStateWithLifecycle()`, build `areaResults = areas.filter { q.isEmpty() || it.name.lowercase().contains(q) }.map { a -> Result(a.name, "Filter tasks by area", "AREA") { onAreaJump(a.name) } }`, add `onAreaJump: (String) -> Unit` param wired in MainScaffold.kt:127 to `{ pop(); activeArea = it; tab = "tasks"; stack.clear() }` (mirroring AreasMenu onPick), and include areaResults in the merged list.

### [Command palette + quick capt] Selecting a capture/note result does nothing (dead row)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:59-60
- Web's capture item navigates on select: onSelect = () => router.push('/tasks') (command-palette.tsx:82), so a tapped capture takes you somewhere. On Android the note Result's run lambda is `{ onDismiss() }` (CommandPalette.kt:60) — tapping a matched note merely closes the palette with no navigation. The row is effectively dead and inconsistent with web. Note the fix detail below differs from web: web ignores any task linkage and always routes to /tasks; on Android captures surface in the 'lists'/Collections tab.
- fix: Make the note action navigate instead of no-op. Closest web parity is to route to the captures destination, i.e. `{ onTab("lists") }` (Collections/captures live there). Optionally, since Android's Capture has a `taskId` field (Models.kt:137) that web's type lacks, you could open the linked task when present (find the TaskItem and onOpenTask(it)) — that exceeds web but is a reasonable improvement; at minimum replace `{ onDismiss() }` with navigation.

### [Command palette + quick capt] Missing route shortcuts: Analytics and Start a focus session
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:50-55
- Web ROUTES exposes six jumps including 'Go to Analytics' (/analytics) and 'Start a focus session' (/focus) (command-palette.tsx:16-23). Android actions only offer Today, Tasks, Calendar, Lists, Settings (CommandPalette.kt:50-55) — no Analytics and no focus-session entry. Analytics exists as Route.Insights (MainScaffold.kt:52, reached via onInsights={ push(Route.Insights(false)) }) and focus exists via the focusTask state + FocusScreen (MainScaffold.kt:100,154-157). Neither is reachable from the palette; the CommandPalette signature (CommandPalette.kt:43) takes no onInsights/onStartFocus callback.
- fix: Add `Result("Go to Analytics", null, "ACTION") { onInsights() }` wired in MainScaffold to `{ pop(); push(Route.Insights(false)) }`. For focus, note web's /focus is task-agnostic but Android's FocusScreen requires a task (focusTask). Add `Result("Start a focus session", null, "ACTION") { onStartFocus() }` and in MainScaffold pick a sensible task (e.g. the pick-start-next / next task) before setting focusTask; if no task is available, fall back to navigating to Today.

### [Settings & preferences (ever] Sound section toggles drive nothing — no chime / bell / completion sound exists
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:64-160
- Web plays three synthesized sounds, each gated INSIDE the play fn by its bool pref (use-sound.ts:76 playChime reads PREF_SOUND_START_CHIME default true; :83 playBell reads PREF_SOUND_OVERRUN_BELL default true; :90 playCompletion reads PREF_SOUND_COMPLETION default false). The focus hook calls them at start (use-focus-timer.ts:223 playChime), on transition into overrun (use-focus-timer.ts:172 playBell, fired once via lastStateRef), and on done (use-focus-timer.ts:259 playCompletion). On Android, grep confirms soundStartChime/soundOverrunBell/soundCompletion appear ONLY in SettingsStore.kt (data class + load/save) and SettingsScreen.kt (the toggles) — zero reads in FocusScreen or AppViewModel.startFocus/finishFocus, and no ToneGenerator/SoundPool/AudioTrack anywhere (the only MediaPlayer is in AmbientAudio.kt, itself unused). All three Sound toggles are pure dead state. Defaults already match web (true/true/false), so only playback is missing.
- fix: Add a small tone surface (ToneGenerator or short AudioTrack tones mirroring lib/use-sound.ts). Play the start chime in AppViewModel.startFocus gated on settings.soundStartChime; play the overrun bell when state transitions RUNNING->OVERRUN in FocusScreen (gated on soundOverrunBell, fire-once via a remembered prev-state ref like the web's lastStateRef); play the completion sound in finishFocus on markDone gated on soundCompletion.

### [Settings & preferences (ever] Overrun grace hardcoded to 1.0 — the 'Soft overrun' setting (Off/5/10) is ignored at runtime
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:84
- Web reads PREF_FOCUS_OVERRUN and converts to seconds (use-focus-timer.ts:69-76: 'Never'->Infinity, '5 min'->300, '10 min'->600) then passes it to deriveState (use-focus-timer.ts:164). Android FocusScreen.kt:84 calls FocusTimer.deriveState(l, nowMs, 1.0) with a literal 1.0, so the session always flips to OVERRUN 1s past estimate regardless of setting. The SegRow (SettingsScreen.kt:122) writes focusOverrunMin as an Int (0='Off', 5, 10), default 5 (SettingsStore.kt:25). FocusTimer has overrunGraceSeconds(pref:String?) (FocusTimer.kt:52-60) but it expects web-style strings ('Never'/'5 min') and is only referenced by its own unit test — never by the UI, and it can't take the Int directly anyway. So 'Off' does nothing (1s overrun still fires) and 5/10 do nothing.
- fix: Compute grace from the Int setting: val grace = if (settings.focusOverrunMin == 0) Double.POSITIVE_INFINITY else settings.focusOverrunMin * 60.0; pass it into deriveState. (deriveState already treats POSITIVE_INFINITY as never-escalate, FocusTimer.kt:44, matching web 'Never'.) Either drop the unused string-based overrunGraceSeconds helper or repurpose it.

### [Settings & preferences (ever] Ambient focus loop (off/brown/pink) never plays — AmbientAudio is dead code, ambient pref unwired
- missing | app/src/main/kotlin/tech/csalliance/unstuck/surface/AmbientAudio.kt:11-33
- Web drives ambient noise from AmbientPlayer mounted inside focus mode (focus-mode.tsx:218), which calls setAmbient() to start/stop generated brown/pink noise (use-sound.ts:139, with distinct buildNoiseBuffer('brown'|'pink') at :106-110); pref key PREF_SOUND_AMBIENT ('off'|'brown'|'pink'). Android persists SettingsState.ambient (SettingsStore.kt:32,66,87) and exposes the off/brown/pink SegRow (SettingsScreen.kt:133), but grep confirms 'ambient' appears only in SettingsStore + the SegRow — never read by FocusScreen. The only audio object, AmbientAudio (surface/AmbientAudio.kt), is referenced only by its own definition (grep finds no start()/stop() call sites). So selecting brown/pink does nothing. Additionally AmbientAudio plays a single R.raw.ambient_focus loop with no brown-vs-pink distinction.
- fix: Drive AmbientAudio from FocusScreen lifecycle gated on settings.ambient: LaunchedEffect(settings.ambient){ if(ambient != "off") AmbientAudio.start(context) else AmbientAudio.stop() } plus DisposableEffect onDispose { AmbientAudio.stop() }. Ideally provide distinct brown/pink output (separate raw assets or synthesized noise) to match web; at minimum honor off vs on.

### [Settings & preferences (ever] Accessibility section: reduce-motion / high-contrast / keyboard-hints toggles do nothing
- missing | app/src/main/kotlin/tech/csalliance/unstuck/SettingsStore.kt:55-57
- Web applies all four a11y prefs (use-accessibility-classes) so CSS reads them; reduce-motion kills animations, high-contrast strengthens borders/text, keyboard-hints surfaces shortcuts, larger-type bumps text (settings-panel.tsx:64-67 wires all four). Android honors only largerType (folded into fontScale, SettingsStore.kt:43 / Theme.kt:144). Grep confirms reduceMotion/highContrast/keyboardHints appear ONLY in SettingsStore.kt and the SettingsScreen toggles (lines 136/138/139) — zero behavioral consumers anywhere. UnstuckTheme.withAccent has no high-contrast variant, and the focus Orbit/breathing + progress-arc animations (FocusScreen.kt) never check reduceMotion. Reduce-motion is the highest-impact gap for the ADHD audience.
- fix: Thread these into theme/behavior: gate animations (Orbit, breathing, progress arc, fades) on !settings.reduceMotion; add a high-contrast color variant (stronger line/ink) applied in UnstuckTheme when settings.highContrast. If keyboardHints has no Android analog (no keyboard shortcuts on touch), drop the toggle from the UI rather than ship a no-op control.

### [Account / Auth (sign in/up/m] No "check your email" confirmation after sign-up / magic link / password reset
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:59-68, 94, 118, 121
- Confirmed. Web routes every email-dispatch flow to a confirmation surface: sign-up → /auth/verify with auto-poll for the session + resend (sign-up/page.tsx:77-79, verify/page.tsx:34-67), magic link → /auth/magic-link-sent (sign-in/page.tsx:85-89, sign-up/page.tsx:92-96), password reset → in-card 'Reset link sent' green success state (forgot/page.tsx:35,58-71). Android AuthScreen.run() does literally nothing on AuthOutcome.Ok (AuthScreen.kt:63 `is AuthOutcome.Ok -> {}`) — busy flips back to false and the form just sits there. Tapping 'Email me a magic link instead' (AuthScreen.kt:118), 'Forgot your password?' (AuthScreen.kt:121), or 'Create account' (AuthScreen.kt:97-98) gives ZERO feedback. The only state surfaced is `message` and it's rendered in c.coralDeep (error color, AuthScreen.kt:94), and it's only ever set on Error. There is no success/green path at all.
- fix: Add a success-message state. In run() accept a success label and set it on Ok (magic link: 'Magic link sent to <email>. Open it on this device.'; reset: 'Reset link sent — check your inbox.'; sign-up: 'One more step — confirm via the email we sent.'). Render it in c.green next to the existing error Text. Ideally port a verify/magic-link-sent confirmation surface mirroring the web.

### [Account / Auth (sign in/up/m] Sign-up "email already exists" anti-enumeration routing not wired
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/AuthService.kt:35-43
- Confirmed. Web signUpWithPassword (auth-helpers.ts:176-187) inspects identities length / email_confirmed_at / last_sign_in_at without a session and returns { alreadyExists: true }; sign-up/page.tsx:69-76 routes to /auth/sign-in?email=…&existing=1, which renders a friendly 'account already exists, sign in / forgot password' banner (sign-in/page.tsx:37,118-133). Android ported the pure detector (AuthErrors.kt:62-72 detectSignupAlreadyExists) and unit-tests it (AuthErrorsTest.kt exists in core/src/test) but it is NEVER called: AuthService.signUp (AuthService.kt:35-43) discards the signUpWith result via fold and always returns AuthOutcome.Ok. AuthOutcome (AuthService.kt:20-23) has no alreadyExists carrier. So re-signing-up with an existing confirmed email yields a fake 'success' with no email and no redirect — the user is stuck.
- fix: Capture the UserInfo returned by signUpWith(Email), feed identities?.size, emailConfirmedAt, lastSignInAt and (currentSession != null) into detectSignupAlreadyExists, and return a richer outcome (add an alreadyExists flag to AuthOutcome.Ok or a new variant) so AuthScreen can flip to sign-in mode with the 'account already exists' banner.

### [Account / Auth (sign in/up/m] No resend-confirmation flow for unverified-email sign-in
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/AuthService.kt:30-47
- Confirmed. Web exposes resendSignupConfirmation (auth-helpers.ts:113-130) calling sb.auth.resend({type:'signup'}), and surfaces a 'Resend confirmation email' button whenever signInWithPassword fails with needsConfirmation (sign-in/page.tsx:30,67-69,100-107,181-197), again on /auth/verify (verify/page.tsx:60-67,107-116). The needsConfirmation flag is set in fail() (auth-helpers.ts:79-80) on the email_not_confirmed code. Android has no resend at all: AuthService has no resend method, AuthOutcome (AuthService.kt:20-23) carries only Ok/Error so the email_not_confirmed case collapses to a plain Error string (humanizeAuthError handles the copy at AuthErrors.kt:29-31 but there's no structured needsConfirmation signal), and AuthScreen shows no resend affordance. A user who signs in before confirming sees only the humanized error with no way to get a fresh link.
- fix: Add AuthService.resendSignupConfirmation(email) calling client.auth.resend(type=signup, email). Surface a needsConfirmation signal on the sign-in outcome (detect email_not_confirmed) and have AuthScreen render a 'Resend confirmation email' button when that error appears.

### [Account / Auth (sign in/up/m] Account screen identity fields are non-reactive getters — stale after display-name / password change
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:360-361,379
- Confirmed. vm.currentName / vm.currentEmail (AppViewModel.kt:360-361) and vm.hasPassword (AppViewModel.kt:379) are plain `get()` getters delegating to AuthService getters that read client.auth.currentUserOrNull() each call (AuthService.kt:77,86,89-95). They are not Compose State / StateFlow. AccountContent reads them directly — `SettingRow("Display name", vm.currentName ?: "Set a name")` (SettingsScreen.kt:179) and `SettingRow(if (vm.hasPassword) "Change password" else "Add a password", …)` (SettingsScreen.kt:181) — with no collectAsState. After updateDisplayName the metadata changes but the row keeps showing the OLD name until an unrelated recompose, while the toast says 'Name updated.' (SettingsScreen.kt:188). Same staleness on the Change/Add-password label after adding a first password. Web PasswordRow explicitly re-reads getSignInMethods() to flip Add→Change without reload (password-row.tsx:93-96); Android has no equivalent refresh.
- fix: Back identity off client.auth.sessionStatus (emits USER_UPDATED on updateUser) as StateFlow for name/email/hasPassword, or bump a mutableStateOf trigger after updateDisplayName/changePassword succeeds, then collectAsState in AccountContent.

### [Onboarding (struggles -> pre] Struggles step is entirely absent; adhd_struggles data flow is dead code
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:62,122 (and AppViewModel.kt:330-342)
- Web flow.tsx defines STRUGGLES (5 options: Starting/Sustaining/Switching/Stopping/Recovering) at flow.tsx:20-26, collects them in step 2 ('What gets you stuck?', flow.tsx:118-186), and on finish persists to localStorage ADHD_STRUGGLES + upserts user_preferences.adhd_struggles for signed-in users (flow.tsx:79-93). hydrate.ts round-trips it (selects at :239, reads at :269, writes localStorage at :299-300). Android OnboardingScreen has NO struggles step — its 4 steps are welcome (0), area-picker (1), first-task (2), treatment (3). Both finish() (OnboardingScreen.kt:62) and Skip (OnboardingScreen.kt:122) call vm.completeOnboarding(emptyList()) with a hardcoded empty list, so the guard `if (uid != null && struggles.isNotEmpty())` in completeOnboarding (AppViewModel.kt:338) is NEVER true. PreferencesClient.setAdhdStruggles (Clients.kt:59-61) and the struggles parameter are unreachable dead code. Struggles are never captured or persisted on Android.
- fix: Add a struggles multi-select step matching web's STRUGGLES array (same 5 keys), store selections in a mutableStateListOf, and pass that list to vm.completeOnboarding(selectedStruggles) from finish() instead of emptyList() so user_preferences.adhd_struggles round-trips identically across platforms.

### [Onboarding (struggles -> pre] First-physical-action step missing; task seeded without firstPhysicalAction
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:61
- Web step 4 ('The smallest physical move', flow.tsx:224-280) captures firstAction and writes it to the seed task as firstPhysicalAction (flow.tsx:71). Android onboarding has no such step; finish() calls vm.addTask(name = firstTask.trim(), estimateMin = 15, lifeArea = 'Personal') (OnboardingScreen.kt:61) and never passes firstPhysicalAction, even though AppViewModel.addTask accepts it (AppViewModel.kt:100,108).
- fix: Add a 'first physical move' multiline text step and pass its value as firstPhysicalAction to vm.addTask, mirroring web's coral-highlighted prompt and 'why this matters' note (flow.tsx:228,262-276).

### [Onboarding (struggles -> pre] Onboarding never starts a focus session or routes to focus
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:59-64
- Web finish() ends onboarding by starting a 15-minute focus timer on the new task and navigating to /focus (flow.tsx:94-95), with a step-5 'Try it now' SessionPreview screen showing READY/15 MIN/15:00 (flow.tsx:281-285,350-433). Android finish() only seeds areas + adds task + completeOnboarding(emptyList()) + onDone() (OnboardingScreen.kt:59-64); onDone just sets `onboarding = false` in MainScaffold (MainScaffold.kt:78), dropping the user to the dashboard. No vm.startFocus call (startFocus exists at AppViewModel.kt:202) and no session-preview step.
- fix: Capture the TaskItem returned by addTask (it already returns one, AppViewModel.kt:103,112), call vm.startFocus(task), and have onDone drive MainScaffold into the focus overlay by setting focusTask = task (focusTask drives the FocusScreen overlay at MainScaffold.kt:69,154-156) rather than just dismissing. Add a session-preview step matching web step 5.

### [Onboarding (struggles -> pre] Treatment selection in step 4 is collected but never persisted
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:59-64,100-119
- Android adds a treatment picker step (Ambient/Cockpit/Monk) bound to local state `treatment` (OnboardingScreen.kt:57,100-119). But finish() (OnboardingScreen.kt:59-64) NEVER reads `treatment` — it is never written to settings. AppViewModel.setTreatment(t) persists via updateSettings { it.copy(treatment = t) } (AppViewModel.kt:211-214) but is not called. The user's pick of Cockpit/Monk is silently discarded and the app defaults to AMBIENT (SettingsStore.kt:33). The selected treatment is dead UI. Note: web onboarding has no treatment step at all.
- fix: In finish(), call vm.setTreatment(treatment) before completeOnboarding (startFocus already applies _settings.value.treatment to a fresh session at AppViewModel.kt:206, so persisting it first carries through). If matching web exactly, remove the treatment step entirely; if keeping it, it must persist.

### [Onboarding (struggles -> pre] Double life-area seeding: picked areas plus 4 hardcoded defaults both get written
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:59-64 (with AppViewModel.kt:330-336)
- finish() seeds one LifeArea per picked tag via vm.upsertLifeArea (OnboardingScreen.kt:60), each dispatched async through launchWrite. It then calls vm.completeOnboarding(emptyList()), whose body seeds Work/Personal/Home/Health when lifeAreas.value.isEmpty() (AppViewModel.kt:331-336). lifeAreas is a WhileSubscribed(5_000) StateFlow (AppViewModel.kt:57,66) and OnboardingScreen never subscribes to it (no collectAsState on lifeAreas in the file), so lifeAreas.value is the emptyList() default at the moment completeOnboarding runs — the guard passes and 4 MORE default areas are seeded on top of the picks. A user who picks Work/Personal/Home ends up with those PLUS Work/Personal/Home/Health (duplicate areas). The two writes also race through launchWrite with no ordering guarantee.
- fix: Seed areas in only one place. Either pass the picked areas into completeOnboarding and seed there, or drop the area-picker step (web onboarding does NOT pick areas — they come from the canonical/hydrate path) and let completeOnboarding's default seed be the single source. Ensure only one seeding path runs.

### [Recurrence (editor incl. unt] RecurrenceEditor has no 'until' (end-date) UI — users cannot set or change a recurrence end date
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/RecurrenceEditor.kt:17-49
- Web exposes a full Until end-date picker in BOTH editors. Create modal task-create-modal.tsx: state recurrenceUntil (line 116), '+ Set end date' button defaulting effectiveDate+28d / 'Open-ended' clear (lines 660-707), fed into buildRecurrence (lines 203-208). Detail-pane RepeatEditor task-detail-pane.tsx: until state seeded from current?.until (line 705), <input type=date> bound to until (lines 808-816), fed into save() (lines 722-725). The Android RecurrenceEditor deliberately omits this — its own header comment (lines 18-19) says '`until` is intentionally omitted from the editor (open-ended series)'. Verified: the only `until` occurrences in app/.../ui are that comment plus unrelated uses (for-loop ranges, prose); there is NO editable until control anywhere. :core fully supports it (materializeOccurrences stops at recurrence.until, Recurrence.kt:43-47; recurrenceLabel renders it, lines 112-117), so the gap is purely UI. An Android user can never bound a series.
- fix: Add an 'Until' row shown when mode != NONE: a '+ Set end date' button defaulting to anchorDate+28d (matching web's 28-day suggestion) opening a DatePicker, plus an 'Open-ended' clear. Initialise the until state from value.until and thread the chosen YYYY-MM-DD into every emission: Recurrence.Daily(until), Recurrence.Weekly(days, until), Recurrence.Monthly(until).

### [Recurrence (editor incl. unt] Editing a recurrence in the detail sheet silently drops an existing 'until' end date
- regression | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/RecurrenceEditor.kt:33-43
- RecurrenceEditor never reads value.until, so every onChange emits a fresh until=null Recurrence: Recurrence.Daily() (line 34), Recurrence.Weekly(...) (line 35), Recurrence.Monthly() (line 36), and the weekly chip toggle (line 43). In TaskDetailSheet this is wired straight to vm.setRecurrence(task, it) (TaskDetailSheet.kt:150). So a series created on web/iOS with until (e.g. {weekly, daysOfWeek:[1,3,5], until:'2026-08-01'}) loses its bound the moment an Android user touches ANY chip: setRecurrence (AppViewModel.kt:135-150) calls regenerateForTask with the now-null until and materialises the full 56-day horizon, then persists the task with recurrence.until=null on next sync. Web preserves until across edits (RepeatEditor seeds until from current?.until, task-detail-pane.tsx:705).
- fix: Carry value.until through every emission until the new until UI lands: val until = value?.until; emit Recurrence.Daily(until), Recurrence.Weekly(next, until), Recurrence.Monthly(until). Then add the editable until control (prior finding) so it can also be changed/cleared.

### [Recurrence (editor incl. unt] TaskDetailSheet 'Schedule' on an already-scheduled task creates a duplicate cal_block (no reschedule guard)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:154-175
- Web's persistOrMove (schedule-task-modal.tsx:56-79) explicitly guards: if the task already has a block, update it in place (+ bumpMoveCount only if the slot changed); otherwise create one — 'This prevents the click-Schedule-N-times → N-blocks bug.' Android's scheduleTask (AppViewModel.kt:154-175) unconditionally inserts a fresh CalBlock(id=newUuid(), ...) (lines 169-171) and never looks up/updates an existing block. The detail-sheet 'Schedule' button (TaskDetailSheet.kt:110-113) computes the task's blocks (myBlocks, line 73) but does NOT use them to guard — it calls vm.scheduleTask with a free slot regardless, so pressing Schedule twice (or scheduling an already-scheduled task) yields N blocks. NOTE: the original finding also blamed DayGrid drag-drop; that part is WRONG — DayGrid only drags from the unscheduled tray, which excludes already-scheduled tasks (DayGrid.kt:98 `it.id !in scheduledIds`), and existing blocks are moved via CalBlockEditSheet/moveBlock (lines 163,227,253), so drag cannot duplicate. The bug is real only for the detail-sheet Schedule button (and any future caller that schedules an already-blocked task).
- fix: Mirror persistOrMove in the non-recurring branch: existing = blocks.value.firstOrNull { it.taskId == task.id && isTaskBlock(it) }; if present, upsert that block with the new date/startTime (and only bumpMoveCount when date/startTime changed); otherwise create one new block with no bump.

### [Recurrence (editor incl. unt] scheduleTask bumps move_count on first/initial scheduling and on every call, inflating the slip detector
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:174
- Web bumps move_count ONLY when a block is removed (use-tasks.ts remove() line 266) or moved to a different slot (persistOrMove only when existing.date/startTime changed, schedule-task-modal.tsx:65-68). Task creation never bumps — confirmed: no bumpMoveCount/moveCount reference exists in task-create-modal.tsx. Android's scheduleTask unconditionally calls write?.upsertTask(bumpMoveCount(task, isoNow())) on EVERY invocation (AppViewModel.kt:174; bumpMoveCount = moveCount+1, TaskMutations.kt:25-26). NewTaskSheet routes creation of a dated task through scheduleTask (NewTaskSheet.kt:192-193), so every brand-new scheduled task is born with moveCount=1, and every detail-sheet 'Schedule' over-increments. Analytics/slip detector reading move_count are systematically inflated vs web/iOS.
- fix: Only bumpMoveCount on a genuine reschedule of an existing block whose slot changed (folds into the duplicate-block fix). For initial scheduling and creation, write the unmodified task so moveCount stays 0, matching web.

### [Life areas + area filter (CR] Area RENAME is completely absent on Android (no UI, no cascade)
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:297-299; app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:323-324
- Web LifeAreaPanel gives every area a dot-menu 'Rename' (life-area-panel.tsx:282; commitRename 175-179) calling update(a.id,{name}); useLifeAreas.update (use-life-areas.ts:160-174) then runs cascadeRenameTaskArea(oldName,newName) remapping EVERY task.lifeArea string. Android AreasContent (SettingsScreen.kt:297-299) DropdownMenu has ONLY 'Delete area' — no rename affordance. AppViewModel exposes only upsertLifeArea/deleteLifeArea (AppViewModel.kt:323-324); there is no renameLifeArea and no area cascade (only renameTag exists with a cascade at 314-320). WriteThrough has no area cascade either. Users cannot rename an area at all.
- fix: Add a Rename DropdownMenuItem in AreasContent that flips the row into inline edit (mirror the TagsContent editing pattern at SettingsScreen.kt:346-356). Add AppViewModel.renameLifeArea(area,newName): upsert area.copy(name=newName) AND cascade tasks.value.filter{it.lifeArea==area.name}.forEach{ write?.upsertTask(it.copy(lifeArea=newName, updatedAt=isoNow())) } — exactly mirroring renameTag (AppViewModel.kt:314-320) and web cascadeRenameTaskArea.

### [Life areas + area filter (CR] Deleting an area does NOT clear lifeArea on its tasks (dangling label, contradicts dialog copy)
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:324; dialog copy at app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:305
- Web remove() (use-life-areas.ts:176-184) calls cascadeRenameTaskArea(removed.name, undefined) so every task tagged with the deleted area gets lifeArea=null and falls into the Unassigned bucket. Android deleteLifeArea (AppViewModel.kt:324) just calls write?.deleteLifeArea(id) = deleteLocalAndEnqueue (WriteThrough.kt:68) with NO task cascade. Consequences confirmed in code: (a) dialog copy 'Tasks keep their data — they just lose this area label' (SettingsScreen.kt:305) is false — they keep the stale name; (b) those tasks do NOT surface under Unassigned: AreasMenu counts only lifeArea==null (AreasMenu.kt:45) and matchesArea(UNASSIGNED) checks taskArea.isNullOrEmpty() (VisibleTasks.kt:25) — a non-null dangling name matches neither the deleted area nor Unassigned; (c) their dot goes gray because areaColorFor returns ink4 when the name isn't in the areas list (Common.kt:138).
- fix: Make deleteLifeArea cascade: fun deleteLifeArea(id){ val a=lifeAreas.value.firstOrNull{it.id==id}; if(a!=null) tasks.value.filter{it.lifeArea==a.name}.forEach{ write?.upsertTask(it.copy(lifeArea=null, updatedAt=isoNow())) }; write?.deleteLifeArea(id) } — matching web cascadeRenameTaskArea(removed.name, undefined) so the 'lose this area label' copy becomes true and orphans land in Unassigned.

### [Life areas + area filter (CR] Area RECOLOR is absent on Android
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:288-300
- Web area dot-menu has a COLOR row with the full token palette; clicking a swatch calls onRecolor -> update(a.id,{color}) (life-area-panel.tsx:283-310). Android AreasContent shows a static ColorChip (SettingsScreen.kt:289) and a menu offering only Delete (SettingsScreen.kt:298) — no recolor. The adjacent TagsContent DOES have a working recolor palette popup (SettingsScreen.kt:339-345 via vm.recolorTag), confirming the gap is areas-only. There is no vm.recolorLifeArea. Users can never change an area's color after creation.
- fix: Mirror the tag palette popup (SettingsScreen.kt:338-345): wrap the area ColorChip in a clickable opening a DropdownMenu of palette swatches, each calling a new vm.recolorLifeArea(area, token) = launchWrite { write?.upsertLifeArea(area.copy(color=token)) }.

### [App shell / nav / overlays /] External Google cal_blocks (g_ ids) get enqueued to the outbox → permanently-failing Postgres upserts that stall sync
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/WriteThrough.kt:29
- VERIFIED. Web bridge.ts dbUpsert() (lib/sync/bridge.ts:64-67) gates EVERY write on UUID_RE.test(id) and skips non-UUID ids (mock/external rows) so they never reach Supabase. On Android, SyncCoordinator.pullCalendar() (SyncCoordinator.kt:101) does blocks.forEach { write.upsertCalBlock(it) }, and WriteThrough.upsertCalBlock (WriteThrough.kt:29-32) ALWAYS enqueues an outbox op with NO uuid/external guard — the only guard present is on dependsOn (taskId), not on b.id. externalEventToBlock (core/GoogleSyncMapping.kt:51) sets id = "g_${ev.id}", which is not a UUID. When OutboxFlusher.apply → SyncGateway.upsert("cal_blocks", row) runs (SyncGateway.kt:19-21), Postgres rejects the uuid column (22P02); the op is never dequeued, so it stays in the outbox forever, pendingCount keeps climbing, and every flush wastes a doomed round-trip on it. The reconciliation deletes for g_ ids (write.deleteCalBlock, SyncCoordinator.kt:105) likewise enqueue pointless, permanently-failing delete ops. The :core helpers isUuid (Uuid.kt:11) and isExternalBlock (CalBlockKind.kt:19) exist but are NOT applied in the upsertCalBlock/deleteCalBlock path. Minor nuance vs the original wording: g_ ops don't poison valid ops within a pass (OutboxFlusher.kt:26-32 only breaks when NO op progressed, so valid ops still drain first), but the g_ ops never clear and re-fail on every subsequent flush. Hits any user who connects Google Calendar, on every pull.
- fix: Make external/non-UUID blocks local-only: in WriteThrough.upsertCalBlock skip enqueue when isExternalBlock(b) || !isUuid(b.id) (still call store.upsert so the block renders); apply the same guard to deleteCalBlock for g_ ids. Or add a uuid guard inside SyncGateway.upsert + the enqueue path, mirroring web's UUID_RE gate in bridge.ts:64.

## MED (58)

### [Task CRUD + detail (create/e] New tasks created on Android have no priority (LOW); web sets 'medium' — diverges Start-Next/Up-Next ranking
- regression | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/NewTaskSheet.kt:187
- Web task-create-modal.tsx:239 sets priority: 'medium' on every created task, and capture-actions.promoteCapture sets priority: 'medium' (capture-actions.ts:28). Android AppViewModel.addTask defaults priority = null (AppViewModel.kt:95) and NewTaskSheet's vm.addTask call (NewTaskSheet.kt:187-191) never passes one, so Android-created tasks stay null -> priorityRank(null)=LOW=1 (PickStartNext.kt:14). AppViewModel.promoteCapture (AppViewModel.kt:261-264) also omits priority. The web ranker maps a missing priority to 'low' (pick-start-next.ts) but web-created tasks are explicitly 'medium' = rank 2, so identical task sets rank differently: Android-created tasks sort below web-created 'medium' tasks in Start Next, Up Next, the /tasks NEXT badge, and focus UP NEXT.
- fix: Pass priority = Priority.MEDIUM in NewTaskSheet's vm.addTask call and in AppViewModel.promoteCapture so cross-platform ranking is identical to web.

### [Task CRUD + detail (create/e] Recurrence 'until' (end date) cannot be set on Android
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/RecurrenceEditor.kt:21
- Web both create-modal (task-create-modal.tsx:660-715) and detail-pane RepeatEditor (task-detail-pane.tsx:808-842) let the user set an inclusive 'Until' end date for daily/weekly/monthly recurrences; the model carries recurrence.until and materializeOccurrences/recurrenceLabel honour it. Android RecurrenceEditor.kt explicitly omits until (comment line 17-19, 'open-ended series') and always emits Recurrence.Daily()/Weekly(...)/Monthly() with until=null (lines 34-36, 43). The :core DOES support until (Recurrence.kt:35-52 materialize stops at until; recurrenceLabel:113-118 formats it), so a web-set until survives in data — but it is silently dropped to open-ended the moment the Android editor re-emits the recurrence (e.g. toggling a weekday at RecurrenceEditor.kt:43 passes only the day list, no until).
- fix: Add an 'Until' date affordance (DatePicker) to RecurrenceEditor mirroring web; thread the current value's until through when re-emitting (e.g. Recurrence.Weekly(next, until = (value as? Recurrence.Weekly)?.until)) so editing days doesn't wipe a previously-set end date.

### [Task CRUD + detail (create/e] Detail-pane Schedule has no slot list / custom date+time / conflict warning (web is a full modal)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:110
- Web TaskDetailPane opens ScheduleTaskModal: up to 3 slots (9 via 'Show more options', schedule-task-modal.tsx:42), a custom date+time picker for any future date ('months ahead is fine', line 222), a live conflict pill (customConflicts:48-52), and a no-slots message. Android TaskDetailSheet.kt:110-113 reduces this to a single button that calls findFreeSlots(..., limit = 1).firstOrNull() and books it; if no slot is found in the next 4 days it does nothing with no feedback. The user cannot choose among slots, cannot pick an explicit future date/time, and gets no conflict feedback. The :core already has findFreeSlots (9-slot capable), findFreeSlotsForDate, and findConflicts available.
- fix: Present a schedule sheet equivalent to ScheduleTaskModal: list multiple free slots, a date+time picker for arbitrary future dates, and a findConflicts amber warning; reschedule-in-place when a block already exists (see the duplicate-block finding).

### [Task CRUD + detail (create/e] Captures in detail pane are not sorted newest-first
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:72
- Web TaskDetailPane sorts task captures by .at descending (task-detail-pane.tsx:66-68: allCaptures.filter(...).sort((a,b) => b.at.localeCompare(a.at))) so the most recent thought is on top. Android TaskDetailSheet.kt:72 uses captures.filter { it.taskId == task.id } with no sort, then renders them in that order at line 163 (taskCaptures.forEach). Order is arbitrary Room/insertion order and will not match web.
- fix: Sort: captures.filter { it.taskId == task.id }.sortedByDescending { it.at }.

### [Tags (vocabulary, filter, pi] Picker chips ignore the tag's color — always render primarySoft
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/components/TagPicker.kt:50,73-81; app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TasksScreen.kt:126
- Verified. TagPicker.kt:50 renders selected chips with `background = c.primarySoft` and a fixed primaryDeep text; the dropdown rows (TagPicker.kt:73-81) render no color dot at all. TagRow carries a color (set via recolorTag, SettingsScreen.kt:342). Web tints every chip with the stored color token (tag-picker.tsx:16-30 chipColorStyles + :100-104 colorByName) and shows a colored dot in the dropdown (tag-picker.tsx:225,247-253). The TasksScreen row chips have the same problem (TasksScreen.kt:126 `background = c.primarySoft`).
- fix: Look up each tag's color from vm.tags by name (case-insensitive) and tint via c.areaColor(token) like web's chipColorStyles (bg ≈ 18% mix, fg ≈ 75%). Add a color dot to the dropdown rows mirroring tag-picker.tsx:247-253.

### [Tags (vocabulary, filter, pi] Picker chip toggle/remove is case-sensitive, web is case-insensitive
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/components/TagPicker.kt:72,75
- Verified. TagPicker.kt:72 `val on = tag.name in selected`, :75 `selected - tag.name`, and chip removal at :50 `onChange(selected - name)` are all strict-equality. Web toggle() compares via a lowercased Set (tag-picker.tsx:77-83). If a task's stored tag casing differs from the vocab casing, the dropdown shows it un-checked and toggling appends a second casing variant instead of removing it. Note: the Create row at TagPicker.kt:84 already guards case-insensitively (`vocab.none { it.name.equals(q, ignoreCase = true) }`), so the divergence is specifically in the toggle/remove paths.
- fix: Compare case-insensitively: `val on = selected.any { it.equals(tag.name, ignoreCase = true) }`, remove with `selected.filterNot { it.equals(tag.name, true) }`; apply the same to the chip removal at line 50.

### [Tags (vocabulary, filter, pi] Settings usage count counts done tasks; web counts only OPEN tasks
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:332,352
- Verified. SettingsScreen.kt:332 `val uses = tasks.count { it.tags?.contains(tag.name) == true }` counts ALL tasks (done included) and is case-sensitive; :352 renders the bare number `Text("$uses", ...)` with no unit. Web usageCount filters `!t.done && (t.tags ?? []).includes(tagName)` (tag-panel.tsx:17-19) and labels it `{usage} open` (:219-221). The Areas panel in the same Android file already does it right (`open` with `!it.done`, SettingsScreen.kt:285,294).
- fix: Filter out done (and match case-insensitively): `tasks.count { !it.done && it.tags?.any { n -> n.equals(tag.name, true) } == true }` and render `"$uses open"`.

### [Tags (vocabulary, filter, pi] Settings 'Add tag' has no inline duplicate feedback and clears the draft on dup
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:366-372
- Verified. SettingsScreen.kt:366-372: the duplicate guard `if (nm.isNotBlank() && tags.none { it.name.equals(nm, ignoreCase = true) })` wraps only the upsert, and `draft = ""` runs unconditionally right after — so tapping Add on an existing name silently wipes the typed text with zero feedback. There is also no 'exists' hint while typing. Web AddTagForm shows an 'exists' badge live (tag-panel.tsx:346,394-396) and disables Add on a duplicate (:446), only committing when `trimmed && !isDuplicate`.
- fix: Compute `isDup = tags.any { it.name.equals(draft.trim(), true) }`; show an 'exists' hint and disable / early-return Add when dup (don't clear draft on the dup path); only clear draft after a successful add.

### [Today / Start-Next / Up-Next] Live-session card shows wrong elapsed for a resumed (prior-progress) session
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:208
- CONFIRMED. LiveSessionCard uses `FocusTimer.elapsedSec(live, now)` for both the ring progress (TodayScreen.kt:208-210) and the displayed MM:SS (lines 225, 229). `elapsedSec` excludes `priorAccumulatedSec` (FocusTimer.kt: `elapsedSec` returns only THIS session's seconds; `displayedElapsedSec = elapsedSec + priorAccumulatedSec`). Web LiveTaskRow uses `displayedElapsedSec` for ring label and progress (task-row.tsx:78-80,90), and Android's own FocusScreen.kt:80 correctly uses `FocusTimer.displayedElapsedSec(l, nowMs)`. The resume-aware path can produce `priorAccumulatedSec > 0` (FocusTimer.start seeds it; finishFocus persists totalFocused), so the Today card under-reports time and the ring resets toward 0 instead of continuing from the accumulated total.
- fix: Use `FocusTimer.displayedElapsedSec(live, now)` for the ring progress and the MM:SS label (matching web LiveTaskRow and FocusScreen.kt:80), keeping `elapsedSec` only for write-back math.

### [Today / Start-Next / Up-Next] Start Next hero ignores firstPhysicalAction (no "first physical action" headline)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:184
- CONFIRMED. Web StartNextCard sets `headline = task.firstPhysicalAction ? task.firstPhysicalAction : task.name` (start-next-card.tsx:39-41), shows `area · name` as the eyebrow (line 124), renders the headline as the big H3 (line 135), and the subtitle 'First physical action. Nothing else right now.' (lines 136-142). Android StartNextHero always shows `task.name` as the big title (TodayScreen.kt:184) with eyebrow `area · name` (line 182) and a generic '${estimateMin} min · Low friction' subtitle (line 185); `firstPhysicalAction` is never read. The model HAS `firstPhysicalAction` (Models.kt:91). The 'do just the first physical step' framing is lost.
- fix: Set the big title to `task.firstPhysicalAction?.ifBlank { null } ?: task.name`, keep `area · name` in the eyebrow, and add 'First physical action. Nothing else right now.' as the subtitle to match the web card.

### [Today / Start-Next / Up-Next] No "Pick another" action on the Start Next hero
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:186
- CONFIRMED. Web StartNextCard exposes 'Pick another' (kind=line) and 'Start now' (kind=coral) buttons (start-next-card.tsx:165-181); dashboard wires `onPickAnother={() => router.push('/tasks')}` (dashboard/page.tsx:53). Android StartNextHero (TodayScreen.kt:171, signature `StartNextHero(task, onStart)`) exposes only the single 'Focus' button (line 186). No way to reject the recommendation from the card. The tab-switch mechanism exists in MainScaffold (`var tab by remember { mutableStateOf("today") }`, MainScaffold.kt:65; `onSelect = { tab = it }` at 111), so threading a lambda is straightforward.
- fix: Add a secondary 'Pick another' button next to Focus; thread an `onPickAnother` lambda from MainScaffold's TodayScreen call (MainScaffold.kt:98) that sets `tab = "tasks"`.

### [Today / Start-Next / Up-Next] No AI rationale chip on Start Next
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:176
- CONFIRMED. Web StartNextCard renders an AIChip with `rationale` ('AI · suggested', start-next-card.tsx:101-105); dashboard computes `startNextRationale = rationaleFor(startNextTask, blocks)` (dashboard/page.tsx) and passes it in. lib/start-next-rationale.ts exists on web (rationaleFor: 'Best fit for a {gap}-min gap before {next}', 'Quick win — only N min before…', 'Lowest-friction option for right now.'). Verified NOT ported: grep for 'rationaleFor|Rationale|Lowest-friction|Best fit' across unstuck_android/core/ returns nothing. Android StartNextHero (TodayScreen.kt:170-190) shows no chip and computes no rationale.
- fix: Port lib/start-next-rationale.ts into :core (rationaleFor(task, blocks, now)) and render a small 'AI · suggested' chip with the rationale text in the Start next header row (next to the 'Start next' label at line 178).

### [Focus: treatments, pause-rea] 'Done' while running does NOT mark the task complete, unlike web 'Done early'
- regression | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:147
- Web running-state button is 'Done early', wired to `onMarkComplete ?? onDone` (controls.tsx:100); onMarkComplete writes the session AND flips task done=true (focus-mode.tsx:135-162). Android's prominent running 'Done' button calls vm.finishFocus(task) with markDone=false (FocusScreen.kt:147 → AppViewModel.kt:223, records the session, keeps the task open). So a user tapping the obvious 'Done' expecting to finish leaves it pending. Android does have a separate '✓ Mark complete' link (FocusScreen.kt:156-159, markDone=true), but the primary button's semantics diverge from web.
- fix: For web parity, rename the running primary to 'Done early' and call finishFocus(task, markDone=true); OR relabel it 'End for now' so its keep-open behavior is unambiguous. Web treats Done early = mark complete.

### [Focus: treatments, pause-rea] No 'done' summary state with Pick next / Save & continue later
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:147-164
- Web has a distinct done state ('You did it.', 'MM:SS in focus') with three actions: 'Mark complete', 'Save & continue later', 'Pick next task' (controls.tsx:152-164; onEndSession stays on /focus showing the summary, focus-mode.tsx:163-170). Android finishFocus() clears the live session (AppViewModel.kt:233) then FocusScreen jumps straight into ReflectSheet (FocusScreen.kt:147,164) and on dismiss calls onClose() back to Today — no done summary, no 'Pick next task', no 'Save & continue later'. The user loses the web's post-session branching. (Android does snapshot reflectElapsed before finishing, FocusScreen.kt:147, so a summary could show real time.)
- fix: After finishing, render a forced DONE state (finishFocus clears the live session, so use a local flag) showing elapsed-in-focus (reuse reflectElapsed, web's doneElapsedSec pattern) and offering Mark complete / Save & continue later / Pick next task.

### [Focus: treatments, pause-rea] Soft-exit confirmation is not enforced when leaving mid-session
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:94
- Web: when softExit is on and state==='running', leaving via ESC triggers window.confirm('Leave focus mid-session? Your timer keeps running.') before navigating (focus-mode.tsx:60-68). Android's '← Out' button calls onClose() unconditionally (FocusScreen.kt:94) and the focus BackHandler also calls `focusTask = null` unconditionally (MainScaffold.kt:90); settings.focusSoftExit is loaded (FocusScreen.kt:61) but never consulted. The intent (session keeps running) is preserved, but the confirmation guard is absent.
- fix: When settings.focusSoftExit && state==RUNNING, show a confirm dialog before onClose() in both the '← Out' tap (FocusScreen.kt:94) and the focus BackHandler (MainScaffold.kt:90). Skip the prompt when paused/idle/done.

### [Focus: treatments, pause-rea] Per-treatment countdown + over-estimate display not reproduced; big number always counts up
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:131-136
- Web cockpit and monk show TIME REMAINING as a countdown formatMMSS(max(0, estimate-elapsed)) and switch to '+MM:SS' with an OVER ESTIMATE label in overrun (cockpit.tsx:76-79,187; monk.tsx:56-60); idle shows the full estimate. Android FocusScreen.kt:131 shows the same raw count-up `formatMMSS(elapsed)` for every treatment and a '${formatMMSS(remaining)} left' caption (line 132), but never renders the over-estimate '+MM:SS' nor switches the big number to a countdown; the only treatment-specific behavior is hiding the ring/chips/title in monk. Cockpit's right rail is also reduced to a 3-line 'Captures' list (CapturesRail, FocusScreen.kt:202-212) with no UP NEXT card.
- fix: Per treatment, compute the displayed timer like web: idle→estimate, running→countdown (estimate-elapsed), overrun→'+'+over. Add an OVER ESTIMATE banner in overrun. For cockpit add an UP NEXT card (pickStartNext excluding the live task) alongside captures.

### [Focus: treatments, pause-rea] Calibration hint on idle (ambient) is absent
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:64
- Web ambient idle screen computes calibrationHint() — average actualSec over sessions whose taskId is in the same lifeArea (>=3 samples), shown as 'Last N <area> tasks averaged X min · estimate looks reasonable.' under the big estimate (ambient.tsx:17-27,138-160). Android FocusScreen auto-starts on mount (line 64) so there is no idle/estimate-preview screen at all, and the hint is never surfaced.
- fix: If an idle/ready state is introduced (see the auto-start fix), port calibrationHint(): average actualSec over sessions whose taskId is in the same lifeArea (>=3 samples) and show the hint line under the estimate.

### [Calendar: day grid (24h/now/] Day-grid NOW line is computed once at composition and never re-ticks
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:171-181
- Verified. Web re-evaluates the now position every 60s via window.setInterval (today-timeline.tsx:189-201). Android computes `val lt = java.time.LocalTime.now()` inline in the composable body (DayGrid.kt:172) with no backing state or periodic trigger, so the coral NOW line/label is frozen at first composition and only moves on an unrelated recomposition. The line drifts out of date during a session.
- fix: Drive a ticking state: `var nowMin by remember { mutableStateOf(LocalTime.now().let{it.hour*60+it.minute}) }; LaunchedEffect(Unit){ while(true){ nowMin = LocalTime.now().let{it.hour*60+it.minute}; delay(60_000) } }` and position the line from nowMin so it recomposes each minute.

### [Calendar: day grid (24h/now/] Cannot pick an arbitrary date or time when rescheduling a block
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:241-254
- Verified. Web lets the user set any date (type=date) and any time (type=time) on a block (cal-block-edit-modal.tsx:349-360) and move across days by drag. Android's only reschedule path is the start-time chip row (DayGrid.kt:252-254), built from findFreeSlotsForDate(blocks, live.durationMinutes, live.date, ...) (DayGrid.kt:241) — constrained to live.date plus algorithmic 8am-6pm slots (FreeSlots.kt:108-109 default dayStartMin/dayEndMin), so a block can never be moved to a different day and times are limited to suggestions. vm.moveBlock() accepts an arbitrary date/time (AppViewModel.kt:180-182) but no UI exposes it.
- fix: Add a date picker + free time picker to the edit sheet that call vm.moveBlock(block, chosenDate, chosenTime); keep free-slot chips as quick suggestions.

### [Calendar: day grid (24h/now/] Cannot drag an existing block on the grid to move it (only tray tasks are draggable)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:157-167
- Verified. Web makes scheduled task blocks draggable: today-timeline.tsx:380-381 (`draggable={interactive}` + startBlockDrag) and week-full.tsx:580-581, with a snapped drop hint. On Android the positioned block Box has only `.clickable { editingBlock = b }` (DayGrid.kt:163) and no detectDragGesturesAfterLongPress — blocks cannot be dragged to move. Only unscheduled tray cards are draggable (DayGrid.kt:199-206).
- fix: Add a long-press drag handler to the block Box that, on drop, snaps Y to a 15-min slot and calls vm.moveBlock(b, date, snappedHhmm) (reuse the existing drop()/snap math in DayGrid.kt:100-111), with a drop-hint overlay like web's dropHint.

### [Calendar: day grid (24h/now/] No resize-by-drag on blocks; duration limited to 5 fixed chip values
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:256-259
- Verified. Web exposes a bottom resize handle on each focus block dragging duration in 15-min steps from 15 to 360 min (today-timeline.tsx:447-499 TimelineResizeHandle, clamp Math.max(15,Math.min(360,...)) at 466; week-full.tsx:645-697 ResizeHandle at 664). Android offers only fixed chips 15/25/45/60/90 (DayGrid.kt:258) — 30, 120, 180, etc. are unreachable from the calendar, and there is no drag-to-resize. vm.resizeBlock already clamps 15..360 (AppViewModel.kt:184), so the backend supports arbitrary values.
- fix: Add a drag-resize handle at the block's bottom edge (15-min snap, clamp 15..360) calling vm.resizeBlock, or at minimum a numeric duration input, instead of the limited chip set.

### [Calendar: day grid (24h/now/] No click-on-empty-slot to create a task/block on the day or week grid
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:127-182
- Verified. Web's day and week grids open a prefilled TaskCreateModal on empty-grid click with snapped date+time: today-timeline.tsx:280-295 (onClick → setCreatePrefill, modal at 434-439) and week-full.tsx:541-552 (modal at 632-637); TaskCreateModal honors prefillDate/prefillTime (props at 74-75, applied at 145-161). Android's DayGrid grid Box (DayGrid.kt:127-182) has no click-to-create handler, and WeekView day columns are non-interactive (CalendarScreen.kt:153-173). Android's NewTaskSheet(vm, onDismiss) (NewTaskSheet.kt:74) accepts no date/startTime prefill at all. Users can only schedule by dragging a tray task.
- fix: Add a tap handler on empty grid space that snaps Y to a 15-min slot and opens NewTaskSheet prefilled with date+time; extend NewTaskSheet to accept optional prefillDate/prefillStartTime (it currently only has a WHEN chip + free-slot time auto-pick).

### [Calendar: day grid (24h/now/] Month-grid density uses month-relative max normalization instead of web's fixed 6h/day reference, so color is not comparable across months
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:200,225-231
- Verified. Web's loadForDay (calendar-density.ts:35-38) normalizes a day's focused minutes against a FIXED USABLE_MIN_PER_DAY = 6*60 = 360 (calendar-density.ts:6), so a full day is the same intensity in every month, and the cell '{Math.round(c.load*6)}h' label (month-full.tsx:288, shown when load>0.3) is meaningful; cell color is `color-mix(... var(--u-primary) ${Math.round(c.load*60)}% ...)` (month-full.tsx:266). Android's MonthView normalizes each day against the visible month's own max: `val max = (byDay.values.maxOrNull() ?: 1).coerceAtLeast(1)` (CalendarScreen.kt:200) then `t = (v.toFloat()/max)` (CalendarScreen.kt:225). A light month's 30-min peak day saturates identically to a heavy month's 6h day — not comparable across months. Android also stores actualSec and divides by max-of-seconds, and renders no per-cell hours label (only the day number, CalendarScreen.kt:231).
- fix: Port loadForDay with the fixed 360-min reference: `val t = ((byDay[iso] ?: 0)/60f / 360f).coerceIn(0f,1f)` (byDay holds seconds, so /60f → minutes) and render a '${(t*6).roundToInt()}h' label on cells with t>0.3, matching month-full.tsx.

### [Calendar: day grid (24h/now/] Month view is missing the Busiest/Lightest/load-delta sidebar and drop-to-schedule
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:188-242
- Verified. Web MonthFull shows a side panel: Busiest day with focused minutes (month-full.tsx:322-347, pickBusiest at 50-57), Lightest day (348-373, pickLightest at 59-70), a 'No sessions yet' empty state (302-321), and a load-delta-vs-last-month flame callout when |delta|>=10% (374-393, loadDeltaPct at 150-152 from prev-month totals). Day cells accept drops to schedule at 09:00 or move a block to that date (onCellDrop at 162-191). Android MonthView (CalendarScreen.kt:188-242) renders only the density heatmap — no busiest/lightest stats, no load delta, no empty-state copy, no drop-to-schedule or block-move on cells.
- fix: Add busiest/lightest/load-delta stat cards computed from byDay + previous month (port pickBusiest/pickLightest/totalMinutes), an empty-state when sessions is empty, and optionally day-cell drop-to-schedule like month-full.tsx onCellDrop.

### [Calendar: day grid (24h/now/] Week grid is missing usable-capacity load, overload risk, per-day planned/usable, move/resize, click-to-create, and unscheduled tray
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:111-177
- Verified. Web WeekFull shows per-day 'planned h / usable h' headers reddened when over (week-full.tsx:457-490), a Focus-planned stat with % load vs usable capacity (321-326, rollup at 210-232 using useUsablePref weekday/weekend mins), an Overload-risk card naming the over-capacity day + amount over (327-334 and flame callout 406-420), a Light-day card (335-342), a 'DRAG INTO YOUR WEEK' unscheduled tray with draggable cards (344-404), draggable/resizable blocks (580-619), day-cell drops to reschedule/create (537-552), and click-to-create. Android WeekView (CalendarScreen.kt:111-177) shows only Focus-planned total + Busiest/Lightest day NAMES (130-134); no usable-capacity/load %, no overload warning, no per-day usable comparison, no unscheduled tray; blocks are tap-to-open-task only (CalendarScreen.kt:168, not draggable/resizable); day columns accept no drops or clicks. It also uses a fixed 6am-11pm window (WSTART=6/WEND=23, CalendarScreen.kt:56-57) vs web's full 24h (FIRST_HOUR=0/LAST_HOUR=23, week-full.tsx:29-30).
- fix: Add usable-capacity load math (port weekday/weekend usable minutes from user prefs), per-day planned/usable header with over-coloring, overload + light-day cards, an unscheduled tray, and at least drop-to-schedule + block-edit parity with the day grid.

### [Google Calendar connect/pull] No foreground auto-sync / no pull on calendar open / no visible-range pull
- missing | app/src/main/kotlin/tech/csalliance/unstuck/surface/SyncWorker.kt:25; app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:64-80 (no pull-on-open); DayGrid.kt:84 (scroll-only)
- VERIFIED. Web runs a 5-min foreground auto-pull while a tab is open: google-sync.ts:334-345 startAutoSync (AUTO_PULL_INTERVAL_MS=5*60*1000, pulls immediately then setInterval), kicked from bootstrap-listener.tsx:133 on sign-in and sync-flow.tsx:139 on connect, and pulls the visible grid range when the month changes (month-full.tsx:123-125 pullAndIngest({from:gridStart,to:gridEnd})). Android only pulls: once on sign-in (SyncCoordinator.kt:133), once on connect (:86), on manual 'Sync now' tap (CalendarScreen.kt:105 -> vm.syncCalendar -> pullCalendar), and a 30-min WorkManager (SyncWorker.kt:25, PeriodicWorkRequest 30 MINUTES, KEEP). grep confirms no foreground recurring pull / no LaunchedEffect-driven pull exists — DayGrid.kt:84 LaunchedEffect(date) only scrolls; the only while(true) near sync is FocusScreen.kt:66 (a 1s clock ticker) and OutboxFlusher.kt:18 (drain loop). So Google changes can be up to 30 min stale and aren't refreshed when the user opens the calendar.
- fix: Add a LaunchedEffect(Unit) in CalendarScreen (or an app foreground scope) that calls vm.syncCalendar() on open then loops with delay(5*60_000) while composed, mirroring startAutoSync's pull-on-mount + 5-min cadence. Optionally pass the visible week/month range into pullCalendar so far-out months refresh like month-full.tsx.

### [Google Calendar connect/pull] No status/diagnostics surface: connect result, pull/push errors, and last-synced are invisible
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:84-108 (no status); SyncCoordinator.kt:88,93,98 (errors swallowed); MainActivity.kt:73 (Boolean result dropped)
- VERIFIED. Web surfaces per-action status strings ('Connecting your Google Calendar…', 'Connected <email>. Pulled N events', 'Connect failed: …', 'Google denied access: …') (sync-flow.tsx:117,132,134,151-152), a 'last synced HH:MM · N events' / 'sync error · HH:MM' label (sync-flow.tsx:419-427), and a SYNC DIAGNOSTICS panel showing last pull (count/error) and last push (action/ok/error/skipped) from getSyncStatus() (sync-flow.tsx:485-520; status from google-sync emitStatusChange). Android shows none: completeGoogleConnect returns Boolean that MainActivity.handleAuthOrCalendar discards (MainActivity.kt:73 `graph.coordinator?.completeGoogleConnect(code, state)` — result unused), CalendarSyncBar shows only a 'Connecting…' label (CalendarScreen.kt:102), pullCalendar returns Unit, and pull/connect failures are swallowed by runCatching{}.getOrNull()/getOrElse{false} (SyncCoordinator.kt:88,93,98) with no user-visible error. A user whose connect or pull fails (including the #1 listConnections crash) gets zero feedback.
- fix: Have pullCalendar/completeGoogleConnect return a result (count + error) and expose it via a StateFlow on AppViewModel (e.g. lastSync: SyncStatus). Render a status line + last-synced time + error state in CalendarSyncBar, mirroring sync-flow.tsx. Route completeGoogleConnect's result back to the UI (shared flow) instead of discarding it in MainActivity.

### [Collections CRUD (new/rename] Card preview ignores pinned items — pinned items may never appear in the preview
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:97
- Web CollectionCard surfaces pinned items first, then the most recent non-pinned, then slices to 3: `const pinned = collection.items.filter(i => i.pinned); const recent = collection.items.filter(i => !i.pinned).slice(-3); const previews = [...pinned, ...recent].slice(0, 3)` (collection-card.tsx lines 19-21). Android just does `col.items.take(2)` (CollectionsScreen.kt line 97), i.e. the first two items in insertion order regardless of pin state. A collection whose pinned items are not in the first two inserted will show preview text that omits exactly the items the user pinned to surface. Web also previews up to 3 lines; Android caps at 2. Verified against both files.
- fix: Compute previews like web: `val pinned = col.items.filter { it.pinned == true }; val recent = col.items.filter { it.pinned != true }.takeLast(3); (pinned + recent).take(3).forEach { ... }`.

### [Collections CRUD (new/rename] Signed-out demo seed for collections is absent on Android
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:50
- Web seeds six demo collections (Groceries, Books to read, Quotes, Watch later, Recipes, Gift ideas) into the cache when the user is signed out and the store is empty (use-collections.ts DEMO_COLLECTIONS lines 42-97 + seedIfSignedOutAndEmpty lines 103-121, gated on __unstuckResolved && !currentUserId()). Confirmed Android has no equivalent: a tree-wide grep for DEMO/seedDemo/Groceries/'Books to read'/'Watch later' across all .kt files returns nothing (only the audit markdown). Android areas/tasks are seeded ONLY via the signed-in onboarding flow (OnboardingScreen.kt) and server hydration — there is NO signed-out localStorage-style demo seed for ANY table. A signed-out / first-run Android user lands on a completely empty Collections screen. NOTE: the original fix text's claim of 'the existing signed-out life-areas/tasks seed' gate is inaccurate — no such signed-out seed gate exists on Android; the closest analog is onboarding. The first-run parity gap itself is real.
- fix: If demo parity is desired, add a signed-out demo seed: when authed==false and the collections store is empty, insert the same six demo collections locally with friendly (non-uuid) ids so the sync uuid-guard skips them. There is no existing signed-out seed to mirror, so this would be a new code path (e.g. in AppViewModel on authed==false + empty collections, or in a hydrate/bootstrap step).

### [Collections CRUD (new/rename] Collections overview has no empty-state
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:85
- Web shows a dedicated empty-state card ('This can hold things for later.' / 'You don't need to remember everything.') whenever the filtered list is empty — both for a genuinely empty account and for a search with no matches (collections-overview.tsx lines 113-134, `filtered.length === 0`). Android's LazyVerticalGrid renders only the header span + search + New button and nothing else when `shown` is empty (CollectionsScreen.kt lines 85-103 just iterate `items(shown, ...)` with no empty branch). Verified against both files.
- fix: When `shown.isEmpty()`, render a full-span empty-state item with the web copy ('This can hold things for later.' / 'You don't need to remember everything.'), optionally distinguishing empty-shelf vs no-search-results.

### [Insights / Analytics (report] Report hides all charts below the 5-session threshold; web shows them with a 'sample' badge
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:103
- Android gates StackedBars, the interruption histogram, and the 'Worth noticing' insights behind if (enough) (InsightsScreen.kt:103-117), so below 5 sessions the Report is just a threshold note + 3 stat cards. Web always renders the stacked bars (report.tsx:406; realBars is computed unconditionally at line 311), the calibration scatter (414) and the interruption shape (415) even below REAL_DATA_THRESHOLD, tagging each below-threshold panel with a 'sample' badge (sampleBadge at 377/161/247) plus empty-state copy ('finish N sessions…'). Note realDots/realBins are [] below threshold (312-313) so those two render as empty sample charts, but realBars shows the user's actual (possibly zero) data. Android loses the shape-suggestive empty state and the sample badge entirely.
- fix: Render StackedBars (and an empty-state calibration scatter + interruption histogram) regardless of enough — they already use real scoped data — and surface a 'sample' badge / sub-copy on each when !enough, matching report.tsx. Keep the insights section gated since topInsights already returns [] below threshold.

### [Insights / Analytics (report] Captures-by-kind is window-scoped on Android but all-time on web
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:150
- Web's CaptureFlow (deep-dive.tsx:335-336) calls useCaptures() DIRECTLY — it intentionally ignores the week/month/all window and shows proportions across ALL captures (sub line 'proportions across all captures', deep-dive.tsx:488). Android feeds the window-scoped captures list into captureBreakdown (InsightsScreen.kt:150), so the 'Captures by kind' bars change with the range toggle and won't match web for the same account. The Deep-dive 'Captures kept' stat IS supposed to be scoped (web uses scoped captures there, deep-dive.tsx:431,444), so only the breakdown chart diverges.
- fix: Pass the unscoped allCaptures into captureBreakdown for the 'Captures by kind' panel (captureBreakdown(allCaptures)), keeping the scoped captures only for the 'Captures' stat card.

### [Insights / Analytics (report] Deep-dive lost the 'Focus this week' total-time headline stat
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:127
- Web Deep-dive's first stat is 'Focus this week' = total focused time formatted as 'Xh Ym' across N sessions (deep-dive.tsx:441, sessionsLabel at 422-424). Android's Deep-dive stat grid is Median / On estimate% / Re-entry<5m / Captures (InsightsScreen.kt:127-132) — it dropped the total-focus-time stat and instead carries 'On estimate $hit%' (which on web is a Report ESTIMATES card, not a Deep-dive stat). totalMin is even computed at InsightsScreen.kt:81 but unused. So the single most prominent Deep-dive number (total focus time) is missing.
- fix: Add a 'Focus this window' StatCard showing total focused time (format sessions.sumOf{it.actualSec} as 'Xh Ym', '—' when 0) and 'across N sessions', matching deep-dive.tsx:441; either replace the redundant 'On estimate' card or expand to a 5-stat layout.

### [Insights / Analytics (report] Interruption histogram is missing its peak narrative, x-axis minute labels, peak-bar highlight and 'minutes into a session' caption
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:105
- Web InterruptionShape (report.tsx:228-287) computes the peak bin, highlights only that bar in coral while the rest are primary-soft (line 274), prints per-bar x labels '{i*3}–{(i+1)*3}' (277-279) plus a 'minutes into a session' caption (283-285), and shows a narrative ('Most happen around the {lo}-{hi} min mark…' or the no-captures empty-state, 250-254). Android reuses the generic Histogram (InsightsScreen.kt:218-232, called at 105) which colors EVERY non-zero bar the same color (line 227, no peak emphasis), has no x-axis bucket labels, no caption, and no narrative line.
- fix: Build a dedicated interruption composable: highlight the max bin in coral and others in primarySoft, add per-bar '{i*3}–{(i+1)*3}' labels, a 'minutes into a session' caption, and the peak narrative / no-captures empty state from report.tsx.

### [Insights / Analytics (report] Deep-dive heatmap omits the per-cell minute numbers and hour-bucket column headers
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:248
- Web TimeHeatmap (deep-dive.tsx:103-153) renders column headers for the six 2-hour buckets ('7–9','9–11','11–1','1–3','3–5','5–7', lines 105,115-123) and prints the rounded minutes (Math.round(data[ri][ci]*60), line 147) inside each non-empty cell. Android Heatmap (InsightsScreen.kt:248-267) draws only the colored cells with Mon–Fri row labels (line 258) — no bucket column headers and no minute numbers in the cells, so the user can't read which hour band or how many minutes a cell represents.
- fix: Add a header row of the six bucket labels above the grid, and render Math.round(v*60) (minutes) centered in each cell when v>0 (white-ish text when intensity is high, ink3 otherwise), matching deep-dive.tsx.

### [Command palette + quick capt] No global relevance ranking — results are bucketed by type, so the best match is buried
- regression | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:50-61
- Web scores every candidate via score() (exact=1000, startsWith=500-len, contains=200-len, else 0; command-palette.tsx:35-43), sorts ALL candidates together and slices the top 12 (lines 105-113); Enter selects the single best match regardless of type. Android concatenates fixed buckets `taskResults + noteResults + actions` (CommandPalette.kt:61) with plain `contains` and no scoring or cross-bucket sort. Typing 'Settings' still lists matching tasks first and pushes the exact 'Settings' action below them; an exact-name match never floats to the top. Ranking/ordering parity is lost.
- fix: Port web's score() into core, attach a score to every candidate (tasks, captures, areas, actions), filter score>0, sort descending, and take(12) across the combined list instead of fixed per-bucket take()s.

### [Command palette + quick capt] Text field does not auto-focus / raise keyboard on open
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:67
- Web focuses the search input as soon as the palette opens (command-palette.tsx:59-61, requestAnimationFrame(() => inputRef.current?.focus())), so the user can type immediately. Android's BasicTextField (CommandPalette.kt:67) has no FocusRequester/LaunchedEffect (confirmed no FocusRequester import or usage in the file), so opening the palette leaves the field unfocused and the soft keyboard hidden until the user taps it — an extra tap before the primary quick-search action.
- fix: Add `val fr = remember { FocusRequester() }`, attach `.focusRequester(fr)` to the BasicTextField modifier, and `LaunchedEffect(Unit) { fr.requestFocus() }` to auto-focus and raise the keyboard on open.

### [Settings & preferences (ever] 'Hide right rail while focusing' (focusCollapseRail) is persisted but never consumed
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:125
- Web reads PREF_FOCUS_COLLAPSE in the product layout to hide the right rail during an active focus session (settings-panel.tsx:55 default true). Android persists focusCollapseRail (SettingsStore.kt:26,60,81, default true) and exposes the toggle 'Hide right rail while focusing' (SettingsScreen.kt:125), but grep finds no consumer outside SettingsStore + the toggle. On a phone the focus screen is full-screen with no persistent right rail, so this is likely a no-op by design — but then it's a control that misleads the user.
- fix: Android has no collapsible right rail (focus is full-screen), so remove the toggle from the Focus section rather than show a setting that changes nothing. (Only wire it if a secondary pane is ever added.)

### [Settings & preferences (ever] 'Soft exit' (focusSoftExit) is persisted but never consumed
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:94
- Web reads PREF_FOCUS_SOFT_EXIT (settings-panel.tsx:56 default true; focus-mode.tsx:31) and uses it to gate ESC soft-exit while running (focus-mode.tsx:64: if (softExit && timer.state === 'running') ...). Android persists focusSoftExit (SettingsStore.kt:27,61,82, default true) and shows the toggle (SettingsScreen.kt:126), but grep finds no consumer beyond SettingsStore + the toggle. The 'Out' tap (FocusScreen.kt:94 onClose) immediately leaves, and MainScaffold's BackHandler (MainScaffold.kt:90 BackHandler(enabled = focusTask != null){ focusTask = null }) also exits with no confirmation. So leaving focus mid-session never asks, regardless of the setting.
- fix: When settings.focusSoftExit and the session is RUNNING, intercept the 'Out' tap and the focus-level system back (currently MainScaffold.kt:90) to show a confirm-exit dialog instead of immediately leaving. Skip the confirm when paused/overrun or when the toggle is off. Note the back interception lives in MainScaffold's BackHandler, not FocusScreen, so the fix must touch both.

### [Settings & preferences (ever] Interface section missing the 'Focus mode treatment' control
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:141-151
- Web Interface section has four rows: Theme, Density, Accent, AND 'Focus mode treatment' (ambient/cockpit/monk) — settings-panel.tsx:275-285 (Segment over ['ambient','cockpit','monk']). Android's INTERFACE SettingsCard (SettingsScreen.kt:141-151) has only Theme, Accent, Density; the treatment selector is absent and only reachable via chips inside the running focus screen (FocusScreen.kt:106-108). A user not in a session can't set their default treatment from Settings. SettingsState.treatment exists and is persisted (SettingsStore.kt:33,67,88), and vm.setTreatment persists via updateSettings (AppViewModel.kt:211-214).
- fix: Add SegRow("Focus mode treatment", listOf("ambient","cockpit","monk"), s.treatment.name.lowercase()){ vm.setTreatment(FocusTreatment.valueOf(it.uppercase())) } to the INTERFACE SettingsCard (mark the previous Density row non-last). setTreatment already persists.

### [Settings & preferences (ever] Life areas: cannot rename or recolor an area, and no weekly-hours stat
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:295-318
- Web LifeAreaPanel supports Rename, Recolor via the full 8-token COLOR_TOKENS palette (indigo/coral/green/amber/teal/blue/violet/red — area-dot.tsx:40), Delete, add-with-color, and shows TWO stats per area: 'N open' AND 'Xh this week' (statsFor sums last-7-day session actualSec, life-area-panel.tsx:27-36, rendered :254-255). Android AreasContent (SettingsScreen.kt:276-320) only supports Add (auto-color palette[areas.size % 6], no picker) and Delete via the dot-menu (only menu item is 'Delete area', :298), shows only 'N open' (correctly gated !it.done at :285), and has NO rename, NO recolor, NO weekly hours. vm.upsertLifeArea exists (AppViewModel.kt:323) so rename/recolor are trivially wireable. Note the Tags section already implements rename + a recolor palette popup (SettingsScreen.kt:339-358), so the pattern exists to copy.
- fix: Add 'Rename' (inline edit -> vm.upsertLifeArea(a.copy(name=...))) and a color-palette popup (mirror the Tags palOpen popup at :339-343, calling vm.upsertLifeArea(a.copy(color=...))) to the area dot-menu; add a color picker to the add-area form; compute and show weekly focused hours per area from sessions (mirror statsFor). Align the palette to web's 8 tokens.

### [Settings & preferences (ever] Backup section is fake: 'Auto-export every Sunday' is a no-op toggle and 'Export now' does nothing
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:152-155
- Web Backup section shows real sync status: a signed-in/paused banner, an expandable status panel with session state, last-sync-ping timestamp, and per-table row counts (settings-panel.tsx:289-352, driven by useSyncStatus). There is NO 'Auto-export every Sunday' feature on web. Android BACKUP (SettingsScreen.kt:152-155) renders ToggleRow("Auto-export every Sunday", true) {} — hardcoded on, no backing state, empty onChange — and SettingRow("Export now", ...) {} with an empty onClick, so the button does nothing. Real export only works via Account > 'Export everything' (SettingsScreen.kt:182, using the CreateDocument exporter + vm.exportJson()). So this whole section is an invented/broken control.
- fix: Remove the fake auto-export toggle. Either wire 'Export now' to the same CreateDocument launcher + vm.exportJson() used in AccountContent (SettingsScreen.kt:171-176,182), or remove it. Add a real sync-status display (signed-in state via vm.currentEmail, pending write count via vm.pendingCount (AppViewModel.kt:70), table row counts) to match the web Backup status panel.

### [Settings & preferences (ever] No overrun check-in / extend prompt in focus mode
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:131
- Web's overrun state renders an explicit check-in with Extend / keep-going / wrap-up actions (controls.tsx onExtend wired to timer.extend via focus-mode.tsx:193 onExtend: timer.extend). Android FocusScreen has no overrun UI: when state==OVERRUN it only tints the timer text coral (FocusScreen.kt:131) and shows no prompt or Extend button. vm.extendFocus exists (AppViewModel.kt:215, calls FocusTimer.extend) but grep confirms its ONLY call site is its own definition — it is never invoked from any UI. So even after fixing the overrun grace, the user is never asked to check in or given a way to extend.
- fix: When state==OVERRUN, show a check-in row (e.g. 'You're past your estimate' + 'Keep going +10' calling vm.extendFocus(10) + 'Wrap up' calling finishFocus). Show it only when overrun escalation is enabled (grace != Infinity, i.e. focusOverrunMin != 0), mirroring web.

### [Account / Auth (sign in/up/m] Data export bundle drops several collections and metadata the web export includes
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:383-412
- Confirmed. Web exportAll (data-export.ts:8-64) writes schemaVersion:1, exportedAt, user{email, displayName}, tasks, sessions, cal_blocks, reason_logs, captures, life_areas, tags, collections, calendar_connections, and preferences{adhd_struggles, today_filter}. Android ExportBundle (AppViewModel.kt:383-390, 399-412) writes only exportedAt, email, tasks, sessions, calBlocks, captures, reasonLogs, collections, tags, lifeAreas. Missing: schemaVersion, user displayName, calendar_connections (the connections StateFlow already exists at AppViewModel.kt:67), and the preferences block. Note: schemaVersion + displayName + connections are trivially available client-side; today_filter has no Android equivalent (no such pref exists in the Android settings store) and adhd_struggles has only a server-side setter (PreferencesClient.setAdhdStruggles, Clients.kt:59) with no reader, so the full preferences block is harder to populate. The high-value, cheap gaps are schemaVersion, displayName, and connections.
- fix: Add schemaVersion=1, displayName (currentName), and connections (connections.value, already available) to ExportBundle/exportJson(), matching the web key names. For preferences, only emit fields the client can read (skip today_filter, which doesn't exist on Android) or add a PreferencesClient reader for adhd_struggles.

### [Account / Auth (sign in/up/m] Delete-account ignores the Edge Function result payload
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/AuthService.kt:69-73
- Confirmed. Web DeleteAccountModal (delete-account-modal.tsx:58-72) checks invokeError AND the body (`if (data?.deleted !== true) setError(data?.error ?? 'Delete request failed.')`) before signing out — explicitly documented as a fix for a 200 response carrying { deleted:false } / { error }. Android AuthService.deleteAccount (AuthService.kt:69-73) calls client.functions.invoke("account-delete") and immediately client.auth.signOut() without inspecting the response. supabase-kt invoke throws on non-2xx, but a 200 body of { deleted:false, error:"..." } is treated as success: the user is signed out and shown nothing while the server-side account may not be deleted.
- fix: Decode the invoke result (e.g. body<JsonObject>()) and only signOut() when deleted == true; otherwise return AuthOutcome.Error with the body's error message, matching the web guard.

### [Account / Auth (sign in/up/m] No client-side validation on the auth form (empty fields, password length, required name)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:88,97-98,118,121
- Confirmed. Web validates before calling Supabase: sign-in requires non-empty email+password (sign-in/page.tsx:56-60), magic link requires non-empty email with 'Type your email first.' (sign-in/page.tsx:76-79, sign-up/page.tsx:83-86); sign-up requires a non-empty name ('Your name helps us greet you…'), non-empty email+password, AND password.length >= 8 (sign-up/page.tsx:49-60). Android AuthScreen submits whatever is typed with no guards (AuthScreen.kt:97-98 runs signUp/signIn directly; magic-link AuthScreen.kt:118 and reset AuthScreen.kt:121 fire even when email is blank). The name field is labelled 'Name (optional)' (AuthScreen.kt:88), contradicting the web's required-name rule, and AuthService.signUp treats an empty name as no-metadata (AuthService.kt:40-41) rather than blocking. A <8-char password reaches the server instead of being caught locally.
- fix: Before invoking: require non-empty email (and password for sign-in/up), require name on sign-up (drop the '(optional)' label to match web copy), enforce password.length>=8 on sign-up with 'Password must be at least 8 characters.', and short-circuit magic-link/reset when email is blank with 'Type your email first.'

### [Onboarding (struggles -> pre] Area color palette assigned by index, diverging from web's name-based color map
- wrong-api | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:48,60 (and AppViewModel.kt:332)
- finish() colors each picked area by AREA_PALETTE[i % size] with AREA_PALETTE = [indigo, coral, green, amber, violet, blue] (OnboardingScreen.kt:48,60). Web assigns by area NAME via COLOR_FOR: Work=indigo, Personal=coral, Volunteering=green, Home=amber, Health=teal (use-life-areas.ts:33-39). So Android gives Home=green (should be amber), Health=amber (should be teal, which isn't even in AREA_PALETTE), and reordering picks changes colors arbitrarily. completeOnboarding's default seed also uses [indigo, coral, violet, green] for Work/Personal/Home/Health (AppViewModel.kt:332-334), which likewise mismatches web (Home should be amber, Health teal).
- fix: Replace index-based palette with a name->color map mirroring web COLOR_FOR (Work=indigo, Personal=coral, Volunteering=green, Home=amber, Health=teal, fallback indigo). Fix both the onboarding picker and completeOnboarding's default seed to the same canonical map.

### [Onboarding (struggles -> pre] Seed task missing web's priority/tags and name fallback
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:61
- Web seeds the first task with priority:'medium', tags:['onboarding'], and name fallback 'My first Unstuck task' when blank (flow.tsx:62-74). Android only sets name (trimmed), estimateMin=15, lifeArea='Personal' (OnboardingScreen.kt:61) and SKIPS task creation entirely if firstTask is blank (`if (firstTask.isNotBlank())`). So a user who leaves the field empty gets no task and never sees the 'onboarding' tag / medium priority web guarantees.
- fix: Always create the seed task (use 'My first Unstuck task' fallback when blank) and pass priority = Priority.MEDIUM and tags = listOf("onboarding") to vm.addTask to match the web seed.

### [Recurrence (editor incl. unt] Weekly default day differs from web (Monday-only vs weekdays)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/RecurrenceEditor.kt:35
- Switching to Weekly with no prior days: web defaults to weekdays [1,2,3,4,5] in both editors (task-create-modal.tsx:115 useState([1,2,3,4,5]); RepeatEditor task-detail-pane.tsx:702-703 [1,2,3,4,5]). Android defaults to a single day: SelectableChip('Weekly') -> Recurrence.Weekly(if (days.isEmpty()) listOf(1) else days) (RecurrenceEditor.kt:35) — i.e. just Monday. Same gesture materialises a once-a-week (Mon) series on Android vs a 5-day weekday series on web.
- fix: Default the empty case to weekdays: Recurrence.Weekly(if (days.isEmpty()) listOf(1,2,3,4,5) else days).

### [Captures & reason logs (crea] Capture body is single-line on Android; web allows multi-line
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/CaptureSheet.kt:53
- Web composes captures in multi-line textareas: CaptureModal uses rows={4} (capture-modal.tsx:164, resize:'none') and the task-detail AddCaptureForm uses rows={2} (task-detail-pane.tsx:451-468). All three Android capture inputs force singleLine=true: focus CaptureSheet (CaptureSheet.kt:53), TaskDetailSheet AddCaptureRow (TaskDetailSheet.kt:259), and NewTaskSheet draft captures (NewTaskSheet.kt:172). With singleLine=true a Compose BasicTextField cannot wrap and the IME 'Done' action commits, so a multi-line thought collapses to one line on Android.
- fix: Drop singleLine=true on all three capture fields (CaptureSheet.kt:53, TaskDetailSheet.kt:259, NewTaskSheet.kt:172) and let them wrap (optionally set minLines/maxLines for a min height), matching the web textarea.

### [Captures & reason logs (crea] No 'Promote to task' from a capture during a focus session
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:202
- On web the cockpit treatment renders every session capture with a 'Promote to task →' button that calls promoteCapture (cockpit.tsx:356-370). Android's in-focus capture list, CapturesRail (FocusScreen.kt:202-212), is read-only — each row is just Text("• ${it.body}") with no promote or discard action. promoteCapture exists in AppViewModel (line 261) and is wired only into TaskDetailSheet (TaskDetailSheet.kt:163,242), so the web's 'turn a captured thought into a task without leaving focus' flow has no Android entry point.
- fix: Add a tappable 'Promote to task →' affordance to each CapturesRail row that calls vm.promoteCapture(it), mirroring cockpit.tsx. CapturesRail currently only renders for COCKPIT (FocusScreen.kt:134-136); consider surfacing it for any treatment that shows captures.

### [Life areas + area filter (CR] Add-area flow has no color picker (always auto-assigns by index)
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:311-318
- Web add forms both let the user choose color before committing: AddAreaForm (life-area-panel.tsx:398-416) and RailAddArea (left-rail.tsx:412-431), both calling add(name,color). Android AreasContent add row is a text field + Add button that hardcodes palette[areas.size % palette.size] with no choice (SettingsScreen.kt:316). Combined with the missing recolor, an Android user can never control an area's color.
- fix: Add a row of selectable ColorChips beside the add field (state var for picked color, default 'indigo') and pass it into the LifeArea(...) constructor instead of palette[areas.size % palette.size].

### [Life areas + area filter (CR] Per-area weekly focus hours stat is missing
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:290-294
- Web AreaRow shows TWO stats: '{open} open' and '{hours}h this week', where hours = sum of session.actualSec for that area's tasks within the last 7 days (statsFor, life-area-panel.tsx:27-37; rendered 254-255). Android AreasContent shows only '$open open' (SettingsScreen.kt:294) — the weekly-hours line is absent, losing the time-investment-per-area signal. vm.sessions is available in the VM (AppViewModel.kt:61).
- fix: Collect vm.sessions, build the set of task ids whose lifeArea==a.name, sum actualSec for sessions whose completedAt is within the last 7d, and render Math.round(sec/3600*10)/10 as a second line under '$open open' (NOTE: divide by 3600 for hours — the original finding's sec/360 was a typo). Mirror statsFor in life-area-panel.tsx.

### [Life areas + area filter (CR] Add-area allows duplicate names (no case-insensitive guard)
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:315-317
- Web add rejects an already-existing name case-insensitively (use-life-areas.ts:148: items.some(a=>a.name.toLowerCase()===trimmed.toLowerCase()) -> return). Android TagsContent enforces this for tags (SettingsScreen.kt:368: tags.none { it.name.equals(nm, true) }) and ensureTag does too (AppViewModel.kt:307). But area-add (SettingsScreen.kt:316) checks only draft.isNotBlank() — two 'Work' areas can be created. Because task.lifeArea is a denormalised name string, duplicate-named areas make counts/filter ambiguous (both rows show the same count; web dedupeByName would silently drop one on next load).
- fix: Guard the add: if (draft.isNotBlank() && areas.none { it.name.equals(draft.trim(), ignoreCase=true) }) { vm.upsertLifeArea(...); draft="" } — matching the tag-add guard at SettingsScreen.kt:368.

### [Life areas + area filter (CR] 'teal' missing from both picker AND color resolver; 'red' missing from picker only (resolver has it)
- missing | design/src/main/kotlin/tech/csalliance/unstuck/design/theme/Theme.kt:38-47; app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:281,328
- Web COLOR_TOKENS = [indigo, coral, green, amber, teal, blue, violet, red] (area-dot.tsx:40) and COLOR_TOKEN_TO_OKLCH maps all 8 (area-dot.tsx:26-35). Android: (1) the area/tag picker palettes are only [indigo, coral, violet, green, amber, blue] (SettingsScreen.kt:281,328) — both 'teal' and 'red' are un-offerable; (2) the resolver UnstuckColors.areaColor (Theme.kt:38-47) DOES include a 'red' branch (line 45) but has NO 'teal' branch, so an area synced from web/iOS with color='teal' falls through to the gray ink4 fallback and renders as an unassigned/gray dot everywhere. CORRECTION to original finding: 'red' is NOT missing from the resolver (Theme.kt:45); only 'teal' is. The picker-palette gap covers both teal and red.
- fix: Add a 'teal' branch to UnstuckColors.areaColor (e.g. teal -> a dedicated Color at oklch(0.70 0.10 200), distinct from blue which is oklch ~240), and extend both picker palettes (SettingsScreen.kt:281,328) to the full 8-token web set incl. teal and red so colors round-trip with web/iOS.

### [App shell / nav / overlays /] MainScaffold Detail overlay calls pop() during composition (state mutation during compose)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:120
- VERIFIED. MainScaffold.kt:118-121: is Route.Detail -> { val t = tasks.firstOrNull { it.id == route.taskId }; if (t != null) TaskDetailScreen(...) else pop() }. pop() (MainScaffold.kt:84) mutates the `stack` SnapshotStateList synchronously inside the composition lambda. `tasks` is a reactive flow (collectAsStateWithLifecycle, MainScaffold.kt:82) backed by Room. A realtime DELETE from another device removes the task (RealtimeMirror.kt:44 store.delete(TASKS,id) on PostgresAction.Delete), so the open Detail task can vanish from `tasks` while Route.Detail is still on the stack → the inline else pop() fires DURING composition. Writing snapshot state during composition is a Compose correctness violation (can throw / unstable recomposition). Same risk applies to Route.Collection if a collection vanishes.
- fix: Replace inline `else pop()` with a side-effect: LaunchedEffect(t == null) { if (t == null) pop() } and render nothing/placeholder that frame. Apply the same to Route.Collection.

### [App shell / nav / overlays /] No sync-status / backup-status surface; BACKUP settings section is a non-functional stub; pendingCount never shown (no offline indicator)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:152
- VERIFIED. Web surfaces sync state in Settings → Backup via useSyncStatus (lib/use-sync-status.ts: signedIn, lastSyncAt, rowCounts) rendered in settings-panel.tsx:346-352 (Session signed-in/out, 'Last sync ping' timestamp, per-collection row counts). On Android, SettingsScreen.kt:152-155 BACKUP section is a stub — ToggleRow('Auto-export every Sunday', true){} and SettingRow('Export now', 'One-shot JSON.'){} both have empty onClick lambdas and do nothing. No last-sync ping, no row counts, no offline/pending indicator anywhere. AppViewModel exposes pendingCount (AppViewModel.kt:70, store.pendingCount() stateIn) but nothing in the UI consumes it, so the user never sees queued/unsynced writes while offline. (Note: a working 'Export everything' DOES exist in the Account section via the CreateDocument launcher + vm.exportJson(), SettingsScreen.kt:171-182 — so the export capability is present, just not wired into the BACKUP card.)
- fix: Wire the BACKUP section: implement 'Export now' by reusing the exportJson + CreateDocument launcher already proven in AccountContent (SettingsScreen.kt:171-182), and render sync status from collected flows — pendingCount, signed-in email, and a persisted last-sync timestamp (store one in SyncCoordinator.syncNow/hydrate in SharedPreferences, analogous to web's unstuck-last-sync-ping). Consider an 'N unsynced' badge in chrome when pendingCount > 0.

### [App shell / nav / overlays /] Calendar pullEvents sends date-only strings (not full ISO) and uses a different window than web
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:96
- VERIFIED. Web calendar-sync.ts defaultPullWindow() (lines 152-157) passes full ISO timestamps via new Date(...).toISOString() as from/to to the calendar-sync/events Edge Function, window -7d..+14d. Android SyncCoordinator.pullCalendar() (SyncCoordinator.kt:95-98) passes LocalDate.now().minusDays(7).toString() / plusDays(30).toString() = bare 'yyyy-MM-dd', window -7d..+30d. Same backend function receives a different value shape; if it forwards from/to as Google timeMin/timeMax (RFC3339 datetime), a date-only string may be rejected or interpreted at UTC midnight, shifting/clipping returned events vs web. (Could not inspect the Edge Function source from this repo to confirm rejection vs tolerance, hence partial/med.)
- fix: Match web: build full ISO-8601 instants (today.minusDays(7).atStartOfDay(ZoneOffset.UTC).toInstant().toString()) for from/to and align the window to -7d..+14d, or confirm the Edge Function tolerates date-only + the wider window.

## LOW (65)

### [Task CRUD + detail (create/e] Sessions list shows only minutes, unsorted, capped at 6; no relative date / estimate / estimate-history calibration block
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:155
- Web TaskDetailPane sorts sessions by completedAt desc (task-detail-pane.tsx:248), shows each with a 'today/yesterday/N days ago' label (howLongAgo:37-45), 'MM:SS in focus' (formatMMSS) and '· est Nm', a 'N attempts' sub, an empty-state ('No sessions yet…' line 245), and a separate 'Estimate history' section ('Estimated N min after K sessions. Calibration improving.' lines 361-381). Android TaskDetailSheet.kt:155-160 renders taskSessions (unsorted) take(6) as bare '• Nm focused' lines, drops the relative date, the est, the attempts count, the empty-state copy (the whole Sessions section is hidden when empty, line 155 `if (taskSessions.isNotEmpty())`), and the entire estimate-history block is absent.
- fix: Sort sessions by completedAt desc, show relative-time + MM:SS + est, always render the section with an empty-state string, and add the estimate-history summary line matching task-detail-pane.tsx:361-381.

### [Task CRUD + detail (create/e] Editing a task's name/first-action loses unsaved keystrokes when a sync update arrives mid-edit
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:208
- EditableText keys both `editing` and `draft` on remember(value) (TaskDetailSheet.kt:208-209). When the field is open for editing and a realtime/sync update changes task.name (cross-device edit, or the user's own updatedAt round-trip after a prior commit), `value` changes, the remember re-initialises, editing flips back to false and draft resets — the in-progress edit is discarded. The web EditableHeadline/EditableFirstAction keep `editing` in independent useState and only re-seed `draft` via useEffect(value) (task-detail-pane.tsx:521, 590), so an external value change does not force-close an open editor.
- fix: Key `editing` on a stable id (remember(task.id) or plain remember) not on `value`; re-seed `draft` from `value` via LaunchedEffect(value) only when NOT editing, so an external value change does not abort an open edit.

### [Task CRUD + detail (create/e] No 'Mark not done' label once a task is completed in the detail view (action works but is undiscoverable)
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:114
- Web detail-pane button text toggles 'Mark done' <-> 'Mark not done' (task-detail-pane.tsx:148-152). Android TaskDetailSheet.kt:114 shows '✓ Done' when task.done; tapping it DOES correctly flip back via vm.toggleDone -> applyCompletion which clears completedAt (TaskMutations.kt:13-22 stampCompletion returns null when !isDone) — so the un-complete logic is correct. But the label '✓ Done' reads as a static completed badge rather than an actionable reopen, so the un-complete action is undiscoverable.
- fix: Label the button 'Mark not done' (or 'Reopen') when task.done, matching the web toggle text, so the reopen action is discoverable.

### [Task CRUD + detail (create/e] Estimate edit on Android lacks the '· learned from N similar' calibration subtext
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:124
- Web EditableEstimate shows '<N> min · learned from <K> similar' tying the estimate to session count (task-detail-pane.tsx:165-172, 917 renders sub). Android Estimate section offers preset chips 15/25/45/60/90 plus a Custom dialog (TaskDetailSheet.kt:124-132) but never surfaces the 'learned from N similar' calibration context, so the learning framing the web presents is missing.
- fix: Add a '· learned from {taskSessions.size} similar' subtext under the Estimate section (and the estimate-history block from the sessions finding) to match web parity.

### [Task CRUD + detail (create/e] Capture body fields are single-line; web uses multi-line textareas
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:259
- Web capture composer (AddCaptureForm textarea rows=2, task-detail-pane.tsx:456) and create-modal draft captures (task-create-modal.tsx:767 textarea rows=2) use multi-line textareas 'for the follow-up question, edit, or idea you don't want to lose'. Android AddCaptureRow sets singleLine = true (TaskDetailSheet.kt:259) and NewTaskSheet draft BasicTextField sets singleLine = true (NewTaskSheet.kt:172), forcing single-line entry. (Note: the WHAT/FIRST STEP fields in NewTaskSheet use OutlinedTextField singleLine=true at lines 116/159, while the web WHAT/FIRST STEP are <input> single-line at task-create-modal.tsx:351/376 — so those two match web; only the capture textareas truly diverge.)
- fix: Drop singleLine = true (or set maxLines > 1) on the capture body fields (TaskDetailSheet.kt:259 and NewTaskSheet.kt:172) to allow multi-line entry like the web textareas.

### [Task CRUD + detail (create/e] TasksScreen has no slip-mode 'AI noticed N tasks aging' review toggle
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TasksScreen.kt:72
- Web TaskListPane renders a footer button that counts slipping tasks (slipCount = tasks.filter(t => isSlipping(t, now)).length, task-list-pane.tsx:74) and toggles slipMode to filter the list to just the slipping ones (lines 224-253). The Android :core already ports isSlipping (VisibleTasks.kt:35) and visibleTasks accepts slipMode (line 57), but TasksScreen.kt:72 hardcodes slipMode = false and renders no slip footer/toggle, so the 'review aging tasks' triage affordance is entirely absent on Android.
- fix: Add a slipMode state + footer button mirroring web: count tasks where isSlipping(t, now), pass slipMode into visibleTasks, and surface the 'AI noticed N tasks aging / Review' toggle.

### [Task CRUD + detail (create/e] Tasks list rows lack the schedule-time chip and the NEXT badge present on web
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TasksScreen.kt:111
- Web ListRow shows a clock + today's scheduled start time (buildScheduleMap, task-list-pane.tsx:25-36, 68, 214; list-row.tsx:132-135) and a coral 'NEXT' badge for the pickStartNext task (task-list-pane.tsx:69-72, 213; list-row.tsx:90-99). Android TasksScreen row (TasksScreen.kt:111-141) shows name, area dot, recurrence glyph (line 121), up to 3 tags (line 125), a Backlog age chip (lines 133-138), and estimate (line 139) — but no scheduled-time chip and no NEXT badge, so the 'what's scheduled today / what to start next' cues are missing from the list. pickStartNext is already ported (PickStartNext.kt) and unused on this screen.
- fix: Build a today-schedule map from blocks (today-dated task blocks, earliest startTime per taskId) and render a clock+time chip per row; compute pickStartNext(tasks, blocks, liveTaskId, activeArea) and render a coral NEXT badge on the matching open task.

### [Tags (vocabulary, filter, pi] Picker has no empty-vocabulary hint
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/components/TagPicker.kt:70-90
- Verified. TagPicker.kt:70-90 renders only the filtered matches and a Create row (the latter only when a non-blank query has no case-insensitive match). With an empty vocabulary and no query typed, the dropdown shows just the search box and nothing else. Web shows 'No tags yet. Type a name above to create one.' when `visibleTags.length === 0 && !queryLower` (tag-picker.tsx:218-222).
- fix: When `matches.isEmpty() && query.isBlank()`, render a hint row 'No tags yet. Type a name above to create one.' mirroring tag-picker.tsx:218-222.

### [Tags (vocabulary, filter, pi] Picker has no Enter-to-toggle / Enter-to-create keyboard behavior
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/components/TagPicker.kt:64-68
- Verified. The BasicTextField at TagPicker.kt:64-68 sets `singleLine = true` but supplies no keyboardOptions/keyboardActions, so the IME Done/Enter key does nothing — the user must always tap a row or the Create chip. Web's input handles Enter: toggles the first visible match and clears the query if matches exist, else creates the typed tag (tag-picker.tsx:194-204). Minor on mobile but a behavioral gap.
- fix: Add keyboardOptions(imeAction = ImeAction.Done) + keyboardActions { onDone = { if (matches.isNotEmpty()) { toggle first + clear query } else if (query.isNotBlank()) { onChange(selected + vm.ensureTag(query.trim())); query = "" } } } mirroring tag-picker.tsx:194-204.

### [Tags (vocabulary, filter, pi] Picker dropdown order is non-deterministic (unsorted), web/Settings use sortOrder
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/components/TagPicker.kt:70
- Verified. TagPicker.kt:70 filters `vocab` straight from vm.tags (the StateFlow off store.tags()), with no sort. store.tags() (LocalStore.kt:50) observes Room via RecordDao.observe whose query is `SELECT * FROM records WHERE tableName = :table` (Daos.kt:13) — no ORDER BY — so order is effectively arbitrary. The Settings panel sorts by sortOrder (SettingsScreen.kt:331) and web renders in useTags insertion order with sortOrder assigned on add (use-tags.ts:94), so the picker order is inconsistent with both.
- fix: Sort before filtering: `vocab.sortedBy { it.sortOrder }.filter { query.isBlank() || it.name.contains(query, ignoreCase = true) }` (matches SettingsScreen.kt:331 and web ordering).

### [Tags (vocabulary, filter, pi] ensureTag returns typed casing instead of the existing tag's canonical name
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:305-311
- Verified. AppViewModel.kt:305-311 ensureTag returns the raw trimmed input `nm` without resolving to an existing tag's stored casing. Web addTag returns the existing TagRow when the name matches case-insensitively (use-tags.ts:86-89) and handleCreate applies `created.name` (tag-picker.tsx:88-90). In practice TagPicker.kt:84 only offers Create when no case-insensitive match exists, so this almost never fires from the picker — but if ensureTag is called directly with a differently-cased existing name it appends the new casing to the task instead of reusing the canonical one. Correctly scoped low/partial.
- fix: Return the existing canonical name when one matches: `tags.value.firstOrNull { it.name.equals(nm, true) }?.name ?: run { if (nm.isNotEmpty()) launchWrite { write?.upsertTag(TagRow(newUuid(), nm, null, tags.value.size)) }; nm }`.

### [Today / Start-Next / Up-Next] Start-next task is hidden from the Today list instead of flagged NEXT
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:98
- CONFIRMED. Web TodayList keeps the recommended task in `visible` and passes `isStartNext={!t.done && t.id === startNextId}` (today-list.tsx:243); StandardTaskRow renders a coral 'NEXT' pill when `isNext` (task-row.tsx:312-322). So the task appears both in the hero and inline with a NEXT badge. Android excludes it: `rows = todayAll.filter { … && it.id != startNext?.id && it.id != liveId }` (TodayScreen.kt:98) and the same for backlogRows (line 101). The Today TaskRow (TodayScreen.kt:241) has no isStartNext param and no NEXT pill. Deliberate de-dupe on mobile but diverges from web and drops the NEXT affordance.
- fix: To match web exactly: keep the start-next task in `rows`/`displayRows` and pass an `isStartNext` flag to TaskRow that renders a coral 'NEXT' pill. If the mobile de-dupe is intentional, confirm it as a product decision (the hero already surfaces the task, so impact is low).

### [Today / Start-Next / Up-Next] No tags / recurrence / firstPhysicalAction meta on Today task rows
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:250
- CONFIRMED. Web StandardTaskRow shows the recurrence ↻ glyph (task-row.tsx:335), up to 2 tag chips via TagChipRow (336-342), a scheduled-time clock (357-361), and a '→ firstPhysicalAction' hint (343-345). Android's Today TaskRow (TodayScreen.kt:250-269) shows only name, area dot + label, optional backlog-age chip, and the estimate — recurrence, tags, schedule time, and the first-action hint are all dropped. Model HAS `tags` (Models.kt:85), `firstPhysicalAction` (91), `recurrence` (95), so the data exists. Note: Android Today doesn't compute a per-task schedule map (web's buildScheduleMap), so the scheduled-time hint would need that added.
- fix: Add a recurrence indicator (when task.recurrence != null), a small tag-chip row (max 2), and the firstPhysicalAction '→ …' hint to the standard Today row. For scheduled time, build a today schedule map from blocks (port buildScheduleMap) if that hint is wanted.

### [Today / Start-Next / Up-Next] Backlog age chip reads "1d" minimum instead of "today"; uses calendar-day diff
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:265
- CONFIRMED. Web StandardTaskRow uses `daysSinceCreated(task, now)` (floored elapsed ms; visible-tasks.ts:149) and labels age 0 → 'today', 1 → '1d', N → 'Nd' (task-row.tsx:254-255). Android passes `ageDays(t.createdAt, now)` (TodayScreen.kt:164) which is a CALENDAR-day diff (Common.kt:39-42, `startOfDay(now) - startOfDay(created)`), then renders `${ageDays.coerceAtLeast(1)}d` (TodayScreen.kt:265) — so it can never show 'today' and always shows ≥'1d'. Note Android DOES have a `daysSinceCreated` port in :core (VisibleTasks.kt:44, floored elapsed) that matches web exactly, but the Today UI uses the calendar-day `ageDays` helper instead. Functionally fine since Backlog excludes today-created tasks, but label semantics differ.
- fix: For full parity, use the existing core `daysSinceCreated(t, now)` (VisibleTasks.kt:44) and a 'today'/'1d'/'Nd' label rule, dropping the `coerceAtLeast(1)`. Or keep as-is (acceptable given Backlog excludes today's tasks).

### [Today / Start-Next / Up-Next] Backlog pill shows no triage count
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:146
- CONFIRMED. Web BacklogPill shows a tabular '· N' count when count > 0 (today-list.tsx:368-409, especially 401-407), driven by `backlogCount` (137-142). Android's Backlog toggle (TodayScreen.kt:141-148) shows just the word 'Backlog' (line 146) with no count. `backlogRows` is already computed at TodayScreen.kt:100-101, so the count is available.
- fix: Append a '· ${backlogRows.size}' count to the Backlog pill text when > 0, using the already-computed `backlogRows` (line 100).

### [Today / Start-Next / Up-Next] Empty-state copy is not context-aware like web
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:104
- CONFIRMED. Web TodayList renders a per-view inline empty message in three cases (today-list.tsx:221-237): backlog-active → 'Backlog is clear…'; backlogCount>0 → 'Nothing scheduled today. Tap Backlog to see what to plan.'; else → 'Nothing scheduled today. Add a task to get going.' Android's EmptyHero only fires on the GLOBAL empty `todayAll.isEmpty() && live==null && backlogRows.isEmpty()` (TodayScreen.kt:104, used at 132) and shows one generic 'You're all clear' hero (273-287). There is no inline empty row when, e.g., Today is empty but backlog has items, or when the backlog view itself is empty — Android just shows the pills with no rows below.
- fix: When `displayRows` is empty but the screen isn't globally empty, render an inline dashed empty message matching web's three cases (backlogActive vs backlogRows.isNotEmpty() vs add-a-task).

### [Today / Start-Next / Up-Next] Filter pills don't show usage-ranking and have no overflow/More menu
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:150
- CONFIRMED. Web ranks areas by open-task count via rankAreas (today-list.tsx:47-58) and collapses excess past MAX_INLINE=6 into a 'More ▾' OverflowMenu (60-72, 210-217, 260-360), keeping the active area inline (splitInlineOverflow swap, 64-70); the overflow menu shows each area's color dot. Android renders every area inline in a horizontalScroll in the raw `areas` order (TodayScreen.kt:150, `areas.forEach { a -> FilterPill(...) }`) — no usage ranking, no overflow menu. The design FilterPill already accepts `dotColor` (Components.kt:103) and Android passes it (TodayScreen.kt:150), so the dot-color claim in the original finding title was wrong — corrected: dots ARE shown; what's missing is the usage-ranking and overflow. Lower impact on mobile due to horizontal scroll.
- fix: Port rankAreas (rank by open-task count, then name) and order the pills accordingly so most-used areas come first, matching web. An overflow 'More' menu is optional on mobile given the horizontal scroller.

### [Focus: treatments, pause-rea] Capture rail not sorted newest-first and capped at 3 instead of 5
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:206
- Web cockpit shows captures for the task sorted by `at` descending, sliced to 5 (cockpit.tsx:66-71). Android CapturesRail does `captures.filter{it.taskId==task.id}.takeLast(3)` (FocusScreen.kt:206). The captures Flow comes from LocalStore.captures() = observe(Tables.CAPTURES) whose Room query is `SELECT * FROM records WHERE tableName = :table` with NO ORDER BY (Daos.kt:13-14), so row order is not guaranteed to be by `at`; the 3 shown may not be the newest, and the cap differs from web's 5.
- fix: Sort explicitly by `at` descending and take 5: `captures.filter{it.taskId==task.id}.sortedByDescending{it.at}.take(5)`.

### [Focus: treatments, pause-rea] Promote-to-task action missing from in-focus capture list
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:208-211
- Web cockpit renders each capture with a 'Promote to task →' action calling promoteCapture (cockpit.tsx:356-370). Android CapturesRail only prints '• body' text (FocusScreen.kt:210) with no per-capture action; vm.promoteCapture(capture) exists (AppViewModel.kt:261-264) but is unreachable from focus.
- fix: Add a 'Promote to task' affordance on each capture row calling vm.promoteCapture(capture), matching the cockpit rail.

### [Focus: treatments, pause-rea] Capture sessionId binding lacks the live-running guard
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:163
- Android passes the live session id straight through: FocusScreen.kt:163 `CaptureSheet(vm, task, live?.id)` → saveCapture(taskId, sessionId=live?.id, …) (AppViewModel.kt:245-248). The web equivalent (capture-modal/finishSession reuses live.id only while a real session is active). When the live session id lingers but the session has ended/cleared, a capture could attach to a non-running session id. Note: the practical exposure is small here since FocusScreen unmounts/clears on finishFocig, but the guard web applies is absent.
- fix: Pass sessionId only when the session is actually live: `if (live?.sessionStart != null) live?.id else null`.

### [Focus: treatments, pause-rea] ReflectSheet selection is never persisted (mockup-only, no data flow)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/ReflectSheet.kt:48-55
- ReflectSheet ('How did that land?' flow/okay/sticky/stopped) has no web counterpart, and its selection `sel` (ReflectSheet.kt:34,48) is written nowhere — both Skip and Save call onDismiss() only (lines 50-53). It is shown after every finish (FocusScreen.kt:147,158,164). Not a web-parity gap (web has no reflection survey), but if intended to feed analytics it captures nothing.
- fix: Either remove the sheet to match web, or persist the mood (e.g. a ReasonLog or a new field) on Save. Until then it should not be presented as a working capability.

### [Calendar: day grid (24h/now/] Week Busiest/Lightest both resolve to Monday on an empty/flat week
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:124-125
- Verified. Android computes busiest = days.getOrNull(plannedByDay.indexOf(plannedByDay.maxOrNull() ?: 0)) and lightest = days.getOrNull(plannedByDay.indexOf(plannedByDay.minOrNull() ?: 0)) (CalendarScreen.kt:124-125). When the week is empty every entry is 0, so both max and min are 0 and indexOf(0) returns 0 — Monday is labeled both Busiest and Lightest. Web instead reports overload 'none' (week-full.tsx:222,329) and picks a genuine lightest by sort (223,337), never showing a misleading busiest. Note: the cited fix mentions a '—' fallback, but Android already only shows '—' when days.getOrNull is null (never, for 0..6), so the guard must be on totalPlanned==0, not on null.
- fix: Guard on totalPlanned: if totalPlanned == 0 show '—' for busiest. Better: replace 'busiest' with web's overload-risk semantics (the day whose planned exceeds usable capacity, else 'none').

### [Calendar: day grid (24h/now/] Day grid lacks a 'Today' jump button and the 'h scheduled' summary present on web
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:116-124
- Verified. Web's day header has Prev / Today / Next (today-timeline.tsx:261-265) plus a live 'Nh scheduled' total (266-270, summing todayBlocks durations /60). Android's DayGrid header has only ‹ prev / label / › next (DayGrid.kt:121-123) — no Today jump (after paging away the user must tap back repeatedly) and no scheduled-hours summary. Note: the Month view DOES have a Today button (CalendarScreen.kt:208); only the Day grid lacks it.
- fix: Add a 'Today' control that sets date = Clock.todayIso() and a 'Nh scheduled' label summing dayBlocks.sumOf{it.durationMinutes}/60.

### [Calendar: day grid (24h/now/] Calendar tray excludes 'saved-for-later' tasks while web does not
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:98
- Verified. Web's calendar unscheduled tray filter is purely `!t.done && !blocks.some(b => b.taskId === t.id)` (unscheduled-tray.tsx:31-33; week-full.tsx:354-356) — no exclusion of later/parked tasks. Android adds `it.later != true` (DayGrid.kt:98), so later-saved tasks cannot be dragged onto the calendar on Android even though they can on web. (Flagging as a parity gap; confirm against product intent before changing, since 'later' semantics may be deliberate on mobile.)
- fix: Drop the `it.later != true` clause to match web's `!done && not already blocked` filter, or confirm product intent.

### [Calendar: day grid (24h/now/] Day grid does not visually distinguish the live/active block; no done strikethrough, end-time, or area-dot on blocks
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:144-167
- Verified. Web classify() flags the block tied to the live focus session as 'active' with a stronger tint (bgFor uses 26% vs 14% mix, today-timeline.tsx:86-87) and the week view adds a coral ring (week-full.tsx:592), shows the life-area dot (today-timeline.tsx:400), start time (411-416), ellipsized name, and struck-through 0.55-opacity when the linked task is done (397,409; week-full.tsx:595,606). Android DayGrid colors blocks by area/external/placeholder (DayGrid.kt:151-156) but does NOT read vm.liveSession to highlight the running block, does not strike-through done tasks, and shows only the name (DayGrid.kt:166) — no time, no area dot.
- fix: Collect vm.liveSession and, when liveSession.taskId == b.taskId, render the block with a stronger tint + coral border; apply line-through + 0.55 opacity when the linked task.done; consider showing start time + area dot to match web block content.

### [Calendar: day grid (24h/now/] No conflict (overlap) warning anywhere in the Android scheduling/edit flow
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:235-263
- Verified. Web warns on overlap in the block edit modal (cal-block-edit-modal.tsx:92-95 findConflicts, banner 369-391) and the block-time modal (block-time-modal.tsx:58-61,206-228), listing overlapping blocks via blockTimeRange. Android has findConflicts + blockTimeRange ported (FreeSlots.kt:123-149) and NewTaskSheet imports findConflicts (NewTaskSheet.kt:40), but the CalBlockEditSheet (DayGrid.kt:235-263) never calls it — rescheduling onto an occupied slot via the start-time chips gives no warning.
- fix: In the edit sheet compute findConflicts(live.date, live.startTime, live.durationMinutes, blocks, live.id) and render an amber banner listing overlaps (blockTimeRange + taskName), matching the web modal.

### [Google Calendar connect/pull] Multiple connections collapsed to one row; bulk disconnect-all; can't add a second account
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:92,104,106
- VERIFIED. Web lists every connected account individually, each with its own Disconnect button (sync-flow.tsx:449-484 connections.map -> per-row displayName/accountEmail·provider + per-row onDisconnect(c.id)) and supports connecting more than one account. Android CalendarSyncBar shows only conns.first().accountEmail (CalendarScreen.kt:104) and a single 'Disconnect' that loops over ALL connections (CalendarScreen.kt:106 conns.forEach { vm.disconnectCalendar(it.id) }). With 2+ accounts the user can't see or selectively disconnect them. Also the connect affordance is hidden entirely once any connection exists (CalendarScreen.kt:92 `if (conns.isEmpty())` else branch has no connect button), so a second account can't be added.
- fix: Render one row per connection (email + provider + per-row Disconnect) and keep a 'Connect another calendar' affordance visible, matching the connected-accounts list in sync-flow.tsx:449-484.

### [Google Calendar connect/pull] Pull window differs from web (-7/+30d vs web default -7/+14d); date-only boundary
- partial | sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:95-97
- VERIFIED. Android pullCalendar uses today-7 .. today+30 built from LocalDate.now().toString() (SyncCoordinator.kt:95-97 — date-only, midnight local). Web defaultPullWindow is -7/+14d built from now±ms then toISOString() (calendar-sync.ts:153-157), plus on-demand visible-range pulls (month-full.tsx). Not a correctness bug, but the reconciliation set differs (Android keeps/deletes EXTERNAL blocks across a wider, date-aligned window vs web's narrower ms-precise window), and boundary-day events could differ by a few hours. Low impact.
- fix: Align the default window to -7/+14d to match web's defaultPullWindow (or make it explicit/configurable), and optionally support pulling the visible grid range as web does.

### [Google Calendar connect/pull] CSRF state lost across process death silently disables the state check (fails open)
- bug | sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:71,76,82-83
- VERIFIED. beginGoogleConnect stores the server-issued state in an in-memory field pendingCalState (SyncCoordinator.kt:71,76). If Android kills the app while the Custom Tab is foreground (common under memory pressure), pendingCalState is null on return; completeGoogleConnect's guard `if (expected != null && expected != state) return false` (SyncCoordinator.kt:82-83) treats null-expected as pass, so the client-side CSRF binding is skipped (fails open). Web persists state in sessionStorage and treats missing/mismatched state as an error ('That Google connect link expired — please click Connect again.', sync-flow.tsx:120-125, fails closed). The server also HMAC-verifies state (state passed to /connect), so real impact is limited, but client behavior diverges.
- fix: Persist pendingCalState in SharedPreferences when beginGoogleConnect issues it, and on completion require a stored value that matches (fail closed like web), clearing it after use.

### [Collections CRUD (new/rename] Collection subtitle is never displayed on Android (card or detail)
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionDetailScreen.kt:89
- Web shows the collection's serif-italic subtitle under the name on both the card (collection-card.tsx lines 72-84) and the detail header (collection-detail.tsx lines 176-180). The Android ItemCollection model carries `subtitle: String? = null` (Models.kt line 180) and the codec round-trips it (DbRowCodec.kt lines 153/157/158: CollectionRow.subtitle, `c.subtitle ?: ''` on encode, `subtitle?.ifEmpty { null }` on decode). But neither CollectionsScreen card (lines 85-101) nor CollectionDetailScreen header (lines 89-97) renders subtitle anywhere — the only `subtitle` usage in app/ is the unrelated ScreenHeader in Common.kt. Demo/server collections with subtitles lose that framing line on Android. Verified across all four files.
- fix: Render `col.subtitle` (when non-blank) in serif-italic ink-3 under the name in the detail header (after line 96), and under the name on the overview card (after line 95), matching web styling.

### [Collections CRUD (new/rename] Card omits the '+N more' overflow indicator; detail header omits the item count
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:96
- Web card shows '+ {n - previews} more' when there are more items than previews (collection-card.tsx lines 113-117) and the detail header shows '{n} item{s}' (collection-detail.tsx lines 182-189). Android card shows only the raw count chip top-right (CollectionsScreen.kt line 93) plus 2 preview lines with no overflow hint. The Android detail header (CollectionDetailScreen.kt lines 89-97) shows no item-count text at all — note the count DOES appear inside the delete-confirm dialog body (line 147 'its N item(s)') but not in the header. Verified against both files.
- fix: Add a '+N more' Text below the previews when col.items.size > previewCount, and add a '{n} items' count to the detail header to match web.

### [Collections CRUD (new/rename] Rename draft can be clobbered mid-edit by a realtime echo
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionDetailScreen.kt:68
- Android keys the title draft on the collection name: `var titleDraft by remember(col?.name) { mutableStateOf(col?.name ?: "") }` (CollectionDetailScreen.kt line 68). If a realtime/hydrate echo updates col.name while the user is mid-rename (editingTitle == true), the remember key changes and titleDraft is reset to the incoming name, discarding in-progress keystrokes. Web avoids this: nameDraft is independent state synced via `useEffect(() => setNameDraft(collection.name), [collection.name])` (collection-detail.tsx line 66), but commitRename compares against the live name and the input is not destroyed/re-seeded by a key change. Verified: line 68 keying and line 95 (`titleDraft = col.name; editingTitle = true`) confirmed. Severity low because the realtime-echo-during-active-rename window is narrow and the whole-row mutex (AppViewModel.kt 276-281) reduces churn.
- fix: Key titleDraft on collectionId (stable) instead of col?.name, and seed it from col.name only when entering edit mode (already done at line 95). Optionally only resync from col.name when !editingTitle.

### [Collections CRUD (new/rename] Pin/done/add concurrency path is sound — no defect (noted for completeness)
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:276
- Verified: Android toggleCollectionItemPin/Done correctly flip Boolean? with `!(it.pinned ?: false)` / `!(it.done ?: false)` (AppViewModel.kt lines 288-291), matching web's `!i.pinned` / `!i.done` (use-collections.ts lines 213/221). The detail splits pinned vs rest in insertion order (CollectionDetailScreen.kt lines 75-76), matching web (collection-detail.tsx lines 77-78). The whole-row mutex re-resolve in mutateCollection (AppViewModel.kt lines 276-281: serialized + re-reads latest from store before upsert) correctly mirrors web's functional-update guard (use-collections.ts writeAndSync lines 140-150) against stale JSONB whole-row upserts. No change required.
- fix: No change required; included so the pin/done/add concurrency path is not mistaken for a defect.

### [Insights / Analytics (report] Report 'Re-entries' card sub-line shows raw capture count instead of sessions-with-a-capture
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:99
- Web's RE-ENTRIES Observation delta is reentryWithNotes = number of sessions that had a capture attached (report.tsx:317 sessions.filter(s => captures.some(c => c.sessionId === s.id)); rendered at 343 as '{n} had a capture attached'). Android's StatCard caption is '${captures.size} captures' (InsightsScreen.kt:99) — the total capture count, not sessions-with-capture. Different number and different meaning. (The card's headline value '${sessions.size}' matches web's reentryCount, only the sub-line diverges.)
- fix: Compute reentryWithNotes = sessions.count { s -> captures.any { it.sessionId == s.id } } and use it in the caption (e.g. "$reentryWithNotes had a capture attached").

### [Insights / Analytics (report] Slip-detector rows omit the per-task area dot the web shows
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:164
- Web SlipDetector (deep-dive.tsx:278-324) renders an AreaDot for each slipping task (color resolved via tasksByName mapping name->lifeArea, built at 419 and read at 295/311) inside a dashed amber-bordered row. Android slip rows (InsightsScreen.kt:161-173) show only the task name and '{moveCount}× · {weeks}w' with no area dot, so the area context is lost.
- fix: Resolve each slip row's task lifeArea (build a name->lifeArea map from tasks) and prepend an area dot (areaColor swatch) to the row, matching deep-dive.tsx.

### [Insights / Analytics (report] Stacked bars omit the per-day hours total label shown above each bar on web
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:199
- Web StackedBars prints the day's total hours as '{total.toFixed(1)}h' (or '·' when zero) above each column (report.tsx:124). Android StackedBars (InsightsScreen.kt:199-209) renders only the day label (line 201) and the bar, dropping the numeric total, so users can't read how many hours a day actually held.
- fix: Add a small mono label per row showing the row's summed hours (data.sum()) as e.g. "%.1fh" or '·' when zero, matching report.tsx:124.

### [Insights / Analytics (report] Median session computed by integer-dividing seconds to minutes BEFORE sorting, vs web sort-then-round
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:121
- Web computes the median over actualSec (sorted ascending), then converts: Math.round(medianSec/60) (deep-dive.tsx:425-430,442). Android does sessions.map { it.actualSec / 60 } (integer truncation to whole minutes) THEN .sorted() THEN takes mins[mins.size/2] (InsightsScreen.kt:121-122). Both use index size/2 (upper-median for even counts) which matches, but the early integer-truncate + lack of rounding can make Android's median ~1 minute lower than web's for the same data (and in edge cases the truncation can reorder ties).
- fix: Sort actualSec values, take arr[size/2], then display Math.round(medianSec/60.0): val secs = sessions.map { it.actualSec }.sorted(); val medianSec = if (secs.isEmpty()) 0 else secs[secs.size/2]; show "${(medianSec/60.0).roundToInt()}m".

### [Insights / Analytics (report] 'Captures by kind' always renders five zero-bars even with no captures; web shows a single placeholder band
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:151
- captureBreakdown returns a LinkedHashMap pre-seeded with all five CaptureTag keys at 0 (Analytics.kt:173-180), so breakdown.isNotEmpty() (InsightsScreen.kt:151) is ALWAYS true. With no captures the Android panel shows five labeled bars all at the 2% floor width. Web's CaptureFlow filters out zero-count tags (deep-dive.tsx:344 .filter(e => e.count > 0)) and, when none remain, pushes one 'No captures yet' placeholder band (348).
- fix: Filter the breakdown to entries with n>0 before rendering; if the filtered list is empty, render a single 'No captures yet' placeholder bar instead of five zero bars.

### [Command palette + quick capt] Capture body is not truncated, differs from web's 80-char cap
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:60
- Web caps the capture label at 80 chars: c.body.slice(0, 80) (command-palette.tsx:80). Android passes the full cap.body as the Result title (CommandPalette.kt:60). The row Text uses maxLines=1 so visually it still clips, so the user-visible impact is minor — but the underlying value differs from web. Severity is low (display-only, already truncated by maxLines).
- fix: Use `cap.body.take(80)` as the Result title to match web's slice(0, 80).

### [Command palette + quick capt] Capture tag label shows enum name (e.g. 'follow_up') instead of web's 'follow-up'
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:60
- Web shows the raw serialized tag value as the capture sub: sub: c.tag (command-palette.tsx:81), where CaptureTag values are the hyphenated strings ('follow-up' | 'idea' | 'edit' | 'question' | 'distraction'). Android uses `cap.tag.name.lowercase()` (CommandPalette.kt:60); for FOLLOW_UP that yields 'follow_up' (underscore). The Kotlin CaptureTag enum has @SerialName("follow-up") (Enums.kt:50) so the serialized value is correct, but .name ignores the annotation. No existing CaptureTag label/display helper was found in :core.
- fix: Add a CaptureTag display helper that returns the serialized/hyphenated value (or map FOLLOW_UP -> 'follow-up') and use it instead of `cap.tag.name.lowercase()`.

### [Command palette + quick capt] Empty-query state shows results instead of web's guidance hint, and no 'No matches' feedback
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:71-83
- Web renders an explicit hint when results are empty: 'Type to search across tasks, captures, areas, and routes.' on empty query, and 'No matches.' when a query matches nothing (command-palette.tsx:191-200). Android has no empty-state copy: with an empty query it just lists up to 8 tasks + 5 actions, and when a query genuinely matches nothing it shows a blank LazyColumn with no feedback (CommandPalette.kt:71-83).
- fix: When results.isEmpty(), render a centered Text: `if (query.isEmpty()) "Type to search across tasks, captures, areas, and routes." else "No matches."`, matching web's states.

### [Command palette + quick capt] Task sub-label uses '—' instead of web's 'Unassigned' for area-less tasks
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:58
- Web shows the task's area or the literal 'Unassigned' when none: sub: t.lifeArea ?? 'Unassigned' (command-palette.tsx:72). Android shows '—': t.lifeArea ?: "—" (CommandPalette.kt:58). Minor wording divergence in the meta line.
- fix: Use `t.lifeArea ?: "Unassigned"` to match web.

### [Command palette + quick capt] Capture/task candidate pool differs (fixed per-bucket take()s vs web's global top-12)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/palette/CommandPalette.kt:57-61
- Web considers up to 50 captures as candidates (command-palette.tsx:76, captures.slice(0, 50)) plus all tasks/areas/routes, then ranks the global top 12 (lines 105-113). Android filters captures (no cap, only when q is non-empty), takes 4 notes and 8 tasks via fixed buckets (CommandPalette.kt:57-59). Because of the per-bucket take()s plus lack of scoring, a strong capture/task match beyond those bucket limits is excluded even though web would surface it in the top 12. This is essentially the same root cause as the ranking finding; tracking the cap/limit aspect separately.
- fix: After porting global scoring, drop the per-bucket take()s, score the combined candidate set, and take(12) overall (optionally pre-limiting captures to take(50) as web does).

### [Settings & preferences (ever] Tag usage count includes done tasks (web counts only open)
- bug | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:332
- Web usageCount filters !t.done before counting (tag-panel.tsx:17-18: tasks.filter(t => !t.done && (t.tags ?? []).includes(tagName)).length). Android counts every task with the tag regardless of done state (SettingsScreen.kt:332: tasks.count { it.tags?.contains(tag.name) == true }). So a tag attached only to completed tasks shows nonzero on Android but 0 on web. Verified the area open-count IS correctly gated with !it.done on Android (SettingsScreen.kt:285), so only tags diverge.
- fix: Change to tasks.count { !it.done && it.tags?.contains(tag.name) == true } to match web.

### [Settings & preferences (ever] Focus pause-reasons default differs from web (false vs true)
- partial | app/src/main/kotlin/tech/csalliance/unstuck/SettingsStore.kt:28
- Web default for PREF_FOCUS_PAUSE_REASONS is true (settings-panel.tsx:57 useLocalStorageBool(..., true)). Android SettingsState.focusPauseReasons defaults false (SettingsStore.kt:28 data-class default and :62 load() getBoolean fallback). So a fresh Android user is NOT prompted for pause reasons on first pause, while a fresh web user is. The feature itself works once enabled (FocusScreen.kt:144 gates the PauseReasons sheet on settings.focusPauseReasons) — only the default diverges.
- fix: Change focusPauseReasons default to true in both the data class default (line 28) and the load() getBoolean fallback (line 62) to match web.

### [Settings & preferences (ever] Add-area color is auto-assigned with no picker; palette tokens differ from web
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:281
- Web add-area form lets the user pick a color (life-area-panel.tsx:356 useState color, palette = COLOR_TOKENS, area-dot.tsx:40) and rows expose recolor over all 8 tokens. Android auto-assigns palette[areas.size % palette.size] from a 6-token list 'indigo,coral,violet,green,amber,blue' (SettingsScreen.kt:281,316) — missing teal and red — with no user choice. Worse, even if 'teal' were added, Theme.areaColor() (Theme.kt:38-47) has no 'teal' branch so it would render gray (ink4); 'red' is handled. Combined with the missing recolor (see life-areas finding), the user can never choose or change an area color.
- fix: Expand the palette to web's 8 tokens (add teal, red), add a 'teal' branch to UnstuckColors.areaColor() in Theme.kt, and add a color picker to the add-area UI plus recolor in the dot menu.

### [Account / Auth (sign in/up/m] Provider-aware password copy from the web is not reproduced
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:181,199
- Confirmed. Web PasswordRow (password-row.tsx:42-54) tailors the sub-label from the user's other identity providers — e.g. 'You sign in with Google. Add a password to also use email + password.' and after-success copy 'Password added. You can now sign in with email + password too.' (password-row.tsx:90). Android shows a fixed sub 'Update your sign-in password' (SettingsScreen.kt:181) and a generic 'Password updated.' toast (SettingsScreen.kt:199), and the Google-only → add-password story is only implied by the row title flipping to 'Add a password' (driven by hasPassword, which checks for an 'email' provider in AuthService.kt:76-77). The provider list isn't exposed on the VM, so no provider-aware copy.
- fix: Expose the provider list (currentUserOrNull()?.identities?.map{it.provider}) on the VM and build the same provider-aware sub-label and success copy as password-row.tsx.

### [Account / Auth (sign in/up/m] Delete-modal lacks busy/disabled state and in-dialog error surface
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:204-224
- Partially confirmed; original title overstated. The empty-email guard is fine and equivalent to web: Android requires `email.isNotBlank() && typed.trim().equals(email, ignoreCase=true)` (SettingsScreen.kt:217) vs web's trimmedEmail.length>0 && match (delete-account-modal.tsx:43-47), so an account with no email can't be deleted by typing nothing — the 'confirm enabled when no email' claim is FALSE. The real, smaller gap: Android closes the dialog immediately (`showDelete = false`) and only THEN runs deleteAccount, surfacing failures via the page-level msg outside the dialog (SettingsScreen.kt:218); there's no busy/'Deleting…' state and no in-dialog error. Web keeps the modal open, shows a dedicated error panel, and renders a 'Deleting…' busy label (delete-account-modal.tsx:49-81,157-177).
- fix: Keep the dialog open during the async call, add a busy/disabled confirm state ('Deleting…'), and surface failures inside the dialog (matching the web modal) instead of closing before the result returns.

### [Onboarding (struggles -> pre] No Back navigation between steps (web has Back on every step)
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:121-125
- Web Footer provides a Back button on steps 2-4 (flow.tsx:435-453, back() at :102) plus 'edit the action' from the session preview (flow.tsx:412-424), and ↵ advances by default (flow.tsx:39-51). Android only offers 'Skip' and a forward Continue/Begin button (OnboardingScreen.kt:121-125); `step` can only increment (step++ at :124), never decrement — no way to go back and fix a mis-picked area/task/treatment. Severity lowered to low: the Android flow is short (4 steps) with all selections still mutable later in Settings.
- fix: Add a Back affordance (and/or wire system back) that decrements step (coerced at 0) on steps > 0, matching web's per-step Back.

### [Onboarding (struggles -> pre] Skip discards in-progress picks and still triggers default seeding
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/onboarding/OnboardingScreen.kt:122
- Web has no 'Skip' — every user goes through the flow and finish() always runs. Android adds a Skip that calls vm.completeOnboarding(emptyList()) + onDone (OnboardingScreen.kt:122), throwing away any areas/task already picked but still seeding the 4 default areas (AppViewModel.kt:331-336), creating no first task, and starting no focus session. Behavioral divergence from web's guaranteed first-task + first-session outcome.
- fix: If keeping Skip, persist whatever the user already picked (or remove it to match web). At minimum ensure it doesn't leave the user with zero tasks and an inconsistent/duplicated area set.

### [Recurrence (editor incl. unt] Weekly day chips ordered Sunday-first; web orders Monday-first
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/RecurrenceEditor.kt:15-46
- Web DOW_OPTIONS renders M,T,W,T,F,S,S with values 1,2,3,4,5,6,0 — Monday-first (task-create-modal.tsx:57-65; RepeatEditor task-detail-pane.tsx:710-718). Android DOW = listOf('S','M','T','W','T','F','S') is Sunday-first with idx==JS-day (RecurrenceEditor.kt:15), iterated via forEachIndexed using idx as the emitted day value (lines 40-43). The emitted day values stay correct/compatible, so this is purely cosmetic chip ordering that differs from web and can confuse cross-platform users.
- fix: Iterate a Monday-first list of (value,label) pairs [(1,'M'),(2,'T'),(3,'W'),(4,'T'),(5,'F'),(6,'S'),(0,'S')] keeping each chip's emitted value = its JS day index, instead of forEachIndexed over a Sun-first label array.

### [Recurrence (editor incl. unt] setRecurrence anchor fallback matches web; the only real gap is until-loss (already covered) — keep as a verification note
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:135-150
- AppViewModel.setRecurrence anchors regeneration on the task's earliest existing task-block, falling back to today@09:00 when none exists (AppViewModel.kt:139-144). Web's RepeatEditor onSave uses the identical earliest-block-or-now / 09:00 fallback (task-detail-pane.tsx:217-224), so the anchor logic itself is at PARITY — no standalone defect here. The materialize/regen :core also correctly honours until (Recurrence.kt:43-47, 77-78). The only behavioural divergence on this path is that the editor cannot pass until, so a regen for a previously-bounded task extends 56 days — which is fully a consequence of findings 1 and 2, not a separate bug.
- fix: No change needed here beyond the editor until-fix. Add a regression test: edit a {weekly, until:<near-past-or-soon>} task via the new until-aware editor and assert regenerateForTask upserts no blocks beyond until.

### [Captures & reason logs (crea] In-focus capture rail shows wrong order and count vs web (oldest tail, not newest; 3 not 5)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:206
- Web cockpit shows captures NEWEST-FIRST and up to 5: allCaptures.filter(c=>c.taskId===task.id).sort((a,b)=>b.at.localeCompare(a.at)).slice(0,5) (cockpit.tsx:66-71). Android uses captures.filter { it.taskId == task.id }.takeLast(3) (FocusScreen.kt:206): no sort (relies on store flow order) and capped at 3. takeLast returns the tail of an unsorted list, so the newest capture made during the session can be absent and ordering diverges from web.
- fix: Use captures.filter { it.taskId == task.id }.sortedByDescending { it.at }.take(5) to match the web sort+limit.

### [Captures & reason logs (crea] promoteCapture does not re-attach a loose (unlinked) capture to the new task
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:261
- Web promoteCapture re-upserts the source capture with taskId: capture.taskId ?? newTaskId (capture-actions.ts:41-44), so a capture with no taskId becomes linked to the spawned task. Android promoteCapture (AppViewModel.kt:261-264) only calls addTask and never touches the capture, so a loose capture stays orphaned (taskId stays null) after promotion. Latent today because Android exposes promote only from TaskDetailSheet, where every capture already has taskId == task.id; it becomes a real divergence the moment promote is wired into the focus rail (prior finding) or any loose-capture surface.
- fix: After addTask, if capture.taskId == null re-upsert the capture linked to the new task: write?.upsertCapture(capture.copy(taskId = capture.taskId ?: newTask.id)). Keep never deleting the capture (preservation), matching capture-actions.ts.

### [Captures & reason logs (crea] Promoted-task name not clamped to 160 chars nor given an 'Untitled task' fallback
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:263
- Web seeds the promoted task name as capture.body.slice(0,160) || 'Untitled task' (capture-actions.ts:23). Android passes the full body unmodified with no empty fallback: addTask(name = capture.body, ...) (AppViewModel.kt:263) — note addTask trims via name.trim() (AppViewModel.kt:106) but does not clamp length or substitute a default. tasks.name is unbounded text so there is no DB error; this is a UX divergence — very long captures yield very long task names and a blank body would create an empty-named task (largely unreachable since promote is only offered on non-empty captures).
- fix: Mirror the web: name = capture.body.take(160).ifBlank { "Untitled task" }.

### [Captures & reason logs (crea] AddCaptureRow does not reset the selected tag after adding
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:266
- Web AddCaptureForm resets BOTH body and tag after submit (task-detail-pane.tsx:433-434: setBody(''); setTag('follow-up')). Android AddCaptureRow only clears body (TaskDetailSheet.kt:266: onAdd(tag, body.trim()); body = "") and leaves tag at the previously chosen value, so the next capture silently inherits the prior tag instead of re-defaulting to follow-up.
- fix: In the Add onClick also reset the tag: onAdd(tag, body.trim()); body = ""; tag = CaptureTag.FOLLOW_UP.

### [Captures & reason logs (crea] No empty-state hint for captures (in-focus rail and task detail)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:162
- Web shows discoverability cues when there are no captures: the cockpit rail renders 'Hit C to capture a thought without losing focus.' (cockpit.tsx:311-321) and the task-detail capture section shows 'No captures attached. Add one below or hit C in /focus.' (task-detail-pane.tsx:282-291). Android CapturesRail returns early and renders nothing when empty (FocusScreen.kt:207: if (recent.isEmpty()) return), and TaskDetailSheet renders nothing between the 'Captures' SectionLabel and AddCaptureRow when taskCaptures is empty (TaskDetailSheet.kt:162-164), so both cues are missing.
- fix: When taskCaptures.isEmpty(), render a muted placeholder card ('No captures attached. Add one below.') before AddCaptureRow; in CapturesRail show the 'Hit Capture…' hint instead of returning early at FocusScreen.kt:207.

### [Life areas + area filter (CR] areaColor token resolver carries phantom name-based mappings
- partial | design/src/main/kotlin/tech/csalliance/unstuck/design/theme/Theme.kt:40,43
- Web resolveAreaColor (area-dot.tsx:47-51) resolves color strictly from a token or the seeded canonical area NAME (Work/Personal/…); there is no fuzzy mapping for arbitrary names. Android UnstuckColors.areaColor (Theme.kt:38-47) hardcodes non-token aliases: 'rethink','test' -> coral (line 40) and 'new feature','bug' -> green (line 43). These are not color tokens and don't correspond to any web behavior; a tag/collection/area literally named 'test' or 'bug' would mis-resolve to coral/green instead of the gray fallback web gives. Harmless for the seeded set, but a latent surprise.
- fix: Drop the non-token aliases ('rethink','test','new feature','bug'); keep only the real tokens (indigo/coral/violet/blue/green/amber/red + a new teal) so resolution matches web resolveAreaColor exactly.

### [Life areas + area filter (CR] Tasks-tab area filter is not reset on fresh tab entry (web resets it)
- partial | app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:111
- Web treats clicking the 'Tasks' rail entry as a fresh entry that drops the ?area= query (left-rail.tsx:96-99 + 166: goto('/tasks') pushes a path with no query, so useSearchParams re-reads empty and the filter clears). On Android, activeArea is MainScaffold state (MainScaffold.kt:70) and BottomNavBar onSelect is { tab = it; stack.clear() } (MainScaffold.kt:111) — it never clears activeArea. So leaving Tasks filtered to e.g. Work and tapping Tasks again lands you still filtered to Work. Note: Today is already area-agnostic (TasksScreen.kt:72 forces activeArea=null for the TODAY view and TodayScreen takes no area), so this is specifically the Tasks-tab persisted-filter divergence.
- fix: In BottomNavBar onSelect, reset activeArea=null on tab switch (or at least when (re)selecting the 'tasks' tab) to match web fresh-entry semantics: onSelect = { tab = it; stack.clear(); activeArea = null }.

### [Life areas + area filter (CR] Signed-out / first-run users get no demo area seed outside onboarding
- missing | app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:330-342
- Web seeds the local demo set (Work/Personal/Volunteering/Home/Health via MOCK_LIFE_AREAS + COLOR_FOR) for signed-out users when localStorage is empty AND auth has resolved (use-life-areas.ts:118-141 seed()/useEffect), so the marketing/first-run experience is never blank; signed-in users get the server-authoritative set. Android only seeds areas inside completeOnboarding (AppViewModel.kt:330-336, areas Work/Personal/Home/Health) and OnboardingScreen.finish (OnboardingScreen.kt:60). Any state where lifeAreas is empty and onboarding didn't run leaves a completely empty Areas list with no fallback. Lower severity because the onboarding gate (MainScaffold.kt:71,77-80) usually runs first, but it's a behavioral gap vs web's always-have-areas guarantee. Note web's seed set (5 areas incl. Volunteering, Health->teal) differs from Android's onboarding seed (4 areas, no Volunteering).
- fix: On first composition when lifeAreas is empty and the user is signed-out/unonboarded, seed the canonical demo set (Work/Personal/Home/Health/Volunteering with their canonical color tokens, matching use-life-areas COLOR_FOR) mirroring web seed().

### [App shell / nav / overlays /] Sign-out vs in-flight hydrate not coordinated by an abort (web uses cancelHydrate / AbortController)
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:116
- VERIFIED. Web bootstrap-listener.tsx:137 calls cancelHydrate() on SIGNED_OUT, which aborts in-flight Supabase selects via AbortController (hydrate.ts:160-173, .abortSignal(signal) on every select, lines 225-239) so a slow read can't write the prev user's rows back after the cache wipe. Android Hydrator.hydrate() (Hydrator.kt) and SyncGateway.fetchAll (SyncGateway.kt:16-17) have NO cancellation hook. It relies on the single sequential collect in SyncCoordinator.start (SyncCoordinator.kt:51) so the Authenticated handler (flush→hydrate→subscribe→pullCalendar, lines 130-133) fully completes before the NotAuthenticated/clearAll handler (lines 135-138) runs. Net data is still cleared (clearAll runs last), but there's a transient window where just-hydrated prev-user rows sit in Room and could render, and a long hydrate delays the sign-out wipe.
- fix: Run the Authenticated flush/hydrate/pull in a cancellable child Job tracked on the coordinator; on NotAuthenticated cancel it before store.clearAll() so a slow hydrate can't land prev-user rows. Optionally re-check auth.currentUserId after hydrate before subscribing.

### [App shell / nav / overlays /] Calendar OAuth CSRF state is in-memory only; check is SKIPPED (not rejected) if the process dies during Custom Tab consent
- partial | /Users/ahmadtambaya/Desktop/projects/unstuck_android/sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:71
- VERIFIED with a correction. SyncCoordinator.beginGoogleConnect stores the server-issued state in pendingCalState (in-memory, SyncCoordinator.kt:71). completeGoogleConnect (SyncCoordinator.kt:83) checks `if (expected != null && expected != state) return false` — so when the host process was evicted while the Chrome Custom Tab was open, expected is null on relaunch and the CSRF check is SILENTLY SKIPPED (connect proceeds unbound). CORRECTION to the original finding: the web does NOT persist state in localStorage — it uses sessionStorage (key 'unstuck-gcal-state', sync-flow.tsx:120/177) AND, more importantly, its check is strict: sync-flow.tsx:121 `if (!saved || saved !== state) { reject }` REJECTS when saved is null rather than proceeding. So the real parity gap is two-fold: (a) Android proceeds-on-null vs web rejects-on-null, and (b) Android keeps state in-memory so it can't survive a reload at all.
- fix: Two changes: (1) make the check fail-closed like web — `if (expected == null || expected != state) return false`; (2) persist pendingCalState to SharedPreferences when issuing it and read it back in completeGoogleConnect (clear after use) so the CSRF binding survives process death (sessionStorage-equivalent durability).

### [App shell / nav / overlays /] Signing out while offline silently discards queued outbox writes
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/data/src/main/kotlin/tech/csalliance/unstuck/data/LocalStore.kt:81
- VERIFIED. On SIGNED_OUT, SyncCoordinator.handle calls store.clearAll() (SyncCoordinator.kt:137), and LocalStore.clearAll (LocalStore.kt:81-85) clears records, outboxDao.clear(), and liveDao.clear(). If the user made edits offline (queued in the outbox) and signs out before reconnecting, those un-flushed writes are permanently lost. Web has no analog because it pushes every mutation immediately while online (bridge.ts dbUpsert awaits the round-trip, no offline queue), so the server already has the data before sign-out. Note severity is correctly low: the Authenticated handler flushes the outbox on every sign-in/initial-session (SyncCoordinator.kt:130), and sign-out while genuinely offline with pending edits is an edge case.
- fix: Before clearAll() on sign-out, attempt a best-effort flusher.flush(uid) while a valid token still exists, or warn/block sign-out when pendingCount > 0 while offline. At minimum document the trade-off.

### [App shell / nav / overlays /] completeOnboarding can re-seed duplicate canonical life-areas (reads a possibly-inactive WhileSubscribed flow)
- bug | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:331
- VERIFIED. AppViewModel.completeOnboarding (AppViewModel.kt:330-336) seeds Work/Personal/Home/Health only when lifeAreas.value.isEmpty(). lifeAreas is sf(store.lifeAreas()) = stateIn(viewModelScope, WhileSubscribed(5_000), emptyList()) (AppViewModel.kt:57/66). OnboardingScreen does not collect lifeAreas, so when completeOnboarding runs the flow may be inactive and .value returns the initial emptyList() even when Room already has areas (e.g. a returning user after reinstall whose server areas were just hydrated, or whose onboarded flag was reset). Seeding then appends a fresh canonical set with new newUuid() ids (line 334) and does NOT de-dupe by name → duplicates.
- fix: Read areas authoritatively inside the suspend launchWrite block — store.lifeAreas().first() — or de-dupe by name before seeding, so seeding never fires when areas already exist server/local-side.

### [App shell / nav / overlays /] Bottom nav lacks the per-view counts / Today badge the web rail shows; AreasMenu has no 'add area' affordance
- missing | /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/components/AreasMenu.kt:58
- VERIFIED. Web left-rail (components/layout/left-rail.tsx) shows open-task counts on Today/Insights/Collections (count={todayCount} l.165, count={completedCount} l.179, count={collections.length} l.187), a coral 'Today' badge (badge="1", l.157), per-area open counts (areaCounts, l.259) plus Unassigned (l.275), and an inline RailAddArea form to add a new area (l.242, defined l.363). Android BottomNavBar / NavCell (design/Chrome.kt:116-132) renders Icon + Text label only — no counts or badges. AreasMenu (AreasMenu.kt:53-58) DOES show per-area open counts + 'All tasks' + 'Unassigned' (good parity there), but offers no way to add a new area, unlike the web rail's inline RailAddArea.
- fix: Optional polish: surface open-task counts/badges on the bottom-nav cells, and add an 'Add area' row in AreasMenu that opens an area editor (name + color), matching web's RailAddArea.

