# Final pre-beta bug sweep — confirmed findings (107: 1 P1 / 33 P2 / 73 P3)

## P1 (1)

### [auth/ui] AuthScreen has no background — dark mode renders near-white text on a white window
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:74-77 (root Column); also app/src/main/res/values/themes.xml:6`
- **Symptom:** On a device set to dark mode, the very first screen a new user sees (sign in / sign up) shows almost-white text, fields, and labels on a white background — headings, the email/password labels, and the footer copy are nearly invisible / extremely low contrast.
- **Root cause:** The auth render path is AppRoot -> AuthScreen with no Surface or background painter. AuthScreen's root Column (line 75) calls fillMaxSize().verticalScroll()... but never .background(c.bg). UnstuckTheme (Theme.kt:120) also wraps content in MaterialTheme without a Surface. So the only paint is the Activity window background, hardcoded to @android:color/white in themes.xml:6 (no values-night variant). In dark mode the dark palette sets ink = oklch(0.96) (near-white) while the window stays white. The authenticated path works because MainScaffold.kt:188 explicitly does Box(Modifier.fillMaxSize().background(c.bg)); the auth/loading/setup screens were never given that, so dark mode is broken for them.
- **Fix:** Wrap AuthScreen's root Column in (or add to it) Modifier.background(UTheme.colors.bg) — or add a Surface(color = colors.bg) around the `content` inside UnstuckTheme so every screen (including AuthScreen, LoadingScreen, SetupScreen) gets a theme-correct background.
- _verify (high): Confirmed against actual code. AuthScreen.kt:74-77 root Column = Modifier.fillMaxSize().verticalScroll(...).imePadding().padding(...) with NO .background(); the only .background() in the file is on the Google-button Row (line 108). Theme.kt:119-147 UnstuckTheme wraps content in only CompositionLocal_

## P2 (33)

### [auth/ui] No client-side empty-field validation; empty email/password fires a network call and surfaces a raw Supabase error
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:101-104, 123, 126`
- **Symptom:** Tapping 'Sign in' / 'Create account' / 'Email me a magic link' / 'Forgot your password?' with the email (or password) field blank makes a real network round-trip and then shows a technical Supabase error instead of a friendly 'Enter your email' hint. Confusing on the first screen, and wastes the rate-limited email-send budget for blank submissions.
- **Root cause:** The run() helper (lines 63-72) and every action call it unconditionally — there is no isBlank() guard on email/password before calling vm.signIn/signUp/magicLink/resetPassword. The web app does guard (app/auth/sign-in/page.tsx:56 `if (!email.trim() || !password)`, sign-up/page.tsx:53,83), so this is a parity gap. Additionally the text links (lines 123,126) lack `enabled = !busy`, so they can be re-tapped mid-request and re-fire run().
- **Fix:** Before calling run(), validate: if email.trim() is empty (and for password actions, password too) set message to a friendly 'Enter your email' and return; and pass enabled = !busy on the magic-link/forgot-password clickables to match the Sign in button.
- _verify (high): Confirmed by reading the actual code. In AuthScreen.kt (lines 63-72, 101-104, 123, 126) the run() helper and every action pass email.trim()/password straight to the VM with no isBlank() guard. AppViewModel.kt:671-680 is a pure pass-through (auth?.signIn(...) ?: "Not configured"), and AuthService.kt:_

### [auth/data] "Forgot password" flow can't actually reset a forgotten password
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:119 (handleDeeplinks) + ui/auth/AuthScreen.kt:126 + ui/settings/SettingsScreen.kt:221-237 (PasswordDialog requires current password)`
- **Symptom:** User taps "Forgot your password?", gets the reset email, taps the link, and is silently dropped onto the signed-in app with no prompt to set a new password. To actually change it they must dig into Settings > Change password — which demands their CURRENT password (the one they forgot), so they're stuck. The reset flow never lets them set a new password.
- **Root cause:** resetPasswordForEmail uses the unstuck://auth-callback redirect, so the recovery deep link is consumed by graph.provider.client.handleDeeplinks(it) (MainActivity.kt:119) which just establishes a recovery session and flips authed→true. There is no PASSWORD_RECOVERY handling anywhere (grep finds none) and no recovery-specific 'set new password' screen. The only change-password UI (SettingsScreen PasswordDialog) gates on vm.hasPassword (true for any email/password user), so it requires the current password and even re-auths with it (SettingsScreen.kt:226-233), which a forgotten-password user cannot supply.
- **Fix:** Detect the SessionStatus.Authenticated recovery source (SessionSource.External / a recovery event) after handleDeeplinks and route the user to a dedicated set-new-password screen that calls auth.changePassword(newPw) WITHOUT requiring the current password.
- _verify (high): Verified end-to-end against current main. (1) AuthService.resetPassword calls client.auth.resetPasswordForEmail(email) with NO explicit redirectUrl (sync/.../AuthService.kt:69-71); supabase-kt then defaults to the configured scheme://host = unstuck://auth-callback (SupabaseClientProvider.kt:19-20,28_

### [tasks/correctness] Scheduling a recurring task drops the chosen first occurrence (today / off-pattern date)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt (+ core/.../logic/Recurrence.kt):AppViewModel.kt:177-203 (recurring branch ~180-191); Recurrence.kt:80-81`
- **Symptom:** Create or schedule a repeating task for today (or, for a Weekly recurrence, for a date whose weekday isn't in the pattern) and no cal_block is created for the date the user actually picked. The recurring task gets blocks starting tomorrow (or on the next matching weekday) but is missing from today's calendar/timeline; the first chosen occurrence silently never lands.
- **Root cause:** NewTaskSheet (line 234) and TaskDetailScreen.pickSchedule both route recurring tasks solely through vm.scheduleTask, whose recurring branch only calls regenerateForTask. regenerateForTask materializes occurrences and then filters `materializeOccurrences(...).filter { it.date > todayIso }` (Recurrence.kt:81), so the today occurrence is discarded, and materializeOccurrences only emits dates that match the recurrence pattern — so an explicitly-picked off-pattern date is never produced. The web reference (task-create-modal.tsx:273-299) unconditionally upserts a `firstBlock` for the chosen effectiveDate FIRST and only then materializes the rest, so the chosen date is never lost. Android has no equivalent firstBlock upsert (confirmed: no upsertCalBlock for the picked date in the recurring path).
- **Fix:** In scheduleTask's recurring branch (and/or NewTaskSheet), always upsert a block for the user-chosen (date,startTime) first — mirroring web's firstBlock — then run regenerateForTask for the remaining horizon, skipping the already-created chosen date.
- _verify (high): Confirmed real on current main. Both recurring-task entry points route solely through vm.scheduleTask: NewTaskSheet.kt:234 (after addTask, which creates no cal_block — AppViewModel.kt:121 only upserts the task) and TaskDetailSheet.kt:82 (pickSchedule). For a recurring task, scheduleTask's recurring _

### [tasks/ui] Scheduling a recurring task for today creates no block (diverges from web/iOS)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:177-203 (recurrence branch 180-191); core/.../logic/Recurrence.kt:80-86`
- **Symptom:** User opens a recurring task, taps Schedule, picks today (the date picker minDate allows today), gets the green 'Scheduled 06-05 9:00 AM' confirmation — but the task never appears on Today's timeline and no block exists for today. Same from the New-task sheet when 'When = Today' with a Repeat rule set. The task instead silently shows up only in Upcoming (tomorrow's occurrence).
- **Root cause:** scheduleTask branches on task.recurrence != null and runs regenerateForTask, whose materialized list is filtered with `.filter { it.date > todayIso }` (Recurrence.kt line 81) — so the today (and any past) anchor occurrence is dropped, and no explicit anchor block is upserted. Web's persistOrMove (schedule-task-modal.tsx:71-94) and iOS scheduleTaskAt (AppModel.swift:219-222) both just upsert ONE anchor CalBlock regardless of recurrence; regenerateForTask is reserved for the recurrence-editor path only. Android is the lone platform routing the schedule action through regenerateForTask. Rescheduling a recurring task is also wrong: it deletes/recreates future blocks at the new time but leaves the original today/past anchor block stale at the old time.
- **Fix:** In scheduleTask, drop the `if (recurrence != null)` regen branch entirely and always upsert/move a single anchor block (the else branch logic), matching web persistOrMove and iOS scheduleTaskAt; leave recurrence materialization to setRecurrence.
- _verify (high): Confirmed real defect on main. In AppViewModel.kt:177-203, scheduleTask branches on task.recurrence != null (line 180) and runs core regenerateForTask (line 184) with NO separate anchor upsert. regenerateForTask (Recurrence.kt:80-81) filters desired occurrences with `.filter { it.date > todayIso }`,_

### [tasks/data] Recurring task scheduled for today (or off-pattern) gets NO cal_block — vanishes from Today + calendar
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:177-203 (scheduleTask, recurring branch 180-191); root logic core/.../logic/Recurrence.kt:80-86 (regenerateForTask)`
- **Symptom:** Create a daily/weekly recurring task with When=Today (NewTaskSheet), or tap Schedule on a recurring task and pick today: future occurrences appear on the calendar, but no block is created for today. The task does not show in the Today tab and the today calendar slot is empty for it. Worse for Weekly: if the chosen date is not one of the selected weekdays (e.g. Weekly-Mon picked while today is Wed, or the editor's default Weekly=[Mon] while creating on a non-Monday), NO block is created for the picked date at all, so the user thinks scheduling failed.
- **Root cause:** scheduleTask routes recurring tasks through regenerateForTask, which is built for EDITING recurrence (preserve past/today history, regenerate only the future): it computes desired = materializeOccurrences(...).filter { it.date > todayIso } (Recurrence.kt:80-81) and only upserts those future, pattern-matching dates. On first-time placement there is no today block to 'preserve', so the today occurrence is silently dropped, and any off-pattern chosen date never matches matchesRecurrence so no block lands there. The web does the opposite: task-create-modal.tsx:273-283 ALWAYS upserts firstBlock at effectiveDate (today included, pattern or not) and only then materialises the rest of the horizon (skipping the duplicate first block). Because future blocks DO exist, visibleTasks also excludes the task from Today (t.id is in upcomingTaskIds, so the isCreatedToday fallback at VisibleTasks.kt:73 is gated off).
- **Fix:** In scheduleTask's recurring branch, when there is no existing block for the chosen date, first upsert a concrete block at (date, startTime) exactly like the non-recurring else-branch / web firstBlock, then call regenerateForTask only to materialise the remaining future horizon (skipping that just-created date) — do not rely on regenerateForTask to create the anchor/today/off-pattern block.
- _verify (high): REAL defect on current main, verified end-to-end. AppViewModel.kt:180-191 routes ALL recurring scheduling (including first-time placement) through regenerateForTask, never upserting a concrete anchor block at the chosen date. regenerateForTask (Recurrence.kt:80-81) computes desired = materializeOccu_

### [calendar/correctness] Unscheduled tray ignores blocks on other days, so a task scheduled on a different date reappears as 'unscheduled' and dragging it silently moves its existing block
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:150-152`
- **Symptom:** A task already scheduled on (say) Tuesday still shows up in the unscheduled tray when you flip the day picker to Wednesday. If you drag that 'unscheduled' card onto Wednesday's grid, the task is NOT added to Wednesday — instead its Tuesday block silently disappears and moves to Wednesday, so the original day loses the block.
- **Root cause:** `val dayBlocks = blocks.filter { it.date == date }` then `scheduledIds = dayBlocks.filter{isTaskBlock}.mapNotNull{it.taskId}.toSet()` and `unscheduled = tasks.filter { ... it.id !in scheduledIds }`. The scheduled set is computed from the currently-viewed date's blocks only, so tasks scheduled on any OTHER day are treated as unscheduled. The web tray (components/calendar/unscheduled-tray.tsx) instead does `!blocks.some(b => b.taskId === t.id)` across ALL blocks. Because scheduleTask's non-recurring path finds `existing` across all blocks and moves the earliest, the re-drag relocates the other-day block instead of creating a new one.
- **Fix:** Compute scheduledIds from the full `blocks` list (`blocks.filter{isTaskBlock}.mapNotNull{it.taskId}.toSet()`), not from `dayBlocks`, to match the web behavior.
- _verify (high): Verified the full chain in code. DayGrid.kt:150-152: `dayBlocks = blocks.filter { it.date == date }`, then `scheduledIds = dayBlocks.filter{isTaskBlock}.mapNotNull{it.taskId}.toSet()` and `unscheduled = tasks.filter { !done && later!=true && id !in scheduledIds }`. scheduledIds is computed ONLY from_

### [calendar/ui] Tapping a Google/external (or placeholder) block opens the "New Task" sheet instead of doing nothing
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:196-204, 253-262`
- **Symptom:** On the Day grid, tapping a blue Google/external event (or a neutral placeholder block) pops open the create-task bottom sheet prefilled at that event's time — as if the user tapped empty space. A tap on a synced calendar event should be inert (or show details), not start creating a task on top of it.
- **Root cause:** The tap-to-create gesture lives on the underlying hour Column (detectTapGestures at line 198). Block Boxes are drawn on top but only get pointer handling when isTaskBlock(b) is true; the `else Modifier` branch (line 261) means EXTERNAL and PLACEHOLDER blocks have no clickable/pointerInput, so the tap is not consumed and falls through to the Column's detectTapGestures, which fires onCreateAt(date, snappedTime). The block's own pixel position is ignored.
- **Fix:** Give non-task blocks a no-op gesture consumer (e.g. `.clickable(...) {}` or `.pointerInput(b.id) { detectTapGestures {} }`) so taps on external/placeholder blocks are swallowed instead of falling through to the create-task handler.
- _verify (high): Confirmed real on current main. In /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt, the underlying hour Column (lines 196-204) carries a detectTapGestures that fires onCreateAt(date, snappedTime). Block Boxes are drawn on top (l_

### [calendar/ui] All-day Google events render as a 15-minute sliver stacked at midnight
- **@** `core/src/main/kotlin/tech/csalliance/unstuck/core/logic/GoogleSyncMapping.kt:49-60 (via core/.../time/Time.kt:16-20)`
- **Symptom:** After connecting Google Calendar, all-day events (holidays, birthdays, OOO, multi-day trips) all appear as tiny 15-minute blocks piled on top of each other at 00:00 at the very top of the Day/Week grid, instead of as recognizable all-day bars. Their title is barely readable and they overlap.
- **Root cause:** The calendar-sync edge function emits all-day events as offset-less local datetimes (google.ts: `e.start.date + 'T00:00:00'`, e.g. "2026-06-05T00:00:00"). externalEventToBlock relies on Time.parseMillis, which only tries Instant.parse and OffsetDateTime.parse — both REJECT strings without a 'Z'/offset and return null. So isoToLocalHHMM falls back to "00:00", diffMinutes hits its null-guard default of 15, and isoToLocalYmd uses iso.take(10). The web works only because JS `new Date("...T00:00:00")` parses offset-less strings as local time; Java does not. (No test covers offset-less input — all parseMillis tests pass a 'Z'.)
- **Fix:** Make Time.parseMillis fall back to parsing an offset-less LocalDateTime (and bare LocalDate) in the system zone, or have externalEventToBlock detect a date-only / T00:00:00 span and render it as an all-day block with proper duration.
- _verify (high): Confirmed end-to-end on current main. (1) The calendar-sync edge function emits all-day events as offset-less local datetimes: supabase/functions/calendar-sync/providers/google.ts:115-116 builds start/end as `e.start.date + 'T00:00:00'` (e.g. "2026-06-05T00:00:00"), with a comment explicitly noting _

### [calendar/data] All-day Google Calendar events collapse to a 15-minute sliver at 00:00
- **@** `core/src/main/kotlin/tech/csalliance/unstuck/core/time/Time.kt (Time.parseMillis) consumed by core/.../logic/GoogleSyncMapping.kt (isoToLocalHHMM / diffMinutes / externalEventToBlock):Time.kt:16-20; GoogleSyncMapping.kt:31-43,49-60`
- **Symptom:** An all-day event pulled from Google (e.g. a birthday, a vacation, an all-day meeting) renders on the Day and Week calendar grids as a tiny 15-minute blue block at 12:00 AM instead of spanning the day, and shows the wrong time. On the web app the same event renders correctly as a full-day block.
- **Root cause:** The calendar-sync Edge Function normalizes all-day Google events to offset-less local datetime strings ('2026-06-05T00:00:00', no 'Z' and no zone offset — supabase/functions/calendar-sync/providers/google.ts normalize()). Time.parseMillis only attempts Instant.parse() and OffsetDateTime.parse(), both of which throw DateTimeParseException on a string with no offset (verified on the JVM), so it returns null. externalEventToBlock then falls back: isoToLocalHHMM returns '00:00' and diffMinutes returns its 15-minute floor instead of the true 1440-minute span. The web client survives this because new Date('2026-06-05T00:00:00') parses as local time, so diffMinutes yields 24h there — a real Android-vs-web parity gap.
- **Fix:** Add a final fallback in Time.parseMillis that parses an offset-less ISO local-datetime as local time, e.g. catch the offset-less form with LocalDateTime.parse(iso).atZone(ZoneId.systemDefault()).toInstant().toEpochMilli() so all-day events get a correct local midnight start and full duration.
- _verify (high): Confirmed real and reachable on main. The full chain checks out: (1) the Edge Function normalize() in supabase/functions/calendar-sync/providers/google.ts:115-116 emits all-day events as offset-less local strings (`e.start.date + 'T00:00:00'`, no Z/offset). (2) CalendarClient.pullEvents (sync/.../Ca_

### [focus/correctness] Capture sheet auto-opens spuriously on a later focus open (focusAutoCapture state leak)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:152, 159, 184, 194, 243, 289`
- **Symptom:** After the user taps the live notification's "Capture" action and then leaves Focus with the system back gesture, the next time they open ANY task's focus screen the Capture bottom sheet pops up on its own, blocking the timer view until dismissed.
- **Root cause:** `focusAutoCapture` is set to `true` at line 152 (the `dl == "capture"` deep-link branch) and is only reset to `false` inside FocusScreen's `onClose` (line 289). System back uses the BackHandler at line 184 (`focusTask = null`) which does NOT reset `focusAutoCapture`, and the other entry points that set `focusTask` (line 159 `focusTask = t`, line 194/243 `onStartFocus = { focusTask = it }`) also never clear it. So the stale `true` is passed as `autoCapture` to the next FocusScreen, whose `var showCapture by remember { mutableStateOf(autoCapture) }` + `LaunchedEffect(autoCapture){ if (autoCapture) showCapture = true }` (FocusScreen.kt lines 83/86) force the sheet open.
- **Fix:** Reset `focusAutoCapture = false` in the BackHandler at line 184 and in every other `focusTask = ...` setter (or, simpler, set `focusAutoCapture = false` whenever `focusTask` is assigned from a non-capture path).
- _verify (high): Confirmed real state-leak. In MainScaffold.kt: `focusAutoCapture` (line 91, plain `remember`, starts false) is set to true ONLY in the capture deep-link branch (line 152: `if (liveTask != null) { focusTask = liveTask; focusAutoCapture = true }`). The only reset is inside the FocusScreen onClose at l_

### [focus/ui] Treatment-switcher chips have inverted selected/unselected affordance in light mode
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:125-129 (and SelectableChip.kt:21-33)`
- **Symptom:** On the always-dark indigo Focus background, when the app runs in LIGHT mode the UNSELECTED treatment chips (ambient/cockpit/monk) render as bright near-white solid pills with dark text, while the SELECTED chip is a dim translucent-white pill. The currently-active treatment looks deselected and the inactive ones look 'on' — confusing and visually broken.
- **Root cause:** FocusScreen passes accent=Color.White.copy(alpha=0.18f) but SelectableChip only uses `accent` for the SELECTED branch; the unselected branch falls back to theme tokens bg = c.bg2 (#F4F2EC in light theme) and text = c.ink2 (#414252). The Focus screen forces its own dark bg (UTheme is still light because AppRoot themes by isSystemInDarkTheme/settings), so light-theme bg2/ink2 produce bright pills that read as 'selected'.
- **Fix:** In FocusScreen build the chips with explicit dark-surface colors (e.g. white-alpha fills/strokes for both states) instead of relying on SelectableChip's theme-derived unselected colors, so the selected chip is the bright one regardless of app theme.
- _verify (high): Verified against current main. FocusScreen.kt:125-129 builds the treatment-switcher chips with `SelectableChip(t.name.lowercase(), selected = treatment == t, accent = Color.White.copy(alpha = 0.18f))`. SelectableChip (actual path ui/tasks/SelectableChip.kt:21-33, not the path in the claim) computes _

### [focus/data] Starting a new task discards the in-progress live session's unsaved focus time
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:317-327 (startFocus); core/.../logic/FocusTimer.kt:68-89 (start)`
- **Symptom:** While a focus session for task A is running/paused, tapping the "Start next" hero (or Focus on another task) on Today opens a fresh session for task B. Task A's accumulated-but-unsaved elapsed time silently vanishes: no Session row is written and task.totalFocused is never incremented, so that focus time is lost from analytics/progress.
- **Root cause:** startFocus only early-returns when cur?.taskId == task.id. For a different task it calls FocusTimer.start(base, taskB, ...), which (since cur.taskId != taskId) takes the "fresh session" branch in FocusTimer.start (FocusTimer.kt:78-88) and overwrites the LiveSession via store.setLiveSession(...) without ever calling finishFocus/upsertSession on the displaced session. This path is reachable: TodayScreen computes startNext via pickStartNext(tasks, blocks, liveId, areaFilter) which explicitly EXCLUDES liveId (TodayScreen.kt:123,129,132), so the hero is always a different task than the live one.
- **Fix:** In startFocus, when cur has a live session for a different taskId, first finalize it (write its Session row + accumulate totalFocused via the same logic as finishFocus) before starting the new task's session — or block/confirm the switch.
- _verify (high): Verified end-to-end on main. In AppViewModel.startFocus (AppViewModel.kt:317-328), the early-return only fires when `cur?.taskId == task.id`. For a different task it calls `FocusTimer.start(base=cur, taskB, ...)`. In FocusTimer.start (FocusTimer.kt:68-89), the two same-task guards (lines 76-77) requ_

### [focus/data] Captures saved during a session can be permanently dropped due to a session_id foreign-key ordering gap
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/WriteThrough.kt:61-64 (upsertCapture); OutboxFlusher.kt:50-56 (poison-pill drop)`
- **Symptom:** A capture taken during a live focus session (cockpit "capture this distraction") can fail to reach the server and be silently dropped — it appears locally but never syncs, and is lost on reinstall/other devices.
- **Root cause:** saveCapture writes the capture with sessionId = live.id and upsertCapture enqueues the captures upsert with NO dependsOn (WriteThrough.kt:61-63). But the matching sessions row is only written at session END (AppViewModel.finishFocus / FocusCommands.end). The DB has captures.session_id uuid references public.sessions(id) (migration 002_captures.sql:17), so if the outbox flushes mid-session (before the session row exists) the capture INSERT violates the FK. OutboxFlusher retries, and after FAIL_CAP=5 consecutive failures (OutboxFlusher.kt:53-56) it drops the op as a "poison pill." A long session with multiple online reconnects can exhaust the retries before the session row is ever created.
- **Fix:** Give the captures outbox op a dependsOn on its session id (so it holds until the sessions row flushes), or write/enqueue a minimal sessions row at session start instead of only at end.
- _verify (high): REAL and reachable on current main. Full chain verified: (1) CaptureSheet during focus receives `live?.id` (FocusScreen.kt:186) → saveCapture(task.id, sessionId,...) → Capture(sessionId=live.id) (AppViewModel:383-385). upsertCapture enqueues the captures op with NO dependsOn (WriteThrough.kt:61-64);_

### [collections/correctness] Owned solo-list item edits can be silently lost while auth is transiently null (isShared false-positive → RPC-only path, no outbox)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:415-416, 443-459`
- **Symptom:** An item the user adds/edits/checks/removes on one of their OWN collections occasionally never reaches the server and disappears on the next sync/hydrate.
- **Root cause:** isShared(c) returns members.isNotEmpty() || (c.ownerId != null && c.ownerId != currentUid()). currentUid() is client.auth.currentUserOrNull()?.id, which can momentarily be null (session restore / token refresh) while server-hydrated collections (ownerId populated) are already on screen. With currentUid()==null, an owned list evaluates ownerId != null && ownerId != null → true, so mutateCollectionItem takes the SHARED branch: it does an optimistic store.upsert (NO outbox) and then share?.let { rpc(it) }. If the RPC fails or share is null, the change exists only in local Room and is never enqueued; the next hydrateCollections does store.replace (full replace, Hydrator.kt:60), wiping the local-only edit.
- **Fix:** Treat a collection as shared only when members.isNotEmpty() || (c.ownerId != null && currentUid() != null && c.ownerId != currentUid()), i.e. never classify as shared when the current user id is unknown, and/or fall back to the outbox whole-row upsert when share == null.
- _verify (medium): REAL defect, reachable on main, though only in a narrower window than the claim states (the claim's "token refresh" trigger is wrong; the real trigger is RefreshFailure / Initializing).

Code confirmed:
- AppViewModel.kt:415-416: isShared(c) = c.members.isNotEmpty() || (c.ownerId != null && c.ownerI_

### [collections/data] "Just me" promote on a shared list is permanent + announces to everyone
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:509-528 (moveItemToTask), 501-505 (markItemPromoted), 512 + CollectionDetailScreen.kt:330 (guards)`
- **Symptom:** On a shared collection, tapping "Just me" in the Move-to-task chooser strikes the item through and shows "<you>'s on it" to ALL members, and that chip never clears: the Move-to-task icon disappears for that item forever and it can never be re-promoted, even after the user finishes the task.
- **Root cause:** moveItemToTask always calls markItemPromoted(...) (line 526) regardless of mode, and for a shared collection mutateCollectionItem routes every write through the collection_set_item_promotion RPC, so even SELF mode persists promoted=true/assignee to the shared row visible to everyone. SELF mode creates an UNLINKED task (sourceCollectionId=null at lines 516-517), so completing it never calls share.taskDone, so the server never sets promotedDone=true. The item is left promoted=true, promotedDone=null permanently. The re-promote guard `item.promoted == true && item.promotedDone != true` (line 512) is then always true → blocked, and the UI hides the Move-to-task action via `if (!promoted || promotedDone)` (CollItemRow line 330). LOOP self-heals because taskDone flips promotedDone=true; SELF never does.
- **Fix:** In moveItemToTask, when mode == SELF, skip markItemPromoted entirely (or pass promoted=false) so a "just me" promote on a shared list does not mark the shared item or block re-promotion.
- _verify (high): Confirmed real on current main by reading the full chain. In AppViewModel.moveItemToTask (line 526) markItemPromoted is ALWAYS called regardless of mode. For SELF mode, loop = (mode==LOOP && isShared) is false, so addTask gets sourceCollectionId=null/sourceItemId=null (lines 516-517) and markItemPro_

### [notifications/correctness] All external-event lead reminders share one notification id (overwrite each other)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/surface/ReminderReceiver.kt:38`
- **Symptom:** With two or more synced Google Calendar events whose pre-event reminders fall close together, only the last "Coming up" notification is visible — earlier ones are silently replaced, so the user misses event reminders.
- **Root cause:** ReminderScheduler arms a LEAD alarm for external blocks (line 80) passing the task id as `b.taskId ?: ""` (line 103), which is empty for external events. The receiver renders with `notifId = NotifIds.reminder(taskId)` (line 38); NotifIds.reminder("") = REMINDER_BASE + ("".hashCode() and 0xFFFF) = a single constant id, so every external lead reminder collides on the same id and overwrites the previous one (the underlying alarms are keyed by block id and all fire, only the notification is lost).
- **Fix:** Derive the lead notification id from the block id when the task id is blank (pass blockId into the receiver and use NotifIds.reminder(blockId.ifBlank-fallback) for external events) so each event gets a distinct notification.
- _verify (high): Confirmed real and correctly diagnosed against current main. For external (Google Calendar) blocks, b.taskId is null, so ReminderScheduler.sync sets taskId="" (line 76) and passes b.taskId ?: "" (line 103) → EXTRA_TASK_ID="". In ReminderReceiver.kt the lead branch (line 38) renders with notifId = No_

### [notifications/data] All external-calendar-event "Coming up" reminders collapse onto one notification id
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/surface/ReminderReceiver.kt:38 (id derived via NotifIds.reminder); origin app/.../surface/ReminderScheduler.kt:103 and NotificationChannels.kt:68-69`
- **Symptom:** When two or more external (Google) calendar events are within their lead window, only the most-recently-fired "Coming up" notification is visible — earlier event reminders are silently overwritten, so the user misses heads-up for the other events.
- **Root cause:** External blocks have taskId == null. ReminderScheduler arms the LEAD alarm with taskId = b.taskId ?: "" (ReminderScheduler.kt:103), and ReminderReceiver posts it with notifId = NotifIds.reminder(taskId). NotifIds.forTask(base, taskId) = base + (taskId.hashCode() and 0xFFFF); "".hashCode() == 0, so every external event resolves to the identical id REMINDER_BASE (0x30000). Re-issuing the same id updates in place, so each new external reminder replaces the previous one. (Per-task reminders are unaffected because each task supplies a distinct taskId.)
- **Fix:** For external events, derive the reminder notification id from the block id instead of the empty task id — e.g. pass/use b.id when taskId is blank: NotifIds.reminder(taskId.ifBlank { blockId }).
- _verify (high): Confirmed real on current main by reading the code. (1) External blocks have taskId=null: GoogleSyncMapping.kt:52 creates external CalBlock with `taskId = null`; Models.kt:110 declares `val taskId: String? = null`. (2) The LEAD/"Coming up" alarm IS armed for external events: ReminderScheduler.kt:79 _

### [settings/ui] Error messages are shown in success-green, looking like confirmations
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:218 (AccountContent), 191 (BackupContent)`
- **Symptom:** When an account action FAILS — "Export failed.", "Current password incorrect.", "Can't verify your current password — no email is set on this account.", or any AuthOutcome.Error message from changePassword/updateDisplayName/deleteAccount — the message renders in the same green used for success ("Exported.", "Name updated.", "Password updated."). A user who mistypes their current password sees a green "Current password incorrect." and may read it as success.
- **Root cause:** Both status lines hardcode the success color: `msg?.let { Text(it, ... color = c.green ...) }` (line 218) and the BackupContent line 191 `color = c.green`. The single `msg` string holds both success and error text, but the color is never branched on outcome.
- **Fix:** Track success vs error separately (e.g. a `Boolean isError` alongside `msg`, or two state slots) and render error text in `c.red` while keeping success text in `c.green`.
- _verify (high): Confirmed by direct reading of SettingsScreen.kt. Both status lines hardcode success-green: line 191 (BackupContent) `msg?.let { Text(it, ... color = c.green ...) }` and line 218 (AccountContent) `msg?.let { Text(it, ... color = c.green ...) }`. A single `msg: String?` state (declared at lines 179 a_

### [settings/ui] Tag delete is immediate with no confirmation, unlike Area delete
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:416 (TagsContent); cascade in AppViewModel.kt deleteTag ~557`
- **Symptom:** In Settings → Tags, opening a tag's kebab menu and tapping "Delete" instantly removes the tag AND strips it from every task that used it, with no confirmation and no undo. By contrast, Settings → Areas shows a "Delete \"name\"?" AlertDialog before deleting. A mis-tap silently mutates many tasks.
- **Root cause:** The Tags menu wires delete directly: `DropdownMenuItem(... onClick = { menu = false; vm.deleteTag(tag.id) })` (line 416), and `deleteTag` cascades `tasks.value.filter{...}.forEach{ upsertTask(... tags stripped ...) }` (AppViewModel ~557-563). The Areas branch instead gates deletion behind a `confirm` AlertDialog (lines 349/353-360).
- **Fix:** Gate tag deletion behind the same confirmation AlertDialog pattern Areas already uses (set a `confirm` flag in the menu item, delete only on the dialog's confirm button).
- _verify (high): Confirmed against current main. In SettingsScreen.kt TagsContent, the Tags kebab "Delete" item wires deletion directly: line 416 `DropdownMenuItem(... onClick = { menu = false; vm.deleteTag(tag.id) })`. TagsContent declares no `confirm` flag (lines 392-395 only declare menu/editing/nameDraft/palOpen_

### [settings/data] Renaming an area/tag has no duplicate-name guard, breaking name-keyed filtering
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:597-601 (renameLifeArea) and 576-582 (renameTag); UI triggers at SettingsScreen.kt:338 and :407`
- **Symptom:** A user with areas "Work" and "Personal" can rename "Work" -> "Personal" (or tag #a -> #b where #b exists). The list then shows two areas/tags with the identical name. On Today/Tasks the area filter pill appears twice, filtering by that name is ambiguous, the per-area "N open" count is split/duplicated across both rows, and the rename cascade rewrites every matching task's label, so the two entries become permanently entangled.
- **Root cause:** renameLifeArea only rejects empty or no-op names (`if (nm.isEmpty() || nm == area.name) return`) and renameTag does the same; neither checks `lifeAreas/tags.none { it.name.equals(nm, ignoreCase = true) }`. The Add path explicitly guards against this (SettingsScreen.kt:371 and :427, with the comment that name-keyed filtering goes ambiguous), but the Rename path omits the same guard. Areas/tags are matched to tasks purely by name string (TodayScreen.kt:129, TasksScreen.kt:107, AppViewModel.kt:560/591), so two same-named rows collide.
- **Fix:** In renameLifeArea/renameTag, bail out (or surface a UI error) when another area/tag already has that name case-insensitively, mirroring the duplicate check already in the Add buttons.
- _verify (high): Confirmed real and reachable on main. In AppViewModel.kt, renameLifeArea (line 597-601) guards only `if (nm.isEmpty() || nm == area.name) return@launchWrite`, and renameTag (line 576-582) does the same — neither checks `lifeAreas/tags.none { it.name.equals(nm, ignoreCase = true) }`. The Add path DOE_

### [insights/correctness] Stacked-bars chart silently drops hours and grays out for users with custom/renamed life areas
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt (+ core/.../logic/Analytics.kt):InsightsScreen.kt:108, 211, 217; Analytics.kt:30, 44`
- **Symptom:** In the Report tab, the "When focus happens" stacked-bars chart shows empty/near-empty bars and a legend whose swatches are all the same gray, even though the user has completed plenty of focus sessions. Any focus time spent on a renamed or user-added life area (e.g. "Family", "Study", "Side project", or a renamed "Work"→"Job") simply does not appear.
- **Root cause:** The chart is hardwired to the canonical list DEFAULT_AREAS = ["Work","Personal","Home","Health","Volunteering"] (Analytics.kt:30). weekdayAreaHours is called with that default (InsightsScreen.kt:108) and buckets a session via areas.indexOf(area) (Analytics.kt:44) — a custom/renamed area name returns -1 and the session is dropped. The legend and segment colors are likewise built by iterating DEFAULT_AREAS and resolving each name through areaColorFor(name, lifeAreas, c) (InsightsScreen.kt:211/217); when the literal name isn't present in the user's lifeAreas, areaColorFor falls back to colors.ink4 (Common.kt:138), so every swatch/segment renders gray. Life areas are fully user-customizable (rename/delete/add in Settings; onboarding offers Family/Study/Side project), so the canonical list is not guaranteed to match.
- **Fix:** Drive the chart from the user's actual lifeAreas — pass lifeAreas.map { it.name } as the `areas` argument to weekdayAreaHours and use the same list (not DEFAULT_AREAS) for the legend and color lookups.
- _verify (high): REAL and reachable on current Android main. Analytics.kt:30 defines DEFAULT_AREAS=["Work","Personal","Home","Health","Volunteering"], used as the default `areas` arg. weekdayAreaHours buckets each session via `areas.indexOf(area); if (ai < 0) continue` (Analytics.kt:44) — any task whose lifeArea isn_

### [insights/data] Notification-shade 'End' orphans the session's captures from the interruption histogram
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/surface/FocusCommands.kt:45-46 (Session id); contract relied on by core/.../logic/Analytics.kt interruptionBins (line 75-89) + ui/insights/InsightsScreen.kt:109`
- **Symptom:** When a user ends a focus session from the notification shade (live-focus 'End' button on FocusTimerService, or the paused-too-long check-in 'End' on NotificationRenderer), the 'When interruptions happen' chart in Insights silently omits every capture taken during that session. Those interruption thoughts never appear in the histogram even though they were saved.
- **Root cause:** interruptionBins joins captures to sessions by Session.id == Capture.sessionId. During a live session every capture is stamped with sessionId = live.id (FocusScreen.kt:186 passes live?.id into CaptureSheet → AppViewModel.saveCapture). AppViewModel.finishFocus correctly reuses that same id: Session(id = live.id ?: newUuid(), ...) (AppViewModel.kt:350, with the comment at 347-348 stating this is required for the histogram). But FocusCommands.end — the code path for the notification 'End' action (NotificationActionReceiver ACTION_END) — writes Session(id = newUuid(), ...) (FocusCommands.kt:46), generating a fresh UUID that matches no capture's sessionId, so sessionStart[s.id] is never the start the captures point at and every such capture is dropped by `start ?: continue`.
- **Fix:** In FocusCommands.end, write the Session with id = live.id ?: newUuid() (reuse the live-session id, exactly as AppViewModel.finishFocus does) so captures taken during the session join back to it.
- _verify (high): Confirmed real and reachable on current main. The join contract: Analytics.interruptionBins (core/.../logic/Analytics.kt:79) builds sessionStart keyed by Session.id, then line 82-83 looks it up by sid = Capture.sessionId, dropping any capture with no matching session id (start ?: continue). Captures_

### [sync/correctness] Deleting an unsynced cal_block resurrects it on the server (outbox reorders delete ahead of its blocked upsert)
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/OutboxFlusher.kt:34, 41-58 (and WriteThrough.deleteCalBlock lines 87-94 which never cancels the pending upsert)`
- **Symptom:** User (offline) creates a task, schedules it (a cal_block), then deletes the scheduled block before sync drains. After reconnecting, the deleted block reappears on the calendar (re-pushed to the server and synced back to every device).
- **Root cause:** A cal_block upsert op carries dependsOn = its parent task id (WriteThrough.upsertCalBlock line 43), so while the task op is still pending the cal_block UPSERT is held back by the flushable filter (`dependsOn !in pendingIds`, line 34). The matching DELETE op has dependsOn = null, so it is flushable in the same pass and runs first — deleting a row the server doesn't yet have (no-op). On the next pass the parent task has flushed, the previously-held UPSERT becomes flushable and is applied, re-INSERTing the just-deleted row. The per-row order guard (`blockedRows`) only kicks in on failures, not on dependency-deferral, and WriteThrough.deleteCalBlock enqueues the delete without removing/cancelling the earlier pending upsert for the same id.
- **Fix:** When enqueuing a delete in WriteThrough.deleteCalBlock (and deleteLocalAndEnqueue), first purge any pending outbox upsert ops for the same (recordTable, recordId); alternatively, in OutboxFlusher have a successful delete also dequeue earlier pending upserts for that same row key.
- _verify (high): Verified against current main. Reconstructed the offline create→schedule→delete-before-drain flow and traced the flusher pass-by-pass; the defect reproduces exactly as claimed.

Ops enqueued (each a distinct row — OutboxEntity PK is `seq` autoGenerate, so the @Insert(REPLACE) in Daos.kt:60-61 never _

### [sync/correctness] Poison-pill drop of a parent task op silently discards the offline-created task AND its dependent cal_block
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/OutboxFlusher.kt:50-56, 63 (FAIL_CAP = 5)`
- **Symptom:** An offline-created task whose server upsert is rejected by a constraint (e.g. an estimate_min the client lets exceed the DB's `between 1 and 1440` check, or any RLS/validation rejection) vanishes after ~5 sync attempts, taking its scheduled cal_block with it, and never reaches any other device. No user-visible error.
- **Root cause:** After FAIL_CAP consecutive failures the op is dropped from the outbox as a 'poison pill' (lines 53-56). A genuinely server-rejected task upsert (not a transient network error) hits this path. Once the task op is dequeued, its dependent cal_block op is no longer held back (dependsOn no longer in pendingIds, line 34), tries to flush, fails on the FK to the never-created parent, and is itself dropped as poison. Because hydrate (Hydrator.hydrateCalBlocks/replace) is server-canonical and wipes local rows not present on the server, the optimistic local task/block also disappear on the next hydrate — permanent silent data loss.
- **Fix:** Distinguish transient (network/5xx) failures, which should keep retrying, from permanent (4xx/constraint/RLS) rejections; surface a permanent rejection to the user (or quarantine the op) instead of silently dropping it after FAIL_CAP, and do not auto-drop a child op merely because its parent was dropped.
- _verify (high): REAL and reachable on current main. Verified the full chain: (1) OutboxFlusher.kt:44-56 — runCatching wraps apply() and treats EVERY thrown exception identically; after FAIL_CAP=5 consecutive failures it unconditionally store.dequeue's the op (poison-pill drop) with no transient-vs-permanent distinc_

### [sync/ui] Sign-out discards un-synced offline writes (outbox wiped without flushing)
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:258-262 (handle: NotAuthenticated.isSignOut) + signOutAndUnregister 61-64`
- **Symptom:** A user who created/edited tasks while offline (or whose outbox simply hadn't drained yet) and then taps 'Sign out' permanently loses those edits — they never reach the server and are gone on the next sign-in.
- **Root cause:** signOut() -> signOutAndUnregister() -> auth.signOut() triggers the SessionStatus.NotAuthenticated branch, which calls store.clearAll(). clearAll() (LocalStore.kt:81-85) wipes records AND outboxDao.clear(). There is no flusher.flush() before sign-out, so any pending OutboxEntity rows (the only copy of unsynced offline writes — see UnstuckDatabase.kt comment) are dropped.
- **Fix:** In signOutAndUnregister(), best-effort flush the outbox first (e.g. auth.currentUserId?.let { flusher.flush(it) } inside runCatching, with a short timeout) before calling auth.signOut(), so queued offline edits are pushed before clearAll() wipes them.
- _verify (high): CONFIRMED real defect, reachable on main. Chain verified end-to-end: signOutAndUnregister() (SyncCoordinator.kt:61-64) calls only push.unregister + auth.signOut() with NO flush. auth.signOut() -> client.auth.signOut() (AuthService.kt:95-97) drives sessionStatus to NotAuthenticated, whose isSignOut b_

### [sync/ui] Poison-pill drop silently loses an offline write (and orphans its dependents)
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/OutboxFlusher.kt:53-56 (FAIL_CAP drop) + 33-34 (dependsOn gate)`
- **Symptom:** A task (or other row) created/edited offline that fails to sync 5 times in one drain is silently dropped: it keeps showing locally until the next hydrate, then vanishes entirely — with no error surfaced to the user. If it was a parent task, its dependent cal_block then flushes against a row the server never received (FK/RLS error) and is eventually dropped too.
- **Root cause:** When failCounts[op.seq] reaches FAIL_CAP (5), the op is store.dequeue'd and treated as 'progressed', discarding the only copy of the write. Because the parent op is now gone from pendingIds, the dependent cal_block (dependsOn = task.id, WriteThrough.kt:43) becomes flushable and is sent referencing a non-existent parent. Nothing re-surfaces the dropped data to the UI or user.
- **Fix:** Before dropping a poison op, persist it to a dead-letter store and/or surface a sync-error indicator; at minimum, when dropping a parent op also drop (or quarantine) its dependents so an orphaned child isn't pushed referencing a row the server never saw.
- _verify (high): Confirmed real on current main. OutboxFlusher.kt:53-56 — when failCounts[op.seq] reaches FAIL_CAP (5) the op is store.dequeue'd and progressed=true, discarding the only persisted copy of the write. The optimistic local Room row was never deleted, so it keeps showing in the UI until the next hydrate;_

### [chrome/correctness] Stale calendar prefill leaks into the FAB "New task" sheet after backgrounding
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:89, 97-99, 205, 209, 269`
- **Symptom:** User taps a calendar time-slot to create a task (sheet pre-filled with that date/time), then backgrounds the app before finishing. Later they tap the center + FAB to add an unrelated task and the New-task sheet opens already pre-filled with the old calendar date/time, silently scheduling the new task to the wrong slot.
- **Root cause:** `newTaskPrefill` (line 89) is only cleared inside `NewTaskSheet`'s `onDismiss` (line 269). The `LifecycleEventEffect(Lifecycle.Event.ON_STOP)` (lines 97-99) sets `showNewTask = false` but does NOT reset `newTaskPrefill`, so it stays non-null. `onFab = { showNewTask = true }` (line 209) reopens the sheet without clearing the prefill, and the calendar path (line 205, `onCreateAt`) is the only thing that ever sets it.
- **Fix:** In the ON_STOP effect also reset `newTaskPrefill = null`, and/or change `onFab` to `{ newTaskPrefill = null; showNewTask = true }` so the FAB always opens a blank composer.
- _verify (high): Confirmed real defect in /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt. grep confirms exactly three sites touch the relevant state: (a) line 205 onCreateAt = { d, t -> newTaskPrefill = d to t; showNewTask = true } is the ONLY non-_

### [chrome/ui] Floating feedback bubble overlaps & steals taps on bottom-right calendar week-grid blocks
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:216-225 (bubble); CalendarScreen.kt:174-213 (grid)`
- **Symptom:** On the Calendar > Week view, the circular feedback bubble (bottom-right) sits on top of the late-hour / Sunday calendar blocks. Tapping one of those small evening blocks hits the bubble and opens the feedback sheet instead of opening the task; the block is also visually covered.
- **Root cause:** The bubble is declared after the content Column in the root Box at align(BottomEnd), padding(end = 16.dp, bottom = 74.dp), size 50dp, so it is drawn on top of and intercepts pointer events over the bottom-right of the tab content. The week grid (CalendarScreen WeekView, WSTART=0..WEND=24 at WHOUR=44dp = 1056dp scrollable) renders small clickable blocks (heightIn 13dp) right down into that bottom-right region, with only a 16dp bottom Box (line 214) of clearance, so blocks land directly under the bubble.
- **Fix:** Either hide the bubble while tab == "calendar" (add to the guard at MainScaffold.kt:216), or give the calendar grid (and Today/Tasks lists) bottom content padding of ~96dp so no tappable element ever sits under the bubble’s footprint.
- _verify (medium): Confirmed against the actual code. In MainScaffold.kt the root Box(line 188) holds two direct children in order: the content Column (189-210, which contains the tab content Box and the BottomNavBar) and then the feedback bubble (216-225). Because the bubble is declared AFTER the Column, Compose draw_

### [chrome/ui] Long feedback message pushes the Send button off-screen (no scroll on the composer Column)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/feedback/FeedbackSheet.kt:84-122`
- **Symptom:** In Send Feedback, typing more than a few lines (a real bug report) grows the message box; the Send button gets pushed below the bottom of the sheet and becomes unreachable, so the user cannot submit and the sheet looks stuck.
- **Root cause:** The composer is a plain Column (line 84) with imePadding but NO verticalScroll, and the message BasicTextField has heightIn(min = 88.dp) with no max and singleLine off (lines 102-107). As body grows, the field expands unbounded inside the fixed-height ModalBottomSheet, displacing the Send UButton (line 114) past the visible/sheet bounds.
- **Fix:** Wrap the composer Column in Modifier.verticalScroll(rememberScrollState()) (or cap the message field with heightIn(min = 88.dp, max = 220.dp) and make the field itself scroll) so Send stays reachable.
- _verify (high): Confirmed real and reachable on current main. In /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/feedback/FeedbackSheet.kt: line 84 the composer is a plain Column with imePadding() and padding but NO verticalScroll; lines 102-107 the message BasicT_

### [chrome/data] Stale calendar prefill leaks into FAB-created tasks after backgrounding
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:97-99, 209, 269`
- **Symptom:** User taps an empty Calendar slot (which prefills a new-task date/time), then backgrounds the app instead of saving. On reopen they land on Today and tap the center + FAB to add an unrelated task — but the New Task sheet silently comes up pre-scheduled with the old calendar date and time, so the task gets created on a date/time the user never picked.
- **Root cause:** onCreateAt sets `newTaskPrefill = d to t; showNewTask = true` (line 205). The ON_STOP lifecycle reset (line 98) clears `showNewTask = false` but does NOT clear `newTaskPrefill`, and the FAB handler `onFab = { showNewTask = true }` (line 209) never resets it either. Only the sheet's onDismiss (line 269) nulls it. So a backgrounded-while-open prefill survives and is consumed by the next FAB-opened NewTaskSheet, whose prefillDate/prefillTime drive `whenSel`/`pickedDate`/`pickedTime` (NewTaskSheet.kt lines 92-95).
- **Fix:** Add `newTaskPrefill = null` to the ON_STOP reset block (line 98) and to the FAB `onFab` handler so a FAB-opened New Task always starts unscheduled.
- _verify (high): Confirmed real and reachable on main. In MainScaffold.kt, `newTaskPrefill` (line 89) is a `remember`-scoped state shared between the calendar-create path and the FAB. The calendar slot handler sets it (line 205: `onCreateAt = { d, t -> newTaskPrefill = d to t; showNewTask = true }`). The ON_STOP lif_

### [chrome/data] Starting focus from a 'starts now' notification silently discards an in-progress session
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:154-165`
- **Symptom:** While actively focusing task A, the user taps 'Start' on a different task B's starts-now notification. Task A's running focus session is replaced by B with no warning, and the time already accumulated on A in that session is lost (no session is recorded for A).
- **Root cause:** The `unstuck://focus/` deep-link branch calls `vm.startFocus(t)` (line 159). startFocus (AppViewModel.kt line 317) only early-returns when `cur?.taskId == task.id`; for a different task it calls `FocusTimer.start(base, ...)`, which for a non-matching taskId returns a brand-new session via `cur.copy(id = newId(), taskId = ...)` (FocusTimer.kt start()) and never calls finishFocus/upsertSession for the previous task, so A's elapsed time is dropped without a Session row.
- **Fix:** In the focus deep-link branch (or in startFocus), if a different task already has a live running session, finalize it first via finishFocus(prevTask, markDone=false) before starting the new one (or prompt the user).
- _verify (high): Confirmed real and reachable on current main. MainScaffold.kt:154-165 routes the starts-now notification deep link (unstuck://focus/$taskId, produced in NotificationRenderer.postTaskStarting line 98) to vm.startFocus(t) at line 159 with NO live-session check. AppViewModel.startFocus (line 317-328) r_

### [entry/correctness] Deep links & calendar callback re-fire on every config change (rotation / theme / locale)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:32, 99-120 (onCreate calls handleAuthOrCalendar(intent); intent is never cleared)`
- **Symptom:** After tapping a notification (capture / task / today / focus / collections) or returning from the Google-Calendar consent bounce, rotating the phone, switching dark mode, or changing locale re-opens the capture sheet / re-navigates to the deep-linked task, or re-runs the calendar connect — even though the user already dismissed it. For the calendar case the user may see a spurious 'connect failed'.
- **Root cause:** MainActivity declares no android:configChanges in the manifest (launchMode=singleTask only), so a config change destroys and recreates the Activity. onCreate unconditionally calls handleAuthOrCalendar(intent), and `intent` (getIntent()) still holds the original launch Intent with its VIEW data / EXTRA_OPEN_CAPTURE extra. Nothing clears or marks the intent as consumed, so the deep-link branches set graph.pendingDeepLink again (re-routing) and the calendar branch re-invokes graph.coordinator.completeGoogleConnect(code, state). Since the first connect already set pendingCalState=null, the second run skips the CSRF guard and re-submits the one-time, already-redeemed OAuth code, which fails (SyncCoordinator.kt:128-132).
- **Fix:** After handling a launch intent, clear it so recreation can't reprocess it — e.g. in handleAuthOrCalendar set `setIntent(Intent())` (or strip the data/extra) once a branch consumes it, and guard handleAuthOrCalendar with a savedInstanceState==null check in onCreate so only genuinely new intents are routed.
- _verify (high): Confirmed real and reachable on current main. AndroidManifest.xml (lines 24-30) declares MainActivity with launchMode="singleTask" and NO android:configChanges, so a rotation/theme/locale change destroys and recreates the Activity, re-running onCreate. onCreate (MainActivity.kt:32) unconditionally c_

### [entry/data] Exact-alarm system settings screen is opened on first launch, before any app UI
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:43, 76-89`
- **Symptom:** A brand-new user, the very first time they open the app, is immediately bounced out to the full-screen system "Alarms & reminders" settings page before they see onboarding or any app content. Feels broken / like the app crashed to settings.
- **Root cause:** onCreate() calls maybePromptExactAlarm() unconditionally before setContent{}. targetSdk=35 means canScheduleExactAlarms() is false by default on Android 12+, and the default reminderLeadMin=10 (SettingsStore.kt:62) is >0, so the guard at MainActivity.kt:80 passes and startActivity(ACTION_REQUEST_SCHEDULE_EXACT_ALARM) (line 85) fires on the first cold start with zero prior user engagement with reminders.
- **Fix:** Gate maybePromptExactAlarm() behind onboarding completion (graph.onboarded) and/or defer it until the user actually enables/uses a reminder, instead of calling it unconditionally in onCreate on first launch.
- _verify (high): Verified against current main. In MainActivity.kt, onCreate() calls maybePromptExactAlarm() unconditionally at line 43, before setContent{} at line 66. Tracing every guard inside maybePromptExactAlarm (lines 76-89) on a brand-new install / first cold start on Android 13+: (1) line 77 SDK_INT < S — f_

## P3 (73)

### [auth/correctness] Toggling Sign in ↔ Sign up keeps the stale error / success message
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:120 (message state at 57-58, rendered at 98)`
- **Symptom:** User mistypes a password, sees the red 'That email and password don't match…' error, then taps 'New here? Create an account' — the red error stays pinned on the sign-up form even though it has nothing to do with creating an account. Same in reverse (a sign-up 'Check your email' success line lingers after switching back to Sign in).
- **Root cause:** The mode toggle is `Modifier.clickable { signUp = !signUp }` — it flips only the `signUp` flag and never resets `message`/`messageOk`. `message` is a remembered state at line 57 that survives the mode switch, and the rendered Text at line 98 keeps showing it.
- **Fix:** In the toggle's onClick, also clear the banner: `Modifier.clickable { signUp = !signUp; message = null }` (and ideally reset `messageOk = false`).
- _verify (high): Confirmed real in AuthScreen.kt. The mode toggle at line 120 is `Modifier.clickable { signUp = !signUp }` — it flips only the `signUp` flag. `message` (line 57: `var message by remember { mutableStateOf<String?>(null) }`) and `messageOk` (line 58) are keyless `remember` states that survive recomposi_

### [auth/correctness] Magic-link / Forgot-password / mode-toggle links aren't disabled while a request is in flight
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:123 and 126 (magic link / forgot password); compare 101 and 109`
- **Symptom:** While a sign-in/sign-up is running (button shows '…'), the 'Email me a magic link instead' and 'Forgot your password?' rows stay tappable. Tapping them repeatedly (or in quick succession) fires multiple overlapping auth requests that race on the shared busy/message state and quickly trip Supabase's 'we can only send a few sign-up emails per hour' rate limit.
- **Root cause:** Those rows use a bare `Modifier.clickable { run { … } }` with no `enabled = !busy` guard, unlike the Sign-in/Create UButton (line 101, `enabled = !busy`) and the Google row (line 109, `clickable(enabled = !busy)`). Each tap calls `run{}` which launches an independent `scope.launch` on `rememberCoroutineScope`, so concurrent calls aren't serialized.
- **Fix:** Gate every action row on `busy`, e.g. `Modifier.clickable(enabled = !busy) { run(...) { … } }` for the magic-link and forgot-password links (and the mode toggle).
- _verify (high): Confirmed by reading AuthScreen.kt. The magic-link (line 123) and forgot-password (line 126) rows use a bare `Modifier.clickable { run(...) { ... } }` with NO `enabled = !busy`, in direct contrast to the Sign-in/Create UButton (line 101, `enabled = !busy`) and the Google row (line 109, `.clickable(e_

### [auth/correctness] Magic-link / reset / sign-in fire with an empty or whitespace-only email
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:123, 126 (and 103)`
- **Symptom:** Tapping 'Email me a magic link instead' or 'Forgot your password?' before typing anything sends `vm.magicLink("")` / `vm.resetPassword("")`, producing a server round-trip and a generic error instead of an instant 'enter your email' hint. A whitespace-only entry behaves the same after `.trim()`.
- **Root cause:** The click handlers call `run { vm.magicLink(email.trim()) }` / `vm.resetPassword(email.trim())` with no local check that `email.trim()` is non-empty; there is no client-side validation before the network call.
- **Fix:** Before invoking `run{}`, short-circuit when `email.trim().isEmpty()` (or `password.isEmpty()` for sign-in) and set `message`/`messageOk` to a local 'Enter your email first.' prompt.
- _verify (high): Confirmed by reading the full call chain. AuthScreen.kt:123 (`vm.magicLink(email.trim())`), :126 (`vm.resetPassword(email.trim())`), and :103 (`vm.signIn(email.trim(), password)`) are invoked inside `Modifier.clickable {}` handlers with no client-side non-empty/whitespace check. The buttons are tapp_

### [auth/ui] Auth text links (magic link / forgot password / mode toggle) are far below the 48dp touch-target minimum and crowded together
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:118-127`
- **Symptom:** The 'New here? Create an account', 'Email me a magic link instead', and 'Forgot your password?' links are ~18dp tall plain text rows separated by only 8dp gaps. They are hard to hit and easy to mis-tap (e.g. trigger a magic-link email when reaching for 'Forgot password'), which is worse because each tap fires an irreversible email send.
- **Root cause:** Each is a bare Text with Modifier.clickable and no minimum height/padding (UFont.sans(13)), stacked with Spacer(Modifier.height(8.dp)) between them (lines 120, 122-123, 125-126). The clickable hit area equals the glyph bounds (~18dp), well under the 48dp Material/accessibility minimum, with minimal vertical separation.
- **Fix:** Give each link a tappable area of at least 48dp (e.g. Modifier.clickable{...}.heightIn(min = 48.dp).padding(vertical = 12.dp) or wrap each in a min-height Box) and increase the inter-link spacing.
- _verify (high): Confirmed against /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt lines 118-127. All three links are bare Text composables styled with UFont.sans(13) (verified in design/src/main/kotlin/.../theme/Type.kt:43 — 13.sp, no built-in p_

### [auth/ui] Stale error/success message persists when toggling between Sign in and Sign up
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:120, 98`
- **Symptom:** After a failed sign-in (red 'That email and password don't match…') the user taps 'New here? Create an account'; the form switches to sign-up but the red error message remains shown, now unrelated to the create-account form. Same for a green success message carrying over into the other mode.
- **Root cause:** The mode toggle modifier `Modifier.clickable { signUp = !signUp }` (line 120) flips signUp but does not reset `message`/`messageOk`. The message Text (line 98) is driven solely by those state vars, which are only cleared at the start of the next run() call (line 64), not on mode change.
- **Fix:** In the toggle click handler, also clear state: `signUp = !signUp; message = null` (and reset messageOk).
- _verify (high): Confirmed in /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt. The mode toggle at line 120 (`modifier = Modifier.clickable { signUp = !signUp }`) only flips `signUp` and does not touch the remembered state vars `message` (line 57)_

### [auth/ui] Footer tagline is not anchored to the bottom — Spacer(weight(1f)) collapses to zero inside a verticalScroll Column
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:75, 128-132`
- **Symptom:** The closing tagline ('The anchor stays steady. You move around it.') is meant to sit pinned at the bottom of the screen but instead renders directly beneath the 'Forgot your password?' link with a large empty gap below it, so the layout looks unbalanced/unfinished on tall screens.
- **Root cause:** The root Column has Modifier.verticalScroll(rememberScrollState()) (line 75), which gives children an infinite max-height. Inside such a Column, Spacer(Modifier.weight(1f)) (line 128) resolves to 0 height (weight has no finite space to distribute), so it cannot push the footer down.
- **Fix:** Drop the verticalScroll-plus-weight combination — either remove verticalScroll and use weight to anchor the footer, or remove the weight Spacer and use a fixed Spacer / bottom alignment so the footer placement is deterministic.
- _verify (high): Confirmed against /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt. The cited lines match exactly: line 75 wraps the root Column in Modifier.fillMaxSize().verticalScroll(rememberScrollState()), and line 128 places Spacer(Modifier._

### [auth/ui] Google sign-in button and its logo expose no accessibility label for screen readers
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:107-115`
- **Symptom:** The 'Continue with Google' control is a Row, not a semantic button, and its logo Image has contentDescription = null. TalkBack announces the visible text but not a button role, and the Google mark is silent; the control is also less discoverable as an actionable element.
- **Root cause:** The clickable Row (lines 107-111) has no Modifier.semantics { role = Button } / contentDescription, and the Image (line 112) passes contentDescription = null. Decorative-null is fine for the logo only because the row itself never declares a button role or accessible name.
- **Fix:** Add Modifier.semantics(mergeDescendants = true) { role = Role.Button } (or use a real button) to the Row so it is announced as a 'Continue with Google' button.
- _verify (medium): Confirmed against AuthScreen.kt:107-115. The "Continue with Google" control is a Row with `.clickable(enabled = !busy) { run { vm.googleSignIn() } }` (lines 108-109) and never sets `Modifier.semantics { role = Role.Button }`. A repo-wide grep for `semantics`/`Role.Button`/`role =` found NO real Role_

### [auth/data] Auth error/success message persists when switching between Sign in and Sign up
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt:120 (signUp = !signUp toggle); message rendered at 98`
- **Symptom:** After a failed sign-in (e.g. red "That email and password don't match") the user taps "New here? Create an account"; the stale red error stays visible on the sign-up form. The same happens for a green success message (e.g. magic-link sent) carrying over after toggling.
- **Root cause:** The toggle Text onClick only does { signUp = !signUp } and never resets message/messageOk; message is hoisted at the screen level so it survives the mode switch and keeps rendering at AuthScreen.kt:98.
- **Fix:** Clear state on toggle: Modifier.clickable { signUp = !signUp; message = null }.
- _verify (high): Confirmed against /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/auth/AuthScreen.kt. `message` (line 57) and `messageOk` (line 58) are hoisted at the composable scope and survive recomposition. The message is rendered at line 98 — `message?.let { _

### [today/correctness] Live focus session with a missing/unhydrated task renders a blank Today body
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:139, 267, 281`
- **Symptom:** If a focus session is in progress but its task isn't in the tasks list — e.g. just after relaunch when liveSession() hydrates before tasks(), or after the task was deleted on another device while the session row lingers — Today shows the greeting and the sticky 'Today'/filter header but a completely empty list: no Start-Next hero, no live-session card, and no empty-state note. The running session is invisible and the screen looks broken.
- **Root cause:** empty (line 139) is gated on `live == null`, so a non-null live session forces the non-empty branch. But the live-session card only renders when `liveTask != null && live != null` (line 267), and liveTask = `tasks.firstOrNull { it.id == liveId }` (line 134) is null when the task is absent. The fallback empty note (line 281) is further gated on `(backlogActive || areaFilter != null)`, so with the default Today/All filter nothing at all is drawn in the body.
- **Fix:** Either show the live card / a generic 'Session in progress' affordance whenever `live != null` regardless of liveTask, or relax the line-281 empty-note condition so a non-empty branch with zero visible items always renders a note.
- _verify (high): The gating logic is exactly as claimed. At TodayScreen.kt:139 `empty = todayAll.isEmpty() && live == null && backlogAll.isEmpty() && startNext == null` — a non-null `live` forces the non-empty (else) branch. The live-session card at :267 is gated `liveTask != null && live != null`, and liveTask (:13_

### [today/correctness] Today block-date matching uses real wall clock while created/completed checks use the minute-snapshot 'now'
- **@** `core/src/main/kotlin/tech/csalliance/unstuck/core/logic/VisibleTasks.kt:59`
- **Symptom:** On a Today screen left open across local midnight, for up to ~60s the same-day cal_block matching and the 'created/completed today' filtering can disagree: a task scheduled for the new day can appear/disappear or a just-completed task can briefly drop out, until the next minute tick re-aligns them.
- **Root cause:** visibleTasks computes `val today = Clock.todayIso()` (line 59) from `System.currentTimeMillis()` directly, but isCreatedToday/isCompletedToday use the passed `now` param, which TodayScreen feeds from a once-per-60s ticker (TodayScreen.kt line 108-110). After midnight the two values reference different days until nowState ticks.
- **Fix:** Derive `today` from the passed `now` via `Clock.dateIso(now)` instead of `Clock.todayIso()` so all date logic in a single visibleTasks call uses one consistent instant.
- _verify (high): Confirmed in code. VisibleTasks.kt:59 derives `val today = Clock.todayIso()`, and Clock.todayIso() (Time.kt:64) = `dateIso(System.currentTimeMillis())` — the LIVE wall clock. The same visibleTasks call's TODAY/BACKLOG/ALL branches (lines 73, 80, 91) instead use the passed `now` param via isCreatedTo_

### [today/ui] "This week · Nh focused" pill silently drops the remaining minutes
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:172`
- **Symptom:** On Today, a user who has focused e.g. 90 minutes this week sees the header pill read "This week · 1h focused" — and 119 minutes also reads "1h focused". The reported number is wrong/under-counted; tapping through to Insights then shows a different, larger figure, which looks broken.
- **Root cause:** Line 172: `if (weekMin >= 60) "${weekMin / 60}h focused" else ...` uses integer division `weekMin / 60`, discarding the minute remainder entirely. The web component this is supposed to mirror (unstuck/components/dashboard/week-focus-stat.tsx lines 30-31) renders `${h}h${m ? \` ${m}m\` : ''} focused` → "1h 30m focused". Its own comment (line 3) says it mirrors "the Android Today 'This week · Nh focused' pill", confirming Android is the regressed side.
- **Fix:** Render hours+minutes like the web: when `weekMin >= 60`, show `"${weekMin / 60}h${if (weekMin % 60 != 0) " ${weekMin % 60}m" else ""} focused"`.
- _verify (high): Confirmed by reading TodayScreen.kt:172: `Text(if (weekMin >= 60) "${weekMin / 60}h focused" else "${weekMin}m focused", ...)`. `weekMin / 60` is Kotlin integer division, so weekMin=90 renders "1h focused" and weekMin=119 also renders "1h focused" — the minute remainder is silently dropped. `weekMin_

### [today/ui] Long task names hard-clip mid-character (no ellipsis) in Today rows and heroes
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:316, 365, 394, 215`
- **Symptom:** A task with a long name shows its text cut off abruptly mid-word with no trailing "…" — in the TaskRow title, the Start-Next pill ("Area · Name"), the live-session card title ("In focus · Name"), and the recap line ("N MIN FOCUSED · Name"). It reads like a missing character rather than deliberate truncation.
- **Root cause:** Every one of these Texts sets `maxLines = 1` but never sets `overflow`. Compose's default `TextOverflow.Clip` hard-cuts the glyphs; there is no `overflow = TextOverflow.Ellipsis`. (StartNextHero line 316, LiveSessionCard line 365, TaskRow line 394, recap line 215.) The web/CSS equivalents use text-overflow: ellipsis.
- **Fix:** Add `overflow = androidx.compose.ui.text.style.TextOverflow.Ellipsis` to each of these `maxLines = 1` Text calls.
- _verify (high): Confirmed all four cited Text calls in TodayScreen.kt set maxLines = 1 but never set overflow: recap line 213-216 ("${...} MIN FOCUSED · ${r.taskName}", mono(11), maxLines = 1), StartNextHero pill line 316 ("${task.lifeArea ?: "Focus"} · ${task.name}", maxLines = 1), LiveSessionCard title line 365 (_

### [today/ui] Recap / nudge dismiss and nudge action have sub-minimum touch targets
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:210, 230, 239`
- **Symptom:** The "✕" to dismiss the recap card (line 210) and the nudge card (line 239), and the nudge's action label (e.g. "Open", line 230), are tiny tap targets (~14-16dp tall) — easy to miss-tap, and below the 48dp accessibility minimum, so they are hard to hit for users with larger fingers or motor impairments.
- **Root cause:** Each is a bare `Text(...)` with `Modifier.clickable { ... }` and no enforced minimum size — the clickable area equals the glyph's intrinsic bounds at sans(12)/sans(13). No `Modifier.size(...)`/`minimumInteractiveComponentSize`/padding is applied to expand the hit area, and the ✕ marks also lack a contentDescription for screen readers.
- **Fix:** Wrap each action/dismiss in a sized, centered Box (e.g. `Modifier.size(40.dp)` or add `minimumInteractiveComponentSize()`) and give the ✕ a `contentDescription` like "Dismiss".
- _verify (high): Verified against current main in /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt. All three cited elements are bare Text(...) + Modifier.clickable with no enforced minimum size:

- Line 210 recap dismiss: `Text("✕", style = UFo_

### [today/ui] Live-session ring timer can overflow its 30dp circle for long sessions
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/today/TodayScreen.kt:362`
- **Symptom:** For a focus session that runs 100 minutes or longer, the MM:SS counter inside the small progress ring on the live-session card reads e.g. "100:00" and overflows / gets clipped by the 30dp circle, looking cramped or cut off.
- **Root cause:** Line 362 renders `Text(formatMMSS(elapsed), style = UFont.mono(7, ...))` with no `maxLines`/`overflow` inside a fixed 30dp `Box` (line 354) whose drawable interior is ~24dp after the 3dp ring stroke. `formatMMSS` (FocusTimer.kt:119) never rolls minutes into hours, so minutes grow unbounded ("100:00" is 6 glyphs ≈ wider than the ring's inner width at 7sp).
- **Fix:** For the in-ring label, show an hours-aware/compact format (e.g. "1h40" once elapsed ≥ 60 min) or constrain the Text so it can't overflow the 30dp ring.
- _verify (low): Verified the cited code. TodayScreen.kt:362 renders `Text(formatMMSS(elapsed), style = UFont.mono(7, FontWeight.Bold), color = c.ink2)` with no maxLines/overflow/softWrap override, centered inside `Box(Modifier.size(30.dp))` (line 354) over a 3dp-stroke ring (lines 355-360), so the ring's drawable i_

### [today/data] Notification-level change doesn't immediately hide/show Today nudge cards
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:242-248`
- **Symptom:** User opens Settings, switches Notifications to "Calm" (which is supposed to silence quiet in-app nudges), returns to Today within a few seconds, and the slipping-task nudge card is still showing. The reverse (Calm -> Balanced) also fails to surface nudges until something else changes.
- **Root cause:** The `nudges` StateFlow is `combine(tasks, captures, _dismissedNudges)` and reads the gate `graph.settings.load().notificationLevel.nudges` *inside* the combine lambda. `_settings` / notificationLevel is NOT one of the combined flows, so changing the level never re-triggers the combine. It only re-evaluates when tasks/captures/dismissed re-emit, or after the `WhileSubscribed(5_000)` window lapses and Today re-subscribes (i.e. after ~5s away). The code comment even admits it only reflects "the next time Today re-subscribes."
- **Fix:** Expose the notification level as a flow (e.g. derive from the existing `settings` StateFlow) and add it as a fourth source to the `combine` so a level change recomputes `nudges` immediately.
- _verify (high): Confirmed real by reading the code. In AppViewModel.kt:242-248, `nudges` is `combine(tasks, captures, _dismissedNudges)` and reads the gate `graph.settings.load().notificationLevel.nudges` INSIDE the lambda. Neither `_settings` nor `settings` (the StateFlow at line 629) is a combine source. `updateS_

### [today/data] Today recap card has no max-age expiry (web parity gap)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:364-372`
- **Symptom:** A "You did the thing." recap card set after a focus session stays on Today indefinitely (until manually dismissed or the process is killed), whereas on web it auto-disappears after 6 hours so a stale recap doesn't linger.
- **Root cause:** `_lastRecap` is a plain `MutableStateFlow<RecapState?>` with no timestamp and no age check; `RecapState(taskName, focusedSec)` carries no `at` field. The web reference (components/dashboard/recap-card.tsx, MAX_AGE_MS = 6h) drops the card once it's older than 6 hours. Severity is limited because the Android recap is in-memory only and clears on process death, so it can't linger across relaunch the way the persisted web value could.
- **Fix:** Add an `at` timestamp to `RecapState` and have `lastRecap` (or the TodayScreen consumer) null out the card once `now - at` exceeds a max age (match the web's 6h).
- _verify (high): Confirmed real, mild parity gap. RecapState (AppViewModel.kt:725) is `data class RecapState(val taskName: String, val focusedSec: Int)` — no `at`/timestamp field. `_lastRecap` (line 370) is a plain `MutableStateFlow<RecapState?>(null)`, written only in finishFocus (line 365) and cleared only by manu_

### [tasks/correctness] DatePicker in NewTaskSheet always opens on today, ignoring the already-picked date
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/NewTaskSheet.kt:245-247 (rememberDatePickerState initialSelectedDateMillis)`
- **Symptom:** After choosing a 'Pick date' value (or when the sheet was opened with a prefillDate from a calendar slot), reopening the date picker to adjust it shows today's date highlighted instead of the date the user previously selected, forcing them to re-navigate to their chosen day.
- **Root cause:** initialSelectedDateMillis is hardcoded to `java.time.LocalDate.now()...` and never seeded from the current `pickedDate` state; rememberDatePickerState only honors the initial value once, so the dialog always starts at today regardless of the prior selection or prefillDate.
- **Fix:** Seed initialSelectedDateMillis from the parsed `pickedDate` (its UTC-midnight epoch) instead of LocalDate.now(), falling back to today only when pickedDate is unset.
- _verify (high): Confirmed real. In /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/NewTaskSheet.kt line 246, initialSelectedDateMillis is hardcoded to java.time.LocalDate.now().atStartOfDay(ZoneOffset.UTC)... (today), and is never seeded from the pickedDate _

### [tasks/ui] Date picker always opens on today, ignoring the already-picked / prefilled date
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/NewTaskSheet.kt:245-247 (and 242-258)`
- **Symptom:** In the New-task sheet, after choosing 'Pick date' and selecting e.g. June 20, tapping the date chip again to adjust re-opens the calendar on today instead of June 20. If the sheet is opened from a future calendar slot (prefillDate), the date picker also starts on today rather than the prefilled day, so a small adjustment silently snaps the task back toward today.
- **Root cause:** rememberDatePickerState is initialized with a hardcoded `initialSelectedDateMillis = java.time.LocalDate.now()...` and never reads `pickedDate` or `prefillDate`. Because the state is created inside the `if (showDatePicker)` block, every open re-seeds to today.
- **Fix:** Seed initialSelectedDateMillis from `pickedDate` (parse its YYYY-MM-DD to UTC-midnight ms) falling back to today, so re-opening shows the currently selected date.
- _verify (high): Confirmed real defect on current main in NewTaskSheet.kt:242-258. The date picker state (lines 245-247) is hardcoded to `initialSelectedDateMillis = java.time.LocalDate.now().atStartOfDay(ZoneOffset.UTC)...` — today, UTC-midnight — and never references `pickedDate` (line 93) or `prefillDate` (param _

### [tasks/ui] 'Pick date' selected but cancelled leaves a stale/default date with no visible cue
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/NewTaskSheet.kt:137-143, 256, 107-112`
- **Symptom:** Tapping the 'Pick date' chip opens the date dialog AND immediately sets whenSel='Pick date'. If the user taps Cancel, the chip stays selected showing pickedDate's default (tomorrow, ...takeLast(5)) and effectiveDate resolves to that default date — the task gets scheduled for tomorrow even though the user never actually confirmed a date.
- **Root cause:** The chip onClick sets `whenSel = w` before the dialog resolves (line 139) and the dialog's dismissButton (line 256) only sets `showDatePicker = false` without reverting whenSel or signaling that no date was chosen; pickedDate defaults to tmrwIso (line 93).
- **Fix:** On date-dialog Cancel, if no date was previously confirmed revert whenSel to its prior value (e.g. back to 'Today'), or only flip whenSel to 'Pick date' inside the confirmButton after a date is actually selected.
- _verify (high): Mechanism confirmed in NewTaskSheet.kt. The "Pick date" chip onClick sets whenSel = "Pick date" eagerly (line 139) and opens the dialog (line 141) BEFORE any date is confirmed. The dialog's dismissButton/Cancel (line 256) only does `showDatePicker = false` — it never reverts whenSel nor flags that n_

### [tasks/data] TaskDetailSheet shows green 'Scheduled …' confirmation even when scheduleTask created no block
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/tasks/TaskDetailSheet.kt:78-89 (pickSchedule); label set unconditionally at 84, rendered at 134`
- **Symptom:** On a recurring task, pick a date that the recurrence doesn't cover for that day (today, or an off-pattern weekday). The detail screen shows 'Scheduled MM-DD H:MM' in green and the 'Schedule' meta-cell updates, but no block was actually created (see the recurring-scheduling bug above), so the calendar/Today are unchanged — a confusing false-success.
- **Root cause:** pickSchedule sets `scheduled = "…"` immediately after calling vm.scheduleTask(...) (line 82-84) without checking whether scheduleTask actually produced a block. scheduleTask is fire-and-forget (launchWrite) and, in the recurring branch, can legitimately create zero blocks for the chosen date.
- **Fix:** Once the underlying scheduleTask is fixed to always create a concrete block for the chosen date, this label becomes accurate; otherwise derive the confirmation from the resulting myBlocks (recomputed from vm.blocks) rather than optimistically stamping it on tap.
- _verify (high): Verified real and reachable on main. In TaskDetailSheet.kt pickSchedule (lines 78-89), line 84 unconditionally sets `scheduled = "${dateIso.takeLast(5)} ${formatTime(timeIso)}"` immediately after the fire-and-forget `vm.scheduleTask(task, dateIso, timeIso)` (line 82, which is `launchWrite{}` in AppV_

### [calendar/correctness] Block-edit 'Start time' chips never offer the block's own day outside 08:00–18:00, so early-morning / evening blocks can't be rescheduled within their own time band
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:350-363`
- **Symptom:** Open the edit sheet on a 7:00 AM or 8:00 PM block: the Start-time chips only show the current time plus mid-day slots; there is no way to nudge it to, e.g., 7:30 AM or 9:00 PM from the chips.
- **Root cause:** `findFreeSlotsForDate(blocks, live.durationMinutes, live.date, vm.nowMs(), limit = 5)` is called with the default `dayStartMin = 8*60` / `dayEndMin = 18*60` window (FreeSlots.kt), so candidate slots are clamped to business hours; only `live.startTime` is force-included via `(listOf(live.startTime) + slots).distinct()`.
- **Fix:** Pass a wider window (e.g. dayStartMin = 0, dayEndMin = 24*60, or the user's configured day bounds) into findFreeSlotsForDate for the edit sheet so chips cover the full day.
- _verify (high): Confirmed real. In DayGrid.kt:350, CalBlockEditSheet calls findFreeSlotsForDate(blocks, live.durationMinutes, live.date, vm.nowMs(), limit = 5) WITHOUT overriding dayStartMin/dayEndMin, which default to 8*60 and 18*60 in FreeSlots.kt:108-109. The Start-time chips are `(listOf(live.startTime) + slots_

### [calendar/correctness] 'Sync now' and 'Disconnect' give no busy/error feedback, unlike the Connect button
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:112-113`
- **Symptom:** Tapping 'Sync now' shows no spinner or result; if the pull fails (network/expired token) nothing is surfaced. 'Disconnect' likewise gives no confirmation or error.
- **Root cause:** `onClick { scope.launch { vm.syncCalendar() } }` and `conns.forEach { vm.disconnectCalendar(it.id) }` launch fire-and-forget coroutines with no `busy`/`error` state, whereas the Connect path correctly sets `busy`/`error`. pullCalendar()/disconnect swallow failures via runCatching+Log only.
- **Fix:** Wrap 'Sync now' and 'Disconnect' in the same busy/error pattern used by the Connect button so failures and in-progress state are visible.
- _verify (high): Confirmed at CalendarScreen.kt:112-113. 'Sync now' = `clickable { scope.launch { vm.syncCalendar() } }` and 'Disconnect' = `clickable { conns.forEach { vm.disconnectCalendar(it.id) } }` — both fire-and-forget, never touching the composable's `busy`/`error` state (declared lines 91-92, surfaced only _

### [calendar/ui] Disconnect Google Calendar is destructive and unconfirmed; nukes all connected accounts
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:110-114`
- **Symptom:** A single tap on the small "Disconnect" text immediately and irreversibly disconnects the calendar (server-side disconnect + purge of all external blocks) with no confirmation. If the user has connected more than one Google account, only one email is shown (`conns.first().accountEmail`) yet `conns.forEach { vm.disconnectCalendar(it.id) }` disconnects every account at once.
- **Root cause:** Line 113 wraps disconnect in a bare `clickable` with no confirmation dialog, and iterates `conns.forEach` so all connections are removed regardless of which account the user thinks they're acting on. The "Synced · <email>" label only renders `conns.first()`, hiding the fact that multiple accounts will be dropped.
- **Fix:** Gate Disconnect behind a confirm dialog and either show/disconnect per-connection or clearly label it as disconnecting all accounts.
- _verify (high): Confirmed against actual code. In CalendarScreen.kt line 113, "Disconnect" is a bare `clickable { conns.forEach { vm.disconnectCalendar(it.id) } }` — no AlertDialog/confirmation anywhere in ui/calendar/ (grep found none), and it iterates EVERY connection. Line 111 shows only `conns.first().accountEm_

### [calendar/ui] NOW-time pill is clipped above the grid in the early-morning hours
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/DayGrid.kt:277-282`
- **Symptom:** Between 00:00 and ~00:08 (and any time the grid is scrolled to the very top while NOW is near 0), the "NOW" coral pill is positioned with a negative y and gets cut off / clipped at the top edge of the scroll viewport, looking broken.
- **Root cause:** The pill uses `.offset(y = topDp - 8.dp)` (line 280) to vertically center it on the NOW line. When `nowMin` is small, `topDp` < 8.dp, so the offset is negative and the pill is pushed above the content's top where the scroll container clips it (no top padding/headroom).
- **Fix:** Clamp the pill's y offset to be non-negative (e.g. `(topDp - 8.dp).coerceAtLeast(0.dp)`) or add top headroom to the grid.
- _verify (medium): Confirmed on current main (HEAD aabbcc7), DayGrid.kt:277-282. The NOW pill is positioned with `.offset(y = topDp - 8.dp)` (line 280) where `topDp = HOUR_HEIGHT * (nowMin / 60f)` and `HOUR_HEIGHT = 56.dp` (line 58, 278). For nowMin 0..8 (00:00-00:08) topDp ranges 0dp..7.47dp, so `topDp - 8.dp` is neg_

### [calendar/ui] Calendar nav arrows and action chips have no content descriptions for TalkBack
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:152-154, 246-248; DayGrid.kt:184-186`
- **Symptom:** With TalkBack on, the previous/next month/week/day navigation controls announce only the raw glyphs "‹" and "›", and the "Sync now"/"Today" chips are plain Text — screen-reader users can't tell these are buttons or what they do; the day-grid prev/next arrows are likewise unlabeled.
- **Root cause:** These controls are bare `Text("‹"/"›")` with a `.clickable {}` modifier and no `Modifier.semantics`/`contentDescription`, `Role.Button`, or `onClickLabel`, so accessibility services read the literal punctuation character.
- **Fix:** Add semantics (Role.Button + a descriptive contentDescription/onClickLabel like "Previous week"/"Next month"/"Sync now") to each clickable arrow/chip.
- _verify (high): Confirmed by reading all three cited locations. CalendarScreen.kt:152/154 (WeekView prev/next week), 246/248 (MonthView prev/next month), and DayGrid.kt:184/186 (prev/next day) are all bare Text("‹"/"›") with a .clickable{} modifier (weekOffset--/++, ym.minusMonths/plusMonths, shiftDate(date,-1/1)) _

### [focus/correctness] Focus sound settings (start chime / overrun bell / completion) are dead toggles — no audio ever plays
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/SettingsStore.kt:93-95 (defined); ui/focus/FocusScreen.kt (no playback)`
- **Symptom:** In Settings the user can enable "Start chime", "Overrun bell", and "Completion sound", but none of them ever play — starting, overrunning, or completing a focus session is silent regardless of the toggles. The ambient-sound setting is likewise inert.
- **Root cause:** `soundStartChime`/`soundOverrunBell`/`soundCompletion` are referenced ONLY in SettingsScreen.kt (the toggle UI) and SettingsStore.kt (persistence) — grep finds no code that reads them to trigger playback. FocusScreen never plays a chime on start/overrun/done, and `surface/AmbientAudio.kt` (the only audio player) is defined but never started or stopped anywhere. The web app wires the equivalent settings through lib/use-sound.ts in use-focus-timer.ts, so this is a missing-parity gap, not a deliberate cut surfaced to the user.
- **Fix:** Wire FocusScreen to play the corresponding R.raw sound when `state` transitions into RUNNING/OVERRUN/done (gated on the three settings), and start/stop AmbientAudio from the focus lifecycle gated on the `ambient` setting.
- _verify (high): Verified against current main. The four focus-audio settings are dead. `soundStartChime`/`soundOverrunBell`/`soundCompletion` (SettingsStore.kt:57-59, 93-95, 116-118) and `ambient` (line 60/96/119) are read ONLY in SettingsStore (persistence) and SettingsScreen.kt:146-149 (toggle UI text "Start chim_

### [focus/correctness] ReflectSheet "Save" silently discards the session reflection
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/ReflectSheet.kt:48-54`
- **Symptom:** After finishing a session the "How did that land?" sheet lets the user pick a mood (flowed / OK / sticky / had to stop) and tap a prominent "Save" button, but the selection is thrown away — "Save" and "Skip" behave identically.
- **Root cause:** `sel` is local `remember` state that is never read after selection; both the Save box (line 52) and Skip text (line 50) just call `onDismiss()`. No callback receives `sel`, and `finishFocus`/the recap path never records a reflection mood. The labeled "Save" button implies persistence that does not exist (the web also does not persist it, so this is parity-consistent but still misleading).
- **Fix:** Either pass `sel` to a `vm.saveReflection(...)` write on Save (persisting the mood, e.g. on the Session row or a ReasonLog), or relabel/remove the "Save" affordance so it doesn't imply data is stored.
- _verify (high): Confirmed real and reachable on current main. In ReflectSheet.kt the local `sel` state (line 34) is only written (`sel = key`) and read for the radio-checked visual on line 48; it is never consumed. Both "Skip" (line 50) and the prominent filled "Save" pill (line 52) call the same no-arg `onDismiss`_

### [focus/correctness] Live notification briefly flashes "FOCUSING" when reopening a paused (save-for-later) session
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:70, 75-81`
- **Symptom:** Opening a task that was "Saved for later" (paused) momentarily shows the coral "FOCUSING · LIVE" running notification with a ticking chronometer before it snaps to the amber paused "Did you step away?" check-in.
- **Root cause:** `LaunchedEffect(sessionStart)` at line 70 calls `FocusTimerService.start(context, task.name, sessionStart)` using the default `paused = false`, so the service first builds the running notification. The separate `LaunchedEffect(live?.paused, sessionStart)` at line 75 only afterwards issues `update(paused = true)`. The two effects race on first composition, producing a visible running→paused flash.
- **Fix:** Pass the current paused state into the initial start call, e.g. `FocusTimerService.start(context, task.name, sessionStart, paused = live?.paused == true)`, mirroring the MainScaffold restore path.
- _verify (medium): Confirmed against the code. FocusScreen.kt:70 calls `FocusTimerService.start(context, task.name, sessionStart)` omitting `paused`, which defaults to `false` (signature FocusTimerService.kt:101 `fun start(..., paused: Boolean = false)`). In the service, the no-action `start` intent hits the `else` br_

### [focus/ui] Primary action Row (Capture / Pause·Resume / Done) can overflow and clip off-screen
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:159-167`
- **Symptom:** On a narrow phone or with a large system font scale, the three pill buttons in the bottom action Row are pushed past the screen edges ("Capture" and "Done" partially clipped), making them hard or impossible to tap.
- **Root cause:** The Row uses Arrangement.spacedBy(10.dp) with no fillMaxWidth/weights and no wrapping; each FocusBtn has 22.dp horizontal padding and text sized via UFont.sans(14)=14.sp (which scales with the user's fontScale). The intrinsic width of three padded sp-sized pills can exceed the available width, and a centered Row simply overflows its bounds with no scroll container.
- **Fix:** Constrain the action Row to fillMaxWidth() with Arrangement.SpaceBetween (or wrap the buttons / reduce per-button padding) so the three controls always fit within the screen width at large font scales.
- _verify (medium): Confirmed in FocusScreen.kt:159-167 and 219-224. The primary action Row uses `Row(horizontalArrangement = Arrangement.spacedBy(10.dp))` with NO fillMaxWidth, no weights, no horizontalScroll, no FlowRow/wrapping. Each FocusBtn (line 221) is a content-sized Box with `padding(horizontal = 22.dp)` (44dp_

### [focus/ui] Focus content has no scroll; COCKPIT captures + long name can clip bottom controls on short screens
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:109-183`
- **Symptom:** In COCKPIT treatment with a long task name and up to 3 captures (CapturesRail), on a short device or with large fonts the fixed-height content exceeds the column height; the weighted spacers collapse to 0 and the bottom 'Save for later / End for now' row can be pushed under the nav bar or clipped, with no way to scroll to it.
- **Root cause:** The outer Column is a fixed fillMaxSize layout using two Spacer(Modifier.weight(1f)) to vertically center content; there is no verticalScroll. When the sum of fixed children (chips + ring + name + estimate + timer + CapturesRail + two button rows) exceeds the viewport, the weighted spacers shrink to zero and the remaining overflow is clipped rather than scrolled.
- **Fix:** Wrap the Column content in Modifier.verticalScroll(rememberScrollState()) (keeping the weighted spacers for the non-overflow case) so all controls remain reachable when content is tall.
- _verify (high): Confirmed real. FocusScreen.kt:109 lays out content in Column(Modifier.fillMaxSize()...) with NO verticalScroll, using two Spacer(Modifier.weight(1f)) (lines 131, 157) for vertical centering. All text uses .sp sizes (Type.kt:41-43) so it scales with the OS font scale (large-font accessibility), and _

### [focus/ui] ReflectSheet mood selection is captured but silently discarded
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/ReflectSheet.kt:34, 48-54`
- **Symptom:** After a session, the 'How did that land?' sheet lets the user pick a mood (It flowed / OK / sticky / I had to stop) and tap a prominent 'Save' button, but the choice is never recorded — Save behaves identically to Skip, so the radio selection is meaningless to the user.
- **Root cause:** `sel` is local state (mutableStateOf) read only to render the selected radio; both the 'Save' button (line 52) and 'Skip' (line 50) just call onDismiss(), and ReflectSheet exposes no callback to persist `sel`. FocusScreen passes only `reflectElapsed` and an onDismiss that closes the screen — nothing consumes the mood.
- **Fix:** Either thread a callback (e.g. onSave: (mood: String?) -> Unit) that persists the selection via the view model, or, if reflection is intentionally display-only for now, disable/remove the 'Save' button affordance so it doesn't imply the choice is stored.
- _verify (high): Confirmed real. In ReflectSheet.kt:34 `sel` is a local `mutableStateOf<String?>` that is only written by the radio click (line 48: `sel = key`) and read only to highlight the selected row (line 48: `sel == key`). The "Skip" text (line 50) and the prominent "Save" button (line 52) both use `onClick =_

### [focus/data] Live-notification chronometer disagrees with the on-screen timer after reopening a partially-focused task
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/focus/FocusScreen.kt:70 (FocusTimerService.start with sessionStart) vs 95 (displayedElapsedSec)`
- **Symptom:** After "End for now" and reopening the same task, the in-app focus timer shows the cumulative total (prior + this session), but the ongoing notification's chronometer starts from 0 for the new session. The two visibly disagree by the prior accumulated minutes.
- **Root cause:** FocusScreen arms the foreground service with FocusTimerService.start(context, task.name, sessionStart) (line 70) and the service sets setWhen(startMs)+setUsesChronometer(true) (FocusTimerService.kt:86-87), so the notification only counts time since this session's sessionStart. The screen instead renders FocusTimer.displayedElapsedSec (line 95), which adds live.priorAccumulatedSec (seeded from task.totalFocused in startFocus, AppViewModel.kt:326). The notification has no knowledge of priorAccumulatedSec.
- **Fix:** Pass a chronometer base of (sessionStart - priorAccumulatedSec*1000) when arming/updating the service so the notification chronometer matches the displayed cumulative elapsed.
- _verify (high): Confirmed real on current main. Data flow verified end-to-end:

1. "End for now" path: AppViewModel.finishFocus(markDone=false) writes task.copy(totalFocused = task.totalFocused + elapsed) (AppViewModel.kt:352) then store.setLiveSession(null) (line 356). So prior focus accumulates on the task and th_

### [collections/correctness] "Just me" promote on a shared list still marks the item promoted for every member with no way to clear it
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:509-528`
- **Symptom:** On a shared collection, choosing "Just me" in the Move-to-task chooser strikes the item and shows "…'s on it / Promoted" to all members permanently — completing the resulting task never un-strikes it for anyone.
- **Root cause:** moveItemToTask computes loop = mode==LOOP && isShared(col); for SELF, loop=false so sourceCollectionId/sourceItemId stay null (no completion link), but markItemPromoted is still called unconditionally with done = if (loop) false else null, writing promoted=true to the shared JSONB via setItemPromotion. Because there is no source link, toggleDone/finishFocus never call share?.taskDone to flip promotedDone, so the shared item stays struck/"Promoted" forever.
- **Fix:** For a SELF promote on a shared list, either don't write the shared promotion flag (keep it local) or link the task (sourceItemId) so completion can clear the shared item's promoted state.
- _verify (high): Confirmed real and reachable on current main. In AppViewModel.kt:509-528, moveItemToTask computes loop = mode==LOOP && isShared(col). For the "Just me" path (CollectionDetailScreen.kt:238 passes PromoteMode.SELF on a shared list), loop=false, so addTask gets sourceCollectionId=null and sourceItemId=_

### [collections/correctness] "Shared with N" count in the share sheet includes pending invites, contradicting the detail header
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/ShareCollectionSheet.kt:129`
- **Symptom:** The share sheet says e.g. "Shared with 2" when only 1 person has actually joined and 1 invite is still pending, while the collection header shows "Shared with 1".
- **Root cause:** members here is the combined list (joined members + pending invites) returned by listMembers, and the label uses members.size. The detail screen header uses col.members.size (joined only, no pending), so the two counts disagree.
- **Fix:** Count only non-pending entries for the label (members.count { !it.pending }) or label it "Shared/invited" to match the combined list.
- _verify (high): Confirmed real, cosmetic. The two facts the claim relies on both check out on current main:

1) ShareCollectionSheet.kt:129 labels "Shared with ${members.size}" where `members` = vm.listCollectionMembers(...) -> CollectionShareClient.listMembers (line 112), which explicitly returns `members + pendin_

### [collections/ui] Promote-to-task time picker renders light in dark mode
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionDetailScreen.kt:110-117 (pickByTimeThen)`
- **Symptom:** When the user has dark mode on (Settings = Dark, or System+dark) and picks "Keep everyone in the loop" on a shared item, the "by" time TimePickerDialog pops up as a bright white/light Material dialog over the dark app — black-on-white in a dark UI, jarring and momentarily unreadable.
- **Root cause:** pickByTimeThen builds `android.app.TimePickerDialog(context, ...)` with no theme override. The native dialog inherits the Activity theme, which is hardcoded to `android:Theme.Material.Light.NoActionBar` in app/src/main/res/values/themes.xml (there is no values-night/themes.xml). The Compose UTheme can be dark while the platform Activity theme stays light, so the dialog is always light. The app's dark-mode preference (AppRoot.kt line 37, ThemePref.DARK) can force dark even when the system is light, guaranteeing the mismatch.
- **Fix:** Pass a dark dialog theme resId when UTheme.colors.isDark (e.g. `TimePickerDialog(context, android.R.style.Theme_Material_Dialog, listener, h, m, false)`), or wrap context in a ContextThemeWrapper with a DayNight dialog theme.
- _verify (high): Confirmed on current main. CollectionDetailScreen.kt:108-118 builds `android.app.TimePickerDialog(context, {listener}, now.hour, now.minute, false)` with the 5-arg constructor and NO theme resId, so the native dialog inherits the host theme. `context` is `LocalContext.current` (line 83) = the Activi_

### [collections/ui] Done checkbox / action icons are 18-19dp — below min touch target, mis-taps fall through to edit
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionDetailScreen.kt:287-291 (checkbox), 328/331/333 (Pin/AddTask/Close)`
- **Symptom:** Tapping the round done-checkbox on a collection item frequently does nothing or instead opens the inline text editor; the revealed Pin / Move-to-task / Remove icons are likewise hard to hit and a near-miss edits the row. Core actions (mark done, remove) feel unreliable.
- **Root cause:** The checkbox is `Box(Modifier.size(18.dp)...clickable{ toggleCollectionItemDone })` and each action icon is `Icon(...Modifier.size(19.dp).clickable{...})` — far below the 48.dp Android minimum touch target, with no `minimumInteractiveComponentSize`/padding. They sit inside the row's `combinedClickable` (line 277, onClick = edit), so any tap landing outside the tiny 18-19dp hit box is consumed by the parent and opens the editor instead.
- **Fix:** Give the checkbox and each action icon an expanded hit area (e.g. wrap in a 40-44dp clickable Box, or add `.minimumInteractiveComponentSize()`) so taps register on the control rather than falling through to the row's edit gesture.
- _verify (high): Verified in CollectionDetailScreen.kt. The done checkbox (line 287-291) is `Box(Modifier.size(18.dp).clip(CircleShape)...clickable { vm.toggleCollectionItemDone(col, item.id) })` and the revealed action icons (lines 328/331/333) are each `Icon(...Modifier.size(19.dp).clickable { ... })`. A grep conf_

### [collections/ui] No empty / no-results state in Collections grid
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:52-125 (shown filter + grid)`
- **Symptom:** Searching the collections list for text that matches nothing, or filtering to a state with zero results, shows only the header ("Things you don't need to remember" + search box) and then blank space — no "No matches" or "Nothing here yet" feedback. Looks like the screen failed to load.
- **Root cause:** The grid only emits header items plus `items(shown, ...)`. When `shown` is empty (search miss, or brand-new account with zero collections) there is no fallback composable — nothing is rendered below the header, and the result reads as a broken/blank screen.
- **Fix:** When `shown.isEmpty()`, render a centered empty-state span (distinct copy for "no collections yet" vs "no matches for <query>").
- _verify (high): Confirmed against CollectionsScreen.kt (/Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt). The LazyVerticalGrid body (lines 59-125) emits only: a span-2 header item (65-101: title + "A calm shelf" + search box + arch_

### [collections/ui] Share sheet shows "Not shared yet" even when member fetch errors
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/ShareCollectionSheet.kt:66-67 + 129-144 (refresh / members render)`
- **Symptom:** If the member-list request fails (offline, edge-function error) on an actually-shared collection, the sheet stops at "Loading…" only briefly then displays the SectionLabel "Not shared yet" with no members — telling the owner the collection has no collaborators when it really does, inviting a duplicate re-share.
- **Root cause:** `vm.listCollectionMembers` -> `CollectionShareClient.listMembers` wraps the call in `runCatching{...}.getOrDefault(emptyList())`, so a network/server failure is indistinguishable from a genuinely empty list. `refresh()` sets `loading = false` unconditionally, and the UI branches on `members.isNotEmpty()`, rendering the empty "Not shared yet" state on error.
- **Fix:** Have listMembers surface failure (e.g. return null or a Result), and render a distinct "Couldn't load members — retry" state instead of "Not shared yet" when the fetch failed.
- _verify (high): Confirmed by direct code reading. CollectionShareClient.listMembers (sync/.../CollectionShareClient.kt:112-125) wraps the entire share-collection edge-function invoke+deserialize in runCatching{...}.getOrDefault(emptyList()), so a network/server failure returns the same emptyList() as a genuinely em_

### [collections/ui] Optimistic member-remove is not reconciled — stays gone if unshare fails
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/ShareCollectionSheet.kt:86-91 (removeMember)`
- **Symptom:** Tapping the X next to a member/pending invite removes them from the list instantly; if the underlying unshare/cancel-invite request fails server-side, the member silently reappears only after the sheet is reopened (or never, until a collections refresh), so the owner believes access was revoked when it wasn't.
- **Root cause:** `removeMember` optimistically does `members = members.filterNot { it === m }` then launches `unshareCollection`/`cancelCollectionInvite` (both swallow errors via runCatching). There is no `refresh()` after the call and no failure path, so a failed revoke leaves the UI permanently out of sync with the server within the sheet's lifetime.
- **Fix:** After the unshare/cancel coroutine completes, call `refresh()` to reconcile against the server (and show an error if it failed) rather than trusting the optimistic removal.
- _verify (high): Confirmed in code. ShareCollectionSheet.kt:86-91 `removeMember` does `members = members.filterNot { it === m }` (purely optimistic) then `scope.launch { ... vm.cancelCollectionInvite / vm.unshareCollection }`, with no `refresh()` afterward and no failure branch. The underlying client methods Collect_

### [collections/ui] Grid cards have ragged heights within a row
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt:102-123 (items + card Column)`
- **Symptom:** Two collection cards side-by-side in the 2-column grid often have noticeably different heights (one with 2 preview lines + SHARED badge is taller than an empty one), giving the shelf an uneven, misaligned look.
- **Root cause:** Each card is `Column(...heightIn(min = 130.dp))` sized to its own content, and `LazyVerticalGrid(GridCells.Fixed(2))` does not equalize cell heights across a row — so neighbours diverge whenever their item counts / shared state differ.
- **Fix:** Pin a fixed card height (e.g. `Modifier.height(150.dp)` or `aspectRatio`) so paired cards align, letting the preview lines clip within a constant box.
- _verify (medium): Confirmed real cosmetic defect in /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/collections/CollectionsScreen.kt. The grid card (line 104-105) uses `Modifier.fillMaxWidth().heightIn(min = 130.dp)` — a MINIMUM height only, with no fixed `.height()_

### [notifications/correctness] Exact-alarm permission prompt skipped when global lead is off but start/drift nudges are on
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:80`
- **Symptom:** A user who sets the global "Remind me before tasks" to Off (but keeps Balanced/Coach, which still send "starts now" and "didn't get to it" alarms) is never prompted to allow exact alarms, so those start-time nudges fall back to inexact alarms and can fire minutes-to-hours late or be Doze-batched.
- **Root cause:** maybePromptExactAlarm() bails at line 80 with `if (graph.settings.load().reminderLeadMin <= 0) return`, treating global lead == 0 as "reminders off". But ATSTART/DRIFTED alarms (ReminderScheduler lines 82/84) and per-task lead overrides do not depend on reminderLeadMin, so they still get scheduled and still need exact-alarm permission.
- **Fix:** Gate the prompt on whether any alarm-driven moment is enabled (reminderLeadMin > 0 OR notificationLevel.atStart), not on the global lead alone.
- _verify (high): Confirmed real defect on current main. MainActivity.kt:80 gates the one-time exact-alarm permission prompt solely on `graph.settings.load().reminderLeadMin <= 0` — i.e. it treats global lead == Off as "no reminders, don't prompt". But ReminderScheduler.sync schedules alarms that do NOT depend on rem_

### [notifications/ui] Notification-center rows clip long text with no ellipsis
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/notifications/NotificationCenterScreen.kt:113-114`
- **Symptom:** A long task name (title) or a long body+timestamp (meta) in a Recent/Upcoming card is cut off abruptly mid-word with no '…', so the row looks visually broken/unfinished rather than gracefully truncated.
- **Root cause:** Both Text composables in the private Card() set `maxLines = 1` / `maxLines = 2` but omit `overflow`. Compose's default is `TextOverflow.Clip` (not Ellipsis), and `UFont.sans()` (Type.kt:43) is a bare TextStyle that sets no overflow either, so the line is hard-clipped.
- **Fix:** Add `overflow = TextOverflow.Ellipsis` to both Text calls in Card().
- _verify (high): Confirmed in NotificationCenterScreen.kt:113-114. Both Text composables in the private Card() set maxLines (1 for `title`, 2 for `meta`) but pass no `overflow` argument, so Compose uses its default TextOverflow.Clip — hard truncation with no ellipsis. The style helper UFont.sans(size, weight) (desig_

### [notifications/ui] External calendar-event reminders never appear in the in-app 'Upcoming' list
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/notifications/NotificationCenterScreen.kt:55-66`
- **Symptom:** The bell's 'Upcoming' section previews only scheduled tasks. External Google-Calendar events that the app DOES schedule lead reminders for (and which fire as real notifications) are silently absent, so the in-app preview disagrees with what the user will actually be reminded about.
- **Root cause:** The `upcoming` builder filters `blocks` with `.filter { isTaskBlock(it) }`, which excludes EXTERNAL blocks — yet ReminderScheduler.sync() (ReminderScheduler.kt:71-80) arms LEAD alarms for `b.kind == CalBlockKind.EXTERNAL` too. The two surfaces use different inclusion rules.
- **Fix:** Include external blocks (e.g. `isTaskBlock(it) || isExternalBlock(it)`) in the Upcoming filter so the preview matches the reminders that are actually scheduled, opening to Today for the non-task ones.
- _verify (high): Confirmed on current main (both cited files clean/committed). The Upcoming builder in NotificationCenterScreen.kt:55-66 filters blocks with `.filter { isTaskBlock(it) }`; per CalBlockKind.kt:17 isTaskBlock is true only for kind==TASK, so EXTERNAL blocks are excluded from the in-app preview. Meanwhil_

### [notifications/ui] Unread badge stays lit if a notification arrives while the center is open
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:115`
- **Symptom:** User opens the bell (badge clears), a new push/reminder lands while they're still on the Notifications screen, and on returning the red dot is still showing even though they just viewed the center — looks like an unread item they can't find at the top.
- **Root cause:** `openNotifs = { vm.markNotificationsSeen(); push(Route.Notifications) }` marks seen exactly once at open. NotificationLog.markAllSeen() stamps lastSeen at that instant; any entry added afterward has `at > seen` so notifUnread (AppViewModel.kt:289-292) recounts it as unread. Nothing re-marks seen on close or on new items while the screen is visible.
- **Fix:** Re-run markNotificationsSeen() when leaving the Notifications route (or via a LaunchedEffect keyed on the notifications list while the screen is shown) so items seen during the visit clear the badge.
- _verify (high): Confirmed real on current main. MainScaffold.kt:115 `openNotifs = { vm.markNotificationsSeen(); push(Route.Notifications) }` calls markNotificationsSeen() exactly once at open. NotificationLog.markAllSeen() (NotificationLog.kt:67-72) stamps _lastSeen = System.currentTimeMillis() at that instant. not_

### [notifications/data] Exact-alarm prompt skipped when lead reminders are Off, even though Balanced/Coach still schedule start-now & drift alarms
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:80`
- **Symptom:** A user who sets "Remind me before tasks" = Off but keeps Notifications at Balanced or Coach is never prompted to grant Alarms & reminders, so their "Time to start" (A2) and "Didn't get to it?" (A4) nudges fall back to inexact alarms and can fire minutes late or not at all under Doze.
- **Root cause:** maybePromptExactAlarm() returns early when graph.settings.load().reminderLeadMin <= 0 (MainActivity.kt:80), treating lead==0 as "reminders off". But ReminderScheduler.sync schedules ATSTART when level.atStart and DRIFTED when level.drifted regardless of reminderLeadMin (ReminderScheduler.kt:82-84), and setAlarm only uses setExactAndAllowWhileIdle when canScheduleExactAlarms() is true (ReminderScheduler.kt:104-106).
- **Fix:** Broaden the gate to also prompt when the notification level enables at-start/drift, e.g. `if (s.reminderLeadMin <= 0 && s.notificationLevel == NotificationLevel.CALM) return` (only skip when no exact-alarm-driven moment is enabled).
- _verify (high): Confirmed real and reachable on current main. MainActivity.kt:76-89 `maybePromptExactAlarm()` is the ONLY site in the app that requests ACTION_REQUEST_SCHEDULE_EXACT_ALARM (verified by grep across app/src/main — no other prompt exists). Line 80 returns early whenever `graph.settings.load().reminderL_

### [settings/correctness] Adding a duplicate tag silently clears the typed text without creating anything
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:425`
- **Symptom:** In Settings → Tags, typing a name that already exists (case-insensitively) and tapping Add wipes the input field and adds no tag, with no message — looks like the app ate the entry.
- **Root cause:** In the Tags Add handler the guard `if (nm.isNotBlank() && tags.none { ... })` only wraps the upsert, but `draft = ""` sits OUTSIDE the `if` (line 434), so the field is always cleared even when the add was rejected. The Areas equivalent (line 374) correctly clears `draft` only inside the success branch, so the two screens behave inconsistently.
- **Fix:** Move `draft = ""` inside the success branch (as Areas does), or surface a 'tag already exists' message so the rejection isn't silent.
- _verify (high): Confirmed by direct reading of SettingsScreen.kt. In TagsContent's Add handler (lines 425-435), the guard `if (nm.isNotBlank() && tags.none { it.name.equals(nm, ignoreCase = true) })` wraps only the upsert (line 432), while `draft = ""` is on line 434, OUTSIDE the if. So when a case-insensitively du_

### [settings/correctness] Delete-account confirmation is permanently un-clickable when the account has no email
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:253`
- **Symptom:** A user whose `currentEmail` is null/blank (e.g. certain OAuth identities) opens Delete my account, but the 'Delete forever' button stays disabled no matter what they type, so they can never delete their account from the app.
- **Root cause:** The confirm button enable condition is `email.isNotBlank() && typed.trim().equals(email, ignoreCase = true)` where `email = vm.currentEmail ?: ""` (line 242). With no email, `email.isNotBlank()` is false, permanently disabling the button; the dialog also instructs the user to 'Type your email to confirm', which they cannot satisfy.
- **Fix:** When `currentEmail` is null/blank, fall back to a different confirmation (e.g. type the word DELETE) so the delete path remains reachable.
- _verify (medium): The code matches the claim exactly. SettingsScreen.kt:242 sets `val email = vm.currentEmail ?: ""`, and line 253 gates the confirm button with `enabled = email.isNotBlank() && typed.trim().equals(email, ignoreCase = true)`. If `currentEmail` is null/blank, `email.isNotBlank()` is false, so "Delete f_

### [settings/ui] Color swatches and rename-confirm control have no content descriptions
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:329, 332, 338, 398, 401, 407`
- **Symptom:** With TalkBack on, the area/tag color swatch (which opens the palette and recolors) and the "✓" rename-confirm control are announced as unlabeled clickable boxes/text, so a screen-reader user cannot tell what they do. The kebab icons are correctly labeled ("Area options"/"Tag options"), making the omission inconsistent.
- **Root cause:** The swatch `Box(Modifier.clickable { palOpen = true }) { ColorChip(...) }` (lines 329, 398) and palette items (332, 401) carry no semantics, and the rename `Text("✓", ... .clickable { ... })` (lines 338, 407) is a bare clickable glyph with no contentDescription/role.
- **Fix:** Add `Modifier.semantics { contentDescription = ... }` (e.g. "Change color", color name, "Save name") to the swatch, palette swatches, and the ✓ control.
- _verify (high): Confirmed by reading SettingsScreen.kt and the ColorChip definition. The cited controls are all clickable (so TalkBack-focusable) but carry no accessibility label: (1) Line 329/398 — Box(Modifier.clickable { palOpen = true }) { ColorChip(...) } opens the palette; ColorChip (design/.../Components.kt:_

### [settings/ui] Kebab and color-swatch tap targets are well below the 48dp minimum
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:346, 413 (kebab), 329/398 (swatch)`
- **Symptom:** The "more options" kebab in each area/tag row is a 20dp hit target, and the color swatches are 30dp (areas) / 26dp (tags). On a phone these are easy to miss, especially the kebab, leading to repeated taps to open the menu.
- **Root cause:** The clickable is sized to the icon itself: `Icon(Icons.Filled.MoreVert, ... modifier = Modifier.size(20.dp).clickable { menu = true })` (lines 346, 413) and `Box(Modifier.clickable { palOpen = true }) { ColorChip(... box = 30/26 ...) }` (329, 398) — no minimum touch-target padding.
- **Fix:** Wrap the clickable in a >=48dp hit area (e.g. add `Modifier.size(48.dp)` with centered content, or `Modifier.minimumInteractiveComponentSize()`) on the kebab and swatch.
- _verify (high): Confirmed accurate against current main. In SettingsScreen.kt the kebab at lines 346 (areas) and 413 (tags) applies `.clickable { menu = true }` directly to `Modifier.size(20.dp)` on the Icon — a 20dp touch target, no IconButton wrapper, no padding, no minimum-size modifier. The color swatches at li_

### [settings/ui] "Signed in" account row shows a clickable ripple but does nothing
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:212`
- **Symptom:** In Settings → Account, tapping the "Signed in" row (which shows the user's email) gives a press ripple as if it were an action, but nothing happens — confusing since every other row in the card is a real action.
- **Root cause:** `SettingRow("Signed in", vm.currentEmail ?: "—") {}` passes an empty onClick, but `SettingRow` unconditionally applies `Modifier.clickable(onClick = onClick)` (line 449), so the row still renders an interactive ripple.
- **Fix:** Give `SettingRow` an optional `onClick: (() -> Unit)? = null` and only apply `.clickable` when non-null, then call this row with no handler so it reads as static info.
- _verify (high): Confirmed in code. SettingsScreen.kt:212 calls SettingRow("Signed in", vm.currentEmail ?: "—") {} with an empty no-op onClick. SettingRow (defined at lines 447-449) takes a non-nullable onClick and unconditionally applies Row(Modifier.fillMaxWidth().clickable(onClick = onClick)...). In Compose, clic_

### [settings/data] Account action error messages are rendered in success-green
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:218 (the `msg?.let { Text(it, ... color = c.green) }`), populated by 220/230/232/235/254`
- **Symptom:** When changing the password fails ("Current password incorrect.", "Can't verify your current password — no email is set on this account."), the display-name update errors, or account deletion errors, the message is shown in the same green used for "Password updated."/"Name updated."/"Exported." A failed action looks like a success.
- **Root cause:** The single `msg` String is rendered unconditionally with `color = c.green`. Both AuthOutcome.Error branches (`msg = r.message`, `msg = "Current password incorrect."`, the no-email branch) and the success branches write to the same `msg` with no error/success distinction at the render site.
- **Fix:** Track whether `msg` is an error (e.g. store a Boolean alongside it or an enum) and color the Text red on failure, green on success.
- _verify (high): Confirmed in SettingsScreen.kt AccountContent. Line 218 renders the single msg String unconditionally with color = c.green: `msg?.let { Text(it, style = UFont.sans(12), color = c.green, ...) }`. Error strings flow into that same msg with no error/success distinction at the render site: line 220 `msg_

### [settings/data] Export reports "Exported." even when nothing was written
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:184-185 (BackupContent) and 205-206 (AccountContent)`
- **Symptom:** If the chosen destination's output stream can't be opened (rare provider edge cases), the user sees the green "Exported." confirmation but the file is empty/absent — a silent data-export failure presented as success.
- **Root cause:** `context.contentResolver.openOutputStream(uri)?.use { it.write(...) }` uses `?.` so a null stream short-circuits to null without throwing; `runCatching {}` still resolves as success, so `.fold` runs the success branch (`msg = "Exported."`). Write success is never actually verified.
- **Fix:** Throw (or report failure) when `openOutputStream(uri)` returns null, e.g. `?: error("no stream")`, so the `runCatching` failure branch shows "Export failed."
- _verify (high): Confirmed real at app/src/main/kotlin/tech/csalliance/unstuck/ui/settings/SettingsScreen.kt:184-185 (BackupContent) and 205-206 (AccountContent), both identical: `runCatching { context.contentResolver.openOutputStream(uri)?.use { it.write(vm.exportJson().toByteArray()) } }.fold({ msg = "Exported." }_

### [settings/data] Notification-level mirror to server is fire-and-forget with no retry/outbox
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:639-650 (updateSettings); upsert in sync/.../Clients.kt:110-113`
- **Symptom:** If a user switches Notifications to "Calm" (or away from it) while offline or during a transient Postgrest error, the device-local level is saved but notification_preferences on the server is not updated. The server-driven morning brief and paused-checkin cap keep using the old level — e.g. the user set "Calm" but still receives the morning brief push. It only re-syncs the next time they change the level again.
- **Root cause:** The server mirror is `runCatching { graph.coordinator?.preferences?.setNotificationLevel(...) }` inside viewModelScope with no failure handling, no outbox enqueue, and no retry on reconnect. The local SettingsStore.save() always succeeds, so the two stores diverge on any network failure (the code comments label it "best-effort").
- **Fix:** Route the notification-level change through the offline outbox (or re-push the current level on the next successful sync/sign-in) so a failed upsert is retried instead of silently dropped.
- _verify (high): Confirmed on current main. In AppViewModel.kt:632-651, updateSettings() unconditionally commits the local store via settingsStore.save(next) (line 636), then mirrors the notification level to the server inside `if (uid != null) viewModelScope.launch { runCatching { graph.coordinator?.preferences?.se_

### [insights/correctness] Deep-dive Median session truncates each session to whole minutes before computing the median
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:125-126`
- **Symptom:** The "Median" stat in the Deep dive can read one minute lower than the equivalent web/iOS value (e.g. a session of 1m59s counts as 1m), and with an even number of sessions the chosen median value can differ from web because the sort happens on truncated minutes.
- **Root cause:** `val mins = sessions.map { it.actualSec / 60 }.sorted()` does integer division (truncation) on each session BEFORE sorting and picking `mins[mins.size / 2]`. The web reference sorts the raw `actualSec`, picks the middle, then does Math.round(medianSec/60) once — so web rounds and sorts on real seconds, Android truncates and sorts on whole minutes.
- **Fix:** Sort on raw actualSec, take the middle element, then display ((medianSec / 60.0).roundToInt()) so it matches the web rounding semantics.
- _verify (high): Confirmed against both codebases. Android InsightsScreen.kt:125-126: `val mins = sessions.map { it.actualSec / 60 }.sorted(); val median = if (mins.isEmpty()) 0 else mins[mins.size / 2]` — integer division truncates each session to whole minutes BEFORE sorting, then picks the middle, and line 131 di_

### [insights/correctness] Re-entry <5m percentage truncates instead of rounding (off-by-1% vs web)
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:128`
- **Symptom:** The "Re-entry <5m" percentage in the Deep dive is consistently 1% lower than the web/iOS figure for the same data (e.g. 2 of 3 fast re-entries shows 66% on Android, 67% on web).
- **Root cause:** `reentryPct = (reentry[0] * 100 / it)` is integer division, which floors the percentage. The web helper pctFastReentry uses Math.round((bins[0]/total)*100).
- **Fix:** Compute it as `(reentry[0] * 100.0 / it).roundToInt()` so it rounds like the web/iOS version.
- _verify (high): Confirmed real parity defect. InsightsScreen.kt:128 computes `reentryPct = reentry.sum().let { if (it == 0) 0 else (reentry[0] * 100 / it) }`. Both `reentry[0] * 100` and `it` are Int, so Kotlin performs integer division (floor/truncation). The web reference pctFastReentry (deep-dive.tsx:272-276) re_

### [insights/ui] "When focus happens" chart silently drops custom areas and shows a wrong legend
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:108 (chart) + core/.../logic/Analytics.kt:30-49 (weekdayAreaHours / DEFAULT_AREAS)`
- **Symptom:** On the Report tab, the 'When focus happens' stacked-bar chart and its legend always show exactly the 5 hardcoded English areas Work/Personal/Home/Health/Volunteering. A user who picked 'Family', 'Study' or 'Side project' in onboarding (those are real options in AREA_OPTIONS), renamed an area, or added a new one sees those areas missing from the bars AND sees legend chips (e.g. 'Volunteering') for areas they never created. Sessions on a non-matching area contribute zero hours, so the chart looks emptier than reality.
- **Root cause:** InsightsScreen passes the literal DEFAULT_AREAS constant to both StackedBars (line 108) for the legend and to weekdayAreaHours. In weekdayAreaHours (Analytics.kt:44) `val ai = areas.indexOf(area); if (ai < 0) continue` drops every session whose task.lifeArea isn't one of the 5 hardcoded strings. Onboarding (OnboardingScreen.kt:48) and renameLifeArea/upsertLifeArea (AppViewModel.kt:585-616) let users create arbitrary area names, so the hardcoded list rarely matches the user's actual lifeAreas StateFlow that the rest of the screen uses.
- **Fix:** Derive the area list from the user's actual lifeAreas (e.g. `lifeAreas.sortedBy { it.sortOrder }.map { it.name }`) and pass that single list to both weekdayAreaHours(...) and StackedBars(...) instead of the hardcoded DEFAULT_AREAS constant.
- _verify (high): Verified real and reachable on main. InsightsScreen.kt:108 passes the literal DEFAULT_AREAS constant to BOTH the bar data and the legend: `weekdayAreaHours(sessions, tasks)` uses the default `areas = DEFAULT_AREAS` (Analytics.kt:35), and `StackedBars(..., DEFAULT_AREAS, lifeAreas)` renders one legen_

### [insights/ui] Interruption / re-entry histograms have no x-axis, so the bars are unreadable
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:109, 152, 223-238 (Histogram)`
- **Symptom:** The 'When interruptions happen' (10 bars = minutes-into-session in 3-min bins) and 'How fast you come back' (12 bars = 5-min re-entry bins) charts render only a title and bare bars. There are no tick labels or units, so the user cannot tell what any bar's position means (is the tall bar early or late? 0 min or 50 min?).
- **Root cause:** The private Histogram composable (lines 224-238) draws only `Text(title)` plus a Row of weighted bars; it never renders the bin scale that interruptionBins(binMin=3) / reEntryDistribution(binMin=5) define in Analytics.kt. The bin width is meaningful but invisible.
- **Fix:** Add a small axis caption or min/max tick labels under the Histogram bars (e.g. '0 min … 30 min' for interruptions, '0 … 60 min' for re-entry) so the bins are interpretable.
- _verify (high): Confirmed accurate against current main. The private Histogram composable (InsightsScreen.kt:223-238) renders only Text(title) plus a Row of weighted Box bars (lines 230-235); it has no x-axis labels, tick marks, or unit caption — unlike sibling charts StackedBars (which has per-row day labels at li_

### [insights/ui] Deep-dive 'Captures' stat ignores the 5-session gentle threshold while its neighbors show '—'
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:131-136`
- **Symptom:** With fewer than 5 sessions, the Deep-dive stat grid shows '—' for Median, On estimate and Re-entry <5m (honouring the 'numbers stay gentle until then' ThresholdNote), but the adjacent 'Captures' card shows a real hard number. The 2x2 grid looks half-blanked / inconsistent and contradicts the threshold message just above it.
- **Root cause:** Lines 131/132/135 wrap their value in `if (enough) "…" else "—"`, but line 136 is `StatCard("Captures", "${captures.size}", …)` with no `enough` guard. The Report tab's StatCard captions (line 103 '${captures.size} captures') have the same ungated behavior.
- **Fix:** Gate the Captures value the same way as its neighbors: `if (enough) "${captures.size}" else "—"` (and consider doing the same for the capture counts used as captions on the Report tab).
- _verify (high): Confirmed against current main (commit aabbcc7, file clean). In InsightsScreen.kt the Deep dive 2x2 grid is rendered in the deep `else` branch at the item starting line 124 with NO `enough` guard around it (only the ThresholdNote on line 123 is conditional, and the grid renders right below it regard_

### [insights/data] Median session length truncates each session to whole minutes before taking the median
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/insights/InsightsScreen.kt:125-126`
- **Symptom:** The Deep-dive 'Median' session value can differ by ~1 minute from the web app for the same sessions.
- **Root cause:** Android does sessions.map { it.actualSec / 60 } — integer division truncates each session to whole minutes BEFORE sorting/taking the median, then displays it directly. The web computes the median on raw actualSec (sorted seconds), takes arr[floor(len/2)], and rounds once at display time: Math.round(medianSec / 60) (deep-dive.tsx:425-428,442). Per-session truncation plus the missing final round shifts the result.
- **Fix:** Take the median over actualSec (sorted seconds), then display (medianSec / 60.0).roundToInt() so the rounding happens once on the median, matching the web.
- _verify (high): Confirmed real by reading both implementations. Android InsightsScreen.kt:125-126: `val mins = sessions.map { it.actualSec / 60 }.sorted(); val median = mins[mins.size / 2]`, then displayed verbatim as "${median}m" (line 131). `actualSec` is an Int holding seconds (DbRowCodec.kt:104 `@SerialName("ac_

### [sync/correctness] Server hydrate wipes an offline-created task cal_block from the UI until the next successful flush
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/Hydrator.kt:77-84 (hydrateCalBlocks) and SyncDecision.mergeHydratedCalBlocks lines 34-39; triggered from SyncCoordinator.completeGoogleConnect line 136 and handle() lines 252-253`
- **Symptom:** Right after connecting Google Calendar (or any sign-in/sync) while offline edits are still queued, a task block the user just scheduled briefly disappears from the calendar, then comes back on a later sync.
- **Root cause:** hydrateCalBlocks builds `merged = remote + localExternal` and calls store.replace, which clears the table and reinserts only those rows. A locally-created TASK cal_block that is still pending in the outbox is in neither `remote` (not yet on the server) nor `localExternal` (isExternalBlock is false for kind=TASK), so the replace deletes it from the local store. The flush that runs first is best-effort: if it errors (offline/transient), hydrate still runs and drops the block. The outbox op survives, so it's eventually re-created — but the UI shows it vanishing in the meantime.
- **Fix:** Before the cal_blocks replace, also preserve local rows that still have a pending outbox op (or run hydrate's cal_blocks replace with preservePrefix-style protection for ids referenced by pending outbox ops), so unsynced optimistic blocks aren't wiped while their upsert is still queued.
- _verify (medium): The mechanism checks out exactly as described and all line citations are accurate. hydrateCalBlocks (Hydrator.kt:77-84) calls store.replace(CAL_BLOCKS, merged, ...) with no preservePrefix argument; LocalStore.replace defaults preservePrefix=null (LocalStore.kt:75), so RecordDao.replaceTable runs cle_

### [sync/ui] Race can create a duplicate external (g_) calendar block beside a task block
- **@** `sync/src/main/kotlin/tech/csalliance/unstuck/sync/SyncCoordinator.kt:160-165 (pullCalendar ownEventIds filter) + WriteThrough.kt:47-53`
- **Symptom:** On the calendar/day timeline the user can see the same scheduled item twice: once as the Unstuck task block and once as a duplicated Google 'g_' external block, until a later sync de-dupes it.
- **Root cause:** pullCalendar() builds ownEventIds from local TASK blocks that already have an externalEventId and filters those Google events out. But the externalEventId is only stamped on the block AFTER the async push(b) INSERT round-trips (WriteThrough.kt:49-51, after a suspend network call). If pullCalendar() runs before that stamp lands (e.g. SyncWorker/syncNow, or the pull in completeGoogleConnect racing the push), the just-inserted Google event isn't in ownEventIds, so externalEventToBlock() mints a g_ EXTERNAL block next to the task block — both then render on DayGrid.
- **Fix:** Also suppress duplicates by externalEventId already present on ANY local block (task or external) when filtering pulled events, and/or skip creating a g_ block whose summary+start matches a local task block lacking a stamped id yet, instead of relying solely on the stamped ownEventIds set.
- _verify (medium): The race is real in the code as written. pullCalendar (SyncCoordinator.kt:160-163) builds ownEventIds ONLY from TASK blocks that already carry a non-blank externalEventId. That id is stamped on the block by WriteThrough.kt:51, which runs AFTER the suspend INSERT round-trip `push(b)` at line 48 — so _

### [sync/ui] Connected-calendar bar only shows/handles the first connection
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/calendar/CalendarScreen.kt:110-113`
- **Symptom:** If a user connects more than one Google account, the bar shows 'Synced · <first account>' only — the second account's email is invisible, and there's no '+ Connect' affordance to add or distinguish accounts; 'Disconnect' silently removes all of them at once.
- **Root cause:** The connected branch hardcodes conns.first().accountEmail for the label and never iterates conns for display; the empty-state '+ Connect Google Calendar' is in the else branch so it disappears entirely once any connection exists. 'Disconnect' does conns.forEach { disconnectCalendar(it.id) }, removing every account with one tap.
- **Fix:** Render one row per connection (email + its own Disconnect), and keep an 'Add another account' affordance so multiple Google accounts are individually visible and manageable.
- _verify (high): Confirmed at CalendarScreen.kt:110-113. The connected branch hardcodes a single account: line 111 `Text("Synced · ${conns.first().accountEmail}")` renders only the first connection and never iterates `conns`. The "＋ Connect Google Calendar" affordance (lines 95-109) is inside `if (conns.isEmpty())`,_

### [chrome/correctness] Task-detail overlay calls pop() during composition when the task is missing
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:242-245`
- **Symptom:** If the open task is deleted (e.g. removed by a realtime sync or from another action) while its detail overlay is on top, the overlay mutates navigation state during composition; in rare timing it can flash an empty bg layer before popping.
- **Root cause:** `is Route.Detail -> { val t = tasks.firstOrNull{...}; if (t != null) TaskDetailScreen(...) else pop() }` invokes `pop()` (which does `stack.removeAt(...)` on a `mutableStateListOf`) directly inside the composable `when`, i.e. a snapshot-state write during composition.
- **Fix:** Move the missing-task pop into a `LaunchedEffect(route.taskId, t == null) { if (t == null) pop() }` instead of mutating the stack inline during composition.
- _verify (high): Confirmed on current main (HEAD aabbcc7) in app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt. The mechanics are exactly as claimed: line 86 declares `val stack = remember { mutableStateListOf<Route>() }` (a snapshot-backed SnapshotStateList); line 114 `fun pop() { if (stack.isNotEmpty(_

### [chrome/correctness] Inbox relative timestamps frozen at screen-open time
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/inbox/InboxScreen.kt:58, 102`
- **Symptom:** Capture rows in the Inbox show relative ages ('just now', '5m ago') that never advance while the screen stays open; a capture that says 'just now' still says 'just now' minutes later until the user leaves and returns.
- **Root cause:** `val now = remember { vm.nowMs() }` (line 58) snapshots the clock once and is reused for every `relPast(now - parseMillis(cap.at))` (line 102); nothing re-keys `now`, so it is stable across recompositions.
- **Fix:** Drive `now` from a periodically-updating source (e.g. `produceState` that re-reads `vm.nowMs()` on a ~30s ticker) so open-screen ages stay current.
- _verify (high): Confirmed real. In /Users/ahmadtambaya/Desktop/projects/unstuck_android/app/src/main/kotlin/tech/csalliance/unstuck/ui/inbox/InboxScreen.kt line 58, `val now = remember { vm.nowMs() }` uses an UNKEYED remember, so the lambda runs only on first composition and the value is cached for the composable's_

### [chrome/ui] AvatarMenu popup vertical offset is a fixed ~52dp guess that mismatches the Today header
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/components/AvatarMenu.kt:48-53`
- **Symptom:** When the account menu is opened from the Today tab, the dropdown card can appear slightly mis-anchored relative to the avatar (gap/overlap differs from the Tasks/Calendar/Lists tabs).
- **Root cause:** The popup uses a hardcoded y = statusBarPx + 52.dp.roundToPx() (line 52) assuming a ~52dp bar height, but the Today header is a custom Row (TodayScreen.kt:147, top=8.dp, 32dp avatar) whose avatar baseline differs from the shared AppBar’s 40dp avatar used on the other three tabs, so the same offset lands differently per tab.
- **Fix:** Anchor the menu to the avatar’s actual on-screen position (e.g. capture the avatar bounds via onGloballyPositioned and feed that into the Popup offset) instead of a fixed 52dp estimate.
- _verify (medium): Confirmed in code, though the claim's mechanism detail is slightly imprecise. AvatarMenu.kt:51-53 anchors a window-level Popup at Alignment.TopEnd with a hardcoded y = statusBarPx + 52.dp.roundToPx(). This single Popup is rendered once at the scaffold level (MainScaffold.kt:271, Sheet.Avatar), so it_

### [entry/correctness] Onboarding restarts from step 0 (and loses typed first task) on rotation
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/MainScaffold.kt:93 (var onboarding by remember { mutableStateOf(!vm.onboarded) }) + OnboardingScreen.kt:55-58`
- **Symptom:** A new user partway through onboarding who rotates the device (or toggles system dark mode) is bounced back to step 1 of 4, losing their picked life areas, the task they typed, and the selected focus treatment.
- **Root cause:** OnboardingScreen holds all state in plain `remember { mutableStateOf(...) }` (step, pickedAreas, firstTask, treatment), and MainScaffold gates it on `remember { mutableStateOf(!vm.onboarded) }`. Because MainActivity has no android:configChanges, a config change recreates the Activity and rebuilds the whole composition; `remember` (not rememberSaveable) re-initializes, and graph.onboarded is still false until finish() runs, so onboarding restarts at step 0.
- **Fix:** Use rememberSaveable for step / firstTask / treatment (and a saveable list for pickedAreas) in OnboardingScreen, or add android:configChanges="orientation|screenSize|uiMode" to MainActivity in the manifest.
- _verify (high): Confirmed real and reachable on main. (1) AndroidManifest.xml lines 24-42: the <activity .name=".MainActivity"> declaration has NO android:configChanges, so by default a rotation or system dark-mode toggle (uiMode) recreates the Activity. (2) MainActivity.kt:66-70 just calls setContent { AppRoot(gra_

### [entry/correctness] Exact-alarm system prompt is consumed permanently even if the system screen never opens
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:76-89 (maybePromptExactAlarm: prefs flag set before startActivity)`
- **Symptom:** If launching the 'Alarms & reminders' settings screen fails (OEM ROMs that don't honor ACTION_REQUEST_SCHEDULE_EXACT_ALARM, or it throws), the user is never shown the toggle, exact alarms stay denied, and reminders fire late/not at all — yet the app never prompts again.
- **Root cause:** The 'exactAlarmPrompted' SharedPreferences flag is written (prefs.edit().putBoolean(...).apply()) BEFORE the runCatching { startActivity(...) } that actually opens the system screen. If startActivity throws (caught and swallowed), the flag is already set, so the one-time prompt is burned without ever having shown anything.
- **Fix:** Only set the 'exactAlarmPrompted' flag inside the success path of the runCatching block (after startActivity returns without throwing), so a failed launch can be retried on a later cold start.
- _verify (high): Confirmed by reading MainActivity.kt:76-89. In maybePromptExactAlarm(), line 83 writes the one-shot flag — prefs.edit().putBoolean("exactAlarmPrompted", true).apply() — BEFORE the runCatching { startActivity(...) } block on lines 84-88 that actually launches the system "Alarms & reminders" screen. T_

### [entry/ui] First launch dumps the user into the system "Alarms & reminders" Settings page before they see the app
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:43, 76-89`
- **Symptom:** On a brand-new install (Android 12/13), the very first time the app is opened it immediately bounces the user out to the OS "Allow setting alarms & reminders" Settings screen, over the splash, before onboarding or sign-in is ever shown. Many users will just back out, confused.
- **Root cause:** onCreate() calls maybePromptExactAlarm() unconditionally (line 43), with no gate on onboarded/auth. The only guards inside are canScheduleExactAlarms() and reminderLeadMin <= 0 — but reminderLeadMin defaults to 10 (SettingsStore.kt:62), so the guard passes for every fresh install. On targetSdk 35 / Android 12-13 canScheduleExactAlarms() is false by default, so startActivity(ACTION_REQUEST_SCHEDULE_EXACT_ALARM) fires at app open.
- **Fix:** Gate maybePromptExactAlarm() (and the FCM/notif prompts) behind graph.onboarded so they only run after the user has completed onboarding, or defer it to the moment the user first enables/saves a reminder.
- _verify (high): The claim is PARTLY right but gets its central technical fact backwards. Verified facts: MainActivity.kt:43 calls maybePromptExactAlarm() unconditionally in onCreate with no onboarded/auth gate (graph.onboarded exists at AppGraph.kt:38 and gates onboarding in MainScaffold.kt:93, but is never referen_

### [entry/ui] Two system permission prompts stack on top of each other at first launch
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:34-43`
- **Symptom:** On first open the POST_NOTIFICATIONS runtime dialog and the exact-alarm Settings redirect are both triggered in the same onCreate, so the user is hit with the notification permission dialog and then (or simultaneously) thrown to the Alarms Settings page — a jarring "permission storm" before the app UI has rendered.
- **Root cause:** onCreate() launches notifPermission.launch(POST_NOTIFICATIONS) (line 37) and maybePromptExactAlarm() (line 43) back-to-back, both before setContent and both ungated by onboarding/auth state. targetSdk=35 means the POST_NOTIFICATIONS runtime prompt is active.
- **Fix:** Sequence these prompts behind a first-run/onboarding step (e.g. a single "enable reminders" screen) instead of firing both immediately in onCreate.
- _verify (high): Confirmed reachable on current main. In MainActivity.onCreate, line 37 launches the POST_NOTIFICATIONS runtime dialog and line 43 calls maybePromptExactAlarm(), which (lines 84-87) does startActivity(ACTION_REQUEST_SCHEDULE_EXACT_ALARM) — a full-screen Settings redirect. Both fire back-to-back, befo_

### [entry/ui] Start-Next Glance widget is always light — unreadable card in dark mode
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/surface/StartNextWidget.kt:41-55`
- **Symptom:** With the system in dark mode (or a dark wallpaper), the home-screen widget shows a bright off-white card (0xFFFAFAF7) with dark text — clashing with every other dark widget and looking broken/out of place on the user's dark home screen.
- **Root cause:** Content() hardcodes ink=Color(0xFF2A2A33), muted=Color(0xFF8A8A95) and background(Color(0xFFFAFAF7)) with no day/night branch. There is no GlanceTheme / Configuration.uiMode check anywhere in the file.
- **Fix:** Switch the widget background/text ColorProviders on night mode (e.g. ColorProvider(day=..., night=...) or read uiMode) so it follows the system theme like the rest of the app.
- _verify (high): Confirmed real. StartNextWidget.kt:41-44 hardcodes ink=ColorProvider(Color(0xFF2A2A33)), muted=ColorProvider(Color(0xFF8A8A95)) and background(Color(0xFFFAFAF7)). In Glance, ColorProvider(color: Color) and background(color: Color) produce FixedColorProviders that return the same value irrespective o_

### [entry/ui] exactAlarmPrompted flag is committed before the Settings activity is launched
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/MainActivity.kt:82-88`
- **Symptom:** On a device with no activity that handles ACTION_REQUEST_SCHEDULE_EXACT_ALARM (or where the launch is blocked), the user is never sent to the toggle AND is never re-prompted, so exact alarms stay off silently and task reminders fire late/never.
- **Root cause:** prefs.edit().putBoolean("exactAlarmPrompted", true).apply() (line 83) runs before the runCatching { startActivity(...) } (lines 84-88). If startActivity throws (ActivityNotFoundException) it's swallowed by runCatching, but the "already prompted" flag is already persisted.
- **Fix:** Only set exactAlarmPrompted = true inside the runCatching after startActivity succeeds (or in its success branch), so a failed launch can be retried on a later run.
- _verify (high): Confirmed in MainActivity.kt:82-88. Line 83 `prefs.edit().putBoolean("exactAlarmPrompted", true).apply()` runs UNCONDITIONALLY before `runCatching { startActivity(...) }` (lines 84-88). The `runCatching` swallows any thrown exception, so if `startActivity` throws ActivityNotFoundException (or the la_

### [entry/ui] Periodic SyncWorker has no network constraint
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/surface/SyncWorker.kt:24-28`
- **Symptom:** Every 30 minutes the background sync wakes even with no connectivity, does a guaranteed-to-fail syncNow() (swallowed by runCatching), and returns success — burning a wakeup/battery cycle for nothing and never retrying when network actually returns.
- **Root cause:** PeriodicWorkRequestBuilder<SyncWorker>(30, MINUTES).build() is enqueued with no Constraints (no setRequiredNetworkType(CONNECTED)); doWork() unconditionally attempts the network sync.
- **Fix:** Attach Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build() to the PeriodicWorkRequest so WorkManager only runs the sync when a network is available.
- _verify (high): Confirmed in SyncWorker.kt:24-28: `PeriodicWorkRequestBuilder<SyncWorker>(30, TimeUnit.MINUTES).build()` is enqueued with no `.setConstraints(...)`, and a repo-wide grep found NO `Constraints`/`setRequiredNetworkType` anywhere. doWork() (lines 15-19) unconditionally calls `runCatching { coordinator?_

### [entry/data] Glance widget snapshot only updates while AppViewModel is alive; stale recommendation persists across process death
- **@** `app/src/main/kotlin/tech/csalliance/unstuck/ui/AppViewModel.kt:270-284`
- **Symptom:** The home-screen "Start Next" widget can keep showing a completed/changed task. If the user finishes the recommended task via a notification action or another surface while the app process is dead, the widget keeps displaying the old task until the app is next foregrounded.
- **Root cause:** writeStartNext + StartNextWidget().updateAll are driven solely by the AppViewModel.init collector on tasks/blocks/liveSession (AppViewModel.kt:274-282). That collector only exists while AppRoot/AppViewModel is composed. SyncWorker.doWork (SyncWorker.kt:15-19) syncs data in the background but never recomputes pickStartNext or refreshes the widget snapshot, and the widget's own provideGlance just reads the last-written DataStore value.
- **Fix:** Recompute pickStartNext from the LocalStore and call writeStartNext + StartNextWidget().updateAll at the end of SyncWorker.doWork (and/or in the background sign-out/sync paths) so the widget stays current without the app being open.
- _verify (high): REAL defect, confirmed by reading the code. The only writers of the widget snapshot are the AppViewModel init collector (AppViewModel.kt:274-283): a grep for writeStartNext / StartNextWidget().updateAll across app/core/data/sync returns ONLY those two lines. That collector runs in viewModelScope.lau_
