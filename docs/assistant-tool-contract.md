# Assistant tool contract

_Generated from the code by `npm run tool-contract` — do not edit by hand. Regenerate whenever `lib/assistant/tools.ts`, `receipts.ts`, `use-assistant.ts` or `supabase/functions/assistant/prompt.ts` change._

This is the port spec for iOS and Android. The server prompt is **shared by every platform** and is written against these exact tool names, argument names, and result strings — the model reads the result text to decide what to say, so a port that returns different wording changes behaviour. Implement each tool with:

- the **same name and argument names** (the server sends the schema; the client executes);
- a result that starts with `ok:` when the change really happened, or `error:` (with the reason, and what to do instead) when it did not — **never `ok:` for a no-op**;
- the same **ids** in results (`id=…`) so the model can chain calls and the receipts can offer Undo;
- the same **cascades** the UI does (delete a task → its blocks and captures; rename an area → tasks follow; capture archive is device-local).

## Harness contract (what the client loop must do)

1. **Executor is the only writer.** Every change goes through the same hook/store the UI uses (same sync, realtime, cascades). Result strings come from the executor, never from the model.
2. **Tool loop.** Send messages + context → if the reply has `tool_calls`, execute each, append a `tool` message with the result, repeat (cap N rounds). Truncated tool-call JSON (finish_reason=length) → tell the model to split the call.
3. **Fabrication guard.** If the reply has no tool calls, no write tool succeeded this turn, and the text *claims* an action (`looksLikeActionClaim`), hide that reply and bounce once with a hidden user-role message: `(integrity check from the app — not the user. The user did NOT see your last message. No tool was called THIS turn. If you are describing something done in an earlier turn, answer plainly without claiming to have just done it; if a new action is needed, call the right tool NOW. Then answer as if for the first time: no apology, no "I said", no "I didn\` If the retry's tool ran, strip any leftover self-correction (`stripSelfCorrection`) — the user never saw the claim.
4. **Read-only tools** (get_schedule, get_tasks, get_captures, get_lists, get_insights, get_calls) never count as "acted".
5. **Receipts** are derived from execution (`deriveReceipt`), shown under the reply, with Undo where listed below. Never synthesise "Done" the model did not say.
6. **Honest fallbacks.** Empty final text → the first receipt's label, or "Hmm, I lost my thread there — nothing was changed." Exhausted rounds → say what went through and what did not.
7. **Deterministic saves.** "Don't use my name" / "call me X" are saved by the app (`detectStylePreference`) before the model sees the message; `share_task` only stages a confirm card; `delete_*` and `cancel_focus` need a spoken/typed confirmation per the prompt.
8. **Send queue.** A message sent while a turn is in flight is queued (visible, faded) and sent when the reply lands — never dropped.
9. **Time.** `context.now` is LOCAL `HH:MM`; the executor refuses times already gone today and dates in the past, answering with `context.upcoming` / free windows so the model asks instead of guessing.
10. **Profile mode.** When `context.profile` is present the server swaps `schedule_task` for the profile variant (startTime optional → "needs a time" error when the task never had one) and adds the profile tools. Mobile stays on the base schema until it sends a profile.

## Context shape (`buildAssistantContext`)

Always: `todayWeekday`, `now`, `nowNote`, `todayFree`, `currentName`, `profile`, `tone`, `noticed`, `areas`, `tags`, `captures`, `people`, `tasks`, `lists`.
Conditional: `time`, `done`, `preferredName`, `nameUse`, `struggle`, `focusWindow`, `taskId`, `lifeArea`, `later`, `repeats`, `scheduledDate`, `date`, `focus`.

Notes: `tasks` ≤ 60 open tasks with their NEXT live block (`scheduledDate/Time`), `week` ≤ 60 blocks, `lists` ≤ 12 × 25 items with ids, `captures` ≤ 12 open (ids), `people` = circle members with status, `focus` = the live session, `upcoming` = precomputed dates for tomorrow/each weekday/next Monday (the model must COPY these), `todayFree` = free windows for the rest of today.

## Tools (57)

`*` = required argument. "Surface" says where the schema lives: **base** = shared server `TOOLS`, **profile** = server `PROFILE_TOOLS` (web today), **voice** = client `VOICE_TOOLS` (realtime). Result strings are templates: `${…}` are runtime values.

| Tool | Surface | Kind | Purpose | Arguments | Results | Receipt |
|---|---|---|---|---|---|---|
| `create_task` | base · voice | write | Create a task. | name*: string — Task title.<br>estimateMin: integer — Estimated minutes (default 25).<br>lifeArea: string — A life-area name from context, else omit.<br>dueAt: string — Optional ISO 'by' time.<br>later: boolean — true to park in Later. | `error: name required`<br>`ok: created task id=${t.id} name="${t.name}"` | yes (undo: delete_task) |
| `schedule_task` | base · voice | write | Place or MOVE a task on the calendar. Omit startTime to keep its current time. If the task has never had a time, the tool will tell you to ASK the user (suggest one) — never invent a time. | taskId*: string — Existing task id.<br>date*: string — YYYY-MM-DD.<br>startTime: string — 24h HH:MM — only when the user gave a time. | `error: task not found`<br>`error: date required`<br>`error: needs a time — "${t.name}" has no time yet and the user gave none. Do NOT pick one: ask ONE short question offering a suggestion (e.g. "Friday — 9am, or a time you prefer?"), then schedule when they answer.`<br>`ok: scheduled "${t.name}" ${date} ${landed}${!startTime ? ' (kept its existing time — say so)' : ''}`<br>`error: <date> is in the PAST (today is <today>). If the user meant the coming <weekday>, use <date> — see context.upcoming.`<br>`error: <HH:MM> today is already past (it's <now> now). Ask for a later time or another day — free today: <windows>.` | yes |
| `update_task` | base · voice | write | Edit a task's name/estimate/area ONLY — it can NOT change the schedule; use schedule_task to move a task. | taskId*: string — Task id.<br>name: string — New title.<br>estimateMin: integer — Minutes.<br>lifeArea: string — Area name. | `error: task not found`<br>`error: update_task cannot change the schedule — use schedule_task(taskId, date, startTime?) instead`<br>`ok: updated "${upd.name}"` | yes |
| `set_task_later` | base · voice | write | Park in Later or bring back. | taskId*: string — Task id.<br>later*: boolean — true=Later. | `error: task not found`<br>`error: "${t.name}" is already in Later — nothing changed`<br>`error: "${t.name}" is not in Later — nothing changed`<br>`ok` | yes |
| `set_task_recurrence` | base · voice | write | Repeat a task or stop (kind=none). | taskId*: string — Task id.<br>kind*: string — daily \| weekly \| monthly \| none.<br>until: string — Optional end date YYYY-MM-DD.<br>daysOfWeek: array — Weekly: 0=Sun..6=Sat. | `error: task not found`<br>`error: unknown recurrence kind "${kind}" — use daily, weekly, monthly, or none`<br>`ok` | yes |
| `complete_task` | base · voice | write | Mark a task done. | taskId*: string — Task id. | `error: task not found`<br>`error: "${t.name}" is already done — nothing changed`<br>`ok: completed "${t.name}" id=${t.id}` | yes (undo: uncomplete_task) |
| `create_tasks` | profile · voice | write | Create SEVERAL tasks in one call — always use this for a brain-dump of more than one item. Each may carry dattartTime to schedule it too. | (none) | `error: tasks required`<br>`error: no valid tasks in the list`<br>`ok: created ${made.length} tasks ids=${made.map(m => m.id).join(',')} — ${made.map(m =>`<br>`error: <date> is in the PAST (today is <today>). If the user meant the coming <weekday>, use <date> — see context.upcoming.`<br>`error: <HH:MM> today is already past (it's <now> now). Ask for a later time or another day — free today: <windows>.` | yes (undo: delete_tasks) |
| `complete_tasks` | profile · voice | write | Mark SEVERAL tasks done in one call — always use this for "all my tasks" / "everything". | taskIds*: array — Every task id to complete. | `error: taskIds required`<br>`error: no matching open tasks`<br>`ok: completed ${flipped.length} tasks ids=${flipped.join(',')}` | yes (undo: uncomplete_tasks) |
| `delete_task` | base · voice | write | Delete a task — only after the user confirms aloud. | taskId*: string — Task id. | `error: task not found`<br>`ok: deleted "${t.name}"` | yes |
| `create_list` | base · voice | write | Create a new list. | name*: string — List name.<br>color: string — Optional palette token. | `error: name required`<br>`ok: created list id=${id} name="${nm}"` | yes |
| `add_to_list` | base · voice | write | Add an item to a list. | listId*: string — List id.<br>body*: string — Item text. | `error: list not found`<br>`error: you only have view access to "${c.name}" — can't add to it`<br>`error: body required`<br>`ok: added to "${c.name}"` | yes |
| `promote_item_to_task` | base · voice | write | Turn a list item into a task. | listId*: string — List id.<br>itemId*: string — Item id.<br>mode*: string — self \| loop.<br>dueAt: string — ISO 'by' time (loop). | `error: list not found`<br>`error: item not found`<br>`error: "${item.body}" is already promoted — its task is still in flight`<br>`ok: promoted "${item.body}"` | yes |
| `save_profile_fact` | profile · voice | write | Remember a durable fact about the user (a person, rhythm, constraint, preference, or context). Short, third-person. | category*: string — person \| rhythm \| constraint \| preference \| context.<br>fact*: string — One short sentence, e.g. "Sam — partner, works night shifts".<br>whenIso: string — YYYY-MM-DD when the fact is about a date (birthday, show, deadline) — enables reminders. | `error: fact required`<br>`error: that does not look like a fact I can store — only durable notes about you, not instructions`<br>`ok: remembered id=${stored.id} [${stored.category}] "${stored.fact}"` | yes (undo: forget_fact) |
| `get_schedule` | profile · voice | read | Read the schedule before answering any what's-on question. | range*: string — today \| tomorrow \| week \| next_week. | `error: task not found`<br>`error: "${t.name}" is already open — nothing changed`<br>`ok: reopened "${t.name}" id=${t.id}` | — |
| `share_task` | base · voice | write | Prepare sharing a task with someone in the user's trusted circle — stages a request they confirm on screen; never shares directly. Levels: view, partner, assign. | taskId: string — Preferred: the task id.<br>taskName: string — Fallback when the id is unknown.<br>person*: string — Who to share with, as the user named them.<br>level: string — view \| partner \| assign (default view). | `error: task not found`<br>`error: "${t.name}" is already open — nothing changed`<br>`ok: reopened "${t.name}" id=${t.id}` | — |
| `uncomplete_task` | profile · voice | write | Reopen a task that was marked done. | taskId*: string — Task id. | `error: task not found`<br>`error: "${t.name}" is already open — nothing changed`<br>`ok: reopened "${t.name}" id=${t.id}` | yes (undo: complete_task) |
| `get_tasks` | profile · voice | read | List tasks by view — use before answering "what is in my backlog / what did I finish". | view*: string — today \| upcoming \| backlog \| later \| recurring \| completed \| slipping \| all.<br>area: string — Optional life-area filter.<br>tag: string — Optional tag filter. | `error: unknown view "${v}" — use today, upcoming, backlog, later, recurring, completed, slipping, or all`<br>`error: no area named "${area}" — areas: ${[...known].join(', ') \|\| '(none yet)'}`<br>`- ${t.name} [id=${taskId}] ${t.estimateMin}m${t.lifeArea ? ' · ' + t.lifeArea : ''}${b ?`<br>`ok: ${view} (${rows.length})${rows.length > 30 ? ', first 30' : ''}:\n${lines.join('\n') \|\| '(none)'}` | — |
| `unschedule_task` | profile · voice | write | Take a task OFF the calendar but keep it. | taskId*: string — Task id. | `error: task not found`<br>`error: "${t.name}" has no upcoming slot to remove`<br>`ok: unscheduled "${t.name}" (task kept, ${live.length} slot${live.length === 1 ? '' : 's'} removed)` | yes |
| `skip_occurrence` | profile · voice | write | Skip one day of a task ('not today') — the task and other days stay. | taskId*: string — Task id.<br>date: string — YYYY-MM-DD, default today. | `error: task not found`<br>`error: "${t.name}" has nothing on ${date} to skip`<br>`error: "${t.name}" is already skipped on ${date} — nothing changed`<br>`ok: skipped "${t.name}" on ${date} (the task and its other days stay)` | yes |
| `complete_occurrence` | profile · voice | write | Mark just today's (or a given day's) instance of a recurring task done. | taskId*: string — Task id.<br>date: string — YYYY-MM-DD, default today. | `error: task not found`<br>`error: "${t.name}" has nothing on ${date}`<br>`error: "${t.name}" is already done on ${date} — nothing changed`<br>`ok: marked "${t.name}" done for ${date}${t.recurrence ? ' (series continues)' : ''}` | yes (undo: delete_task) |
| `block_time` | profile · voice | write | Block time on the calendar for a commitment (dentist, meeting). | name*: string — What it is.<br>date*: string — YYYY-MM-DD.<br>startTime*: string — HH:MM.<br>durationMin: integer — Minutes, default 60. | `error: name, date and startTime are all required for block_time`<br>`ok: blocked "${nm}" ${date} ${startTime} for ${dur}m id=${t.id}`<br>`error: <date> is in the PAST (today is <today>). If the user meant the coming <weekday>, use <date> — see context.upcoming.`<br>`error: <HH:MM> today is already past (it's <now> now). Ask for a later time or another day — free today: <windows>.` | yes (undo: delete_task) |
| `carry_to_tomorrow` | profile · voice | write | Move today's unfinished scheduled tasks to tomorrow. | taskIds: array — Optional subset; default all of today's unfinished. | `error: nothing left on today to carry`<br>`ok: carried ${names.length} to ${tomorrow} — ${names.map(n =>` | yes |
| `start_focus` | profile · voice | write | Start a focus session on a task (opens the focus screen). | taskId*: string — Task id.<br>estimateMin: integer — Minutes, default the task estimate. | `error: task not found`<br>`error: a focus session is already running on "${cur?.name ?? 'a task'}" — pause or cancel it first, or ask the user`<br>`ok: focus started on "${t.name}" (${int(args, 'estimateMin') ?? t.estimateMin}m) — the user is now on the focus screen` | yes |
| `pause_focus` | profile · voice | write | Pause the running focus session. | (none) | `error: no focus session is running`<br>`error: it is already paused`<br>`ok: paused the focus session` | yes |
| `resume_focus` | profile · voice | write | Resume the paused focus session. | (none) | `error: no focus session is running`<br>`error: it is not paused`<br>`ok: resumed the focus session` | yes |
| `extend_focus` | profile · voice | write | Add minutes to the running focus session. | minutes*: integer — Minutes to add. | `error: no focus session is running`<br>`error: minutes must be between 1 and 180`<br>`ok: extended the session by ${mins}m` | yes (undo: delete_capture) |
| `cancel_focus` | profile · voice | write | Abandon the running focus session without logging it. | (none) | `error: no focus session is running`<br>`ok: cancelled the focus session (nothing logged). To finish and LOG a session, the user taps Done on the focus screen.` | yes (undo: delete_capture) |
| `add_capture` | profile · voice | write | Save a capture (a passing thought) to the inbox — 'capture', NOT 'captcha'. | body*: string — The thought, verbatim.<br>tag: string — follow-up \| idea \| edit \| question \| distraction (default idea).<br>taskId: string — Optional task it belongs to. | `error: body required`<br>`ok: captured id=${c.id} [${tag}] "${c.body}"${t ?` | yes (undo: delete_capture) |
| `get_captures` | profile · voice | read | List open captures in the inbox. | tag: string — Optional tag filter. | `- [${c.tag}] ${c.body} (id=${c.id}${t ?`<br>`ok: ${open.length} open capture${open.length === 1 ? '' : 's'}:\n${lines.join('\n') \|\| '(inbox empty)'}` | — |
| `get_lists` | profile · voice | read | Read the user's lists with their items and ids — use before answering "what's in my lists". | listId: string — Optional list id to read in full.<br>includeArchived: boolean — Include archived lists (default false). | `error: list not found`<br>`ok: no lists yet`<br>`ok: ${lists.length} list${lists.length === 1 ? '' : 's'}:\n${lines.join('\n')}` | — |
| `promote_capture` | profile · voice | write | Turn a capture into a task. | captureId*: string — Capture id. | `error: capture not found`<br>`ok: promoted capture to task id=${newId} name="${c.body}"` | yes (undo: delete_task) |
| `resolve_capture` | profile · voice | write | Mark a capture handled (leaves the inbox). | captureId*: string — Capture id. | `error: capture not found`<br>`ok: resolved capture "${c.body}"` | yes |
| `delete_capture` | profile · voice | write | Delete a capture. | captureId*: string — Capture id. | `error: capture not found`<br>`ok: deleted capture "${c.body}"` | yes |
| `rename_list` | profile · voice | write | Rename a list. | listId*: string — List id.<br>name*: string — New name. | `error: list not found`<br>`error: name required`<br>`error: you can't edit "${c.name}"`<br>`ok: renamed list "${c.name}" → "${nm}"` | yes |
| `archive_list` | profile · voice | write | Archive (or unarchive) a list. | listId*: string — List id.<br>archived: boolean — Default true. | `error: list not found`<br>`error: you can't edit "${c.name}"`<br>`ok: ${archived ? 'archived' : 'unarchived'} list "${c.name}"` | yes |
| `delete_list` | profile · voice | write | Delete a list — only after the user confirms aloud. | listId*: string — List id. | `error: list not found`<br>`error: you can't edit "${c.name}"`<br>`ok: deleted list "${c.name}"` | yes |
| `edit_list_item` | profile · voice | write | Change a list item's text. | listId*: string — List id.<br>itemId*: string — Item id.<br>body*: string — New text. | `error: list item not found`<br>`error: body required`<br>`error: you can't edit "${c.name}"`<br>`ok: edited item in "${c.name}" → "${body}"` | yes |
| `remove_list_item` | profile · voice | write | Remove an item from a list. | listId*: string — List id.<br>itemId*: string — Item id. | `error: list item not found`<br>`error: you can't edit "${c.name}"`<br>`ok: removed "${item.body}" from "${c.name}"` | yes |
| `set_list_item_done` | profile · voice | write | Tick or untick a list item. | listId*: string — List id.<br>itemId*: string — Item id.<br>done: boolean — Default true. | `error: list item not found`<br>`error: you can't edit "${c.name}"`<br>`ok: ${done ? 'ticked' : 'unticked'} "${item.body}" in "${c.name}"` | yes |
| `create_area` | profile · voice | write | Create a life area. | name*: string — Area name.<br>color: string — Optional palette token. | `error: name required`<br>`error: area "${nm}" already exists`<br>`ok: created area "${nm}"` | yes |
| `rename_area` | profile · voice | write | Rename a life area (tasks follow). | name*: string — Current name.<br>newName*: string — New name. | `error: no area named "${from}" — areas: ${api.getAreas().join(', ')}`<br>`error: newName required`<br>`ok: renamed area "${from}" → "${to}" (tasks updated)` | yes |
| `delete_area` | profile · voice | write | Delete a life area — only after the user confirms. | name*: string — Area name. | `error: no area named "${from}"`<br>`ok: deleted area "${from}" (its tasks keep everything else)` | yes |
| `create_tag` | profile · voice | write | Create a tag. | name*: string — Tag name. | `error: name required`<br>`ok: tag "${nm}" ready` | yes |
| `rename_tag` | profile · voice | write | Rename a tag everywhere. | name*: string — Current name.<br>newName*: string — New name. | `error: no tag named "${from}"`<br>`error: newName required`<br>`ok: renamed tag "${from}" → "${to}"` | yes |
| `delete_tag` | profile · voice | write | Delete a tag everywhere — only after the user confirms. | name*: string — Tag name. | `error: no tag named "${from}"`<br>`ok: deleted tag "${from}" (removed from tasks)` | yes |
| `unshare_task` | profile · voice | write | Stop sharing a task with someone. | taskId*: string — Task id.<br>person: string — Who, as the user named them. | `error: task not found`<br>`error: "${t.name}" isn't shared with anyone`<br>`error: ${!who ? 'say who' : hits.length === 0 ?`<br>`ok: stopped sharing "${t.name}" with ${hits[0].recipientName}` | yes |
| `set_usable_minutes` | profile · voice | write | Set how many minutes a day they have for focus. | weekdayMin: integer — Weekday minutes.<br>weekendMin: integer — Weekend-day minutes. | `error: give weekdayMin and/or weekendMin`<br>`error: minutes must be between 15 and 1440`<br>`ok: usable time set${wd !== undefined ?` | yes |
| `set_notification_level` | profile · voice | write | Set notification style. | level*: string — calm \| balanced \| coach. | `error: level must be calm, balanced, or coach` | yes |
| `set_reminder_lead` | profile · voice | write | How many minutes before a task to remind. | minutes*: integer — 0 (off), 5, 10, or 15. | `error: minutes must be 0 (off), 5, 10, or 15` | yes |
| `set_ritual` | profile · voice | write | Turn a recurring assistant moment on or off. | ritual*: string — morning \| evening \| friday \| sunday.<br>on: boolean — Default true. | `error: ritual must be morning, evening, friday, or sunday`<br>`ok: ${r} moment ${on ? 'on' : 'off'}` | yes |
| `forget_fact` | profile · voice | write | Forget something you remembered about them. | factId: string — Fact id if known.<br>match: string — Or words from the fact. | `ok: forgot "${target.fact}"` | yes |
| `get_insights` | profile · voice | read | How their focus is going — totals, estimate accuracy, why they pause, what is slipping. | window: string — week \| month \| all (default week). | `error: window must be week, month, or all` | — |
| `open_screen` | profile · voice | write | Open a screen in the app. | screen*: string — today \| tasks \| calendar \| week \| month \| focus \| insights \| lists \| captures \| settings \| people \| notifications.<br>id: string — Optional task/list id to open. | `error: unknown screen "${s}" — try today, tasks, calendar, week, month, focus, insights, lists, captures, settings, people, notifications`<br>`ok: opened ${s}` | — |
| `request_call` | profile · voice | write | Book a phone call from Unstuck ONLY when the user asks for one ("call me at 3 about James"). Give when (standalone time) OR taskId with leadMin (rings before its slot). Notes are read back VERBATIM when the call opens — one item each. Never book unasked; you may offer one. | when: string — Local 'YYYY-MM-DD HH:MM' — only when the user gave a time.<br>taskId: string — Task to ring before (uses its next scheduled slot).<br>leadMin: integer — Minutes before the slot (default 15).<br>label*: string — What the call is about, in a few words: "speak to James".<br>notes: array — The user's reminders, verbatim, one per item. | `error: task not found`<br>`error: label required — say what the call is about (e.g. "speak to James")`<br>`error: when must be 'YYYY-MM-DD HH:MM' in the user's local time (got "${when}")`<br>`error: "${t.name}" has no upcoming slot — schedule_task it first, or give a time with when`<br>`error: ${leadMin} min before "${t.name}" (${date} ${hm}) is already past — give a time with when instead`<br>`error: needs a time — ask ONE short question suggesting one (e.g. "3pm today, or a time you prefer?"), then book when they answer`<br>`error: a call is already booked for "${dup.label}" at ${d.date} ${d.hm} id=${dup.id} — update_call or cancel_call it`<br>`ok: call booked ${date} ${hm} "${label}" (${noteCount(notes.length)}) id=${c.id}`<br>`error: <date> is in the PAST (today is <today>). If the user meant the coming <weekday>, use <date> — see context.upcoming.`<br>`error: <HH:MM> today is already past (it's <now> now). Ask for a later time or another day — free today: <windows>.` | yes (undo: cancel_call) |
| `cancel_call` | profile · voice | write | Cancel a booked call. | callId*: string — Call id from get_calls. | `error: callId required — use get_calls to find it`<br>`error: call not found — use get_calls`<br>`error: that call is already ${c.status}`<br>`error: could not cancel the call`<br>`ok: cancelled the call about "${c.label}" (${d.date} ${d.hm})` | yes |
| `update_call` | profile · voice | write | Change a booked call's notes, time, or label. | callId*: string — Call id from get_calls / request_call.<br>when: string — New local 'YYYY-MM-DD HH:MM'.<br>label: string — New label.<br>notes: array — Replaces the notes, verbatim. | `error: callId required — use get_calls to find it`<br>`error: call not found — use get_calls`<br>`error: that call is already ${c.status} — book a new one with request_call`<br>`error: nothing to change — give notes and/or when`<br>`error: that call is in progress right now — I can change its notes or label, but not its time; snooze_call or book another with request_call`<br>`error: when must be 'YYYY-MM-DD HH:MM' in the user's local time (got "${when}")`<br>`ok: updated call "${next.label}" — ${d.date} ${d.hm}, ${noteCount(next.notes.length)} id=${next.id}`<br>`error: <date> is in the PAST (today is <today>). If the user meant the coming <weekday>, use <date> — see context.upcoming.`<br>`error: <HH:MM> today is already past (it's <now> now). Ask for a later time or another day — free today: <windows>.` | yes |
| `get_calls` | profile · voice | read | List the calls booked from Unstuck (with ids). | (none) | `ok: no calls booked`<br>`- ${d.date} ${d.hm} "${c.label}" (${noteCount(c.notes.length)})${t ?`<br>`ok: ${live.length} upcoming call${live.length === 1 ? '' : 's'}:\n${lines.join('\n')}` | — |
